name: M3 PWA validation

on:
  pull_request:
    branches: [main]
    paths:
      - "src/app/**"
      - "public/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/m3-pwa-validation-temp.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  web:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: salespunch360
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d salespunch360"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/salespunch360
      DIRECT_URL: postgresql://postgres:postgres@localhost:5432/salespunch360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate
      - run: npx prisma validate
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test -- src/lib/mobile/android-f3m-correction.contract.test.ts
      - name: Validate PWA assets
        run: |
          test -f public/sw.js
          test -f src/app/manifest.ts
          test -f src/app/pwa-register.tsx
          grep -q 'display: "standalone"' src/app/manifest.ts
          grep -q 'manifest: "/manifest.webmanifest"' src/app/layout.tsx
          grep -q 'serviceWorker.register("/sw.js"' src/app/pwa-register.tsx
      - run: npm run build
