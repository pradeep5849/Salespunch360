import type { QuotationInput } from "./service";
export function quotationEditorInitial(document: Record<string, unknown> & { branchId:string;documentType:string;customerId:string|null;sourceLeadId:string|null;revisions:Array<Record<string,unknown>&{issueDate:Date;validUntil:Date|null;lines:Array<Record<string,unknown>>;adjustments:Array<Record<string,unknown>>;paymentSchedule:Array<Record<string,unknown>>}> }):QuotationInput {const revision=document.revisions[0];return {branchId:document.branchId,documentType:document.documentType as QuotationInput["documentType"],...(document.customerId?{customerId:document.customerId}:{sourceLeadId:document.sourceLeadId!}),issueDate:revision.issueDate,validUntil:revision.validUntil??undefined,terms:String(revision.terms??""),inclusions:String(revision.inclusions??""),exclusions:String(revision.exclusions??""),notes:String(revision.notes??""),lines:revision.lines.map(line=>({lineType:line.lineType as QuotationInput["lines"][number]["lineType"],sourceId:String(line.productId??line.serviceId??line.workPackageId??"")||undefined,unitId:String(line.unitId??"")||undefined,itemName:String(line.itemName??""),description:String(line.description??""),specification:String(line.specification??""),unitName:String(line.unitName??""),unitSymbol:String(line.unitSymbol??""),quantity:String(line.quantity),rate:String(line.rate),...("internalUnitCost" in line?{internalUnitCost:String(line.internalUnitCost)}:{}),taxRate:String(line.taxRate),discountType:(line.discountType??null) as "PERCENTAGE"|"FIXED"|null,discountValue:line.discountValue?String(line.discountValue):undefined})),adjustments:revision.adjustments.map(row=>({type:row.type as "DISCOUNT"|"ADDITIONAL_CHARGE",label:String(row.label),valueType:row.valueType as "PERCENTAGE"|"FIXED",value:String(row.value),taxable:Boolean(row.taxable),taxRate:String(row.taxRate??0)})),paymentSchedule:revision.paymentSchedule.map(row=>({label:String(row.label),valueType:row.valueType as "PERCENTAGE"|"FIXED",value:String(row.value),dueDescription:String(row.dueDescription??"")}))};}
