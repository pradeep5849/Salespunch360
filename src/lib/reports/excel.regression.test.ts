import{readFileSync}from"node:fs";import ExcelJS from"exceljs";import{describe,expect,it}from"vitest";
const route=readFileSync("src/app/api/reports/[report]/excel/route.ts","utf8");
describe("Excel report export",()=>{it("produces valid xlsx bytes",async()=>{const book=new ExcelJS.Workbook();book.addWorksheet("Report").addRow(["Company","Acme"]);const bytes=await book.xlsx.writeBuffer();expect(Buffer.from(bytes).subarray(0,2).toString()).toBe("PK")});it("preserves filters, ignores UI pagination, and enforces ceiling",()=>{expect(route).toContain('all={...raw,page:"1",pageSize:String(MAX_ROWS)}');expect(route).toContain("MAX_ROWS=10_000");expect(route).toContain("Narrow your filters");expect(route).toContain("Object.entries(raw)")});it("supports every approved report and India time",()=>{for(const name of["check-ins","advanced-check-ins","attendance","gps","leads","geofence-breaches","targets","expenses"])expect(route).toContain(name);expect(route).toContain('timeZone:"Asia/Kolkata"')});it("server-derives report scope and reuses cleaned distance",()=>{expect(route).toContain("reportActor()");expect(readFileSync("src/lib/reports/expenses.ts","utf8")).toContain("calculateTravelDistanceMeters");expect(readFileSync("src/lib/reports/gps.ts","utf8")).toContain("calculateTravelDistanceMeters")})});
