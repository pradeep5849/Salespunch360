import{describe,expect,it}from"vitest";import{assertOperationalBranch,operationalBranchWhere,resolveWriteBranch}from"./operational-scope";
const actor={id:"u",companyId:"a",branchAccessScope:"SELECTED_BRANCHES" as const,role:"SALES",salesRole:"SALES",isActive:true,salesAccessActive:true};const one={id:"one",companyId:"a",isActive:true},two={id:"two",companyId:"a",isActive:true};
describe("operational Branch security",()=>{it("denies cross-tenant, inactive, Account-only, and Super Admin principals",()=>{for(const pair of [[actor,{...one,companyId:"b"}], [actor,{...one,isActive:false}], [{...actor,salesRole:null,salesAccessActive:false},one],[{...actor,role:"SUPER_ADMIN"},one]] as const)expect(()=>assertOperationalBranch(pair[0],pair[1])).toThrow("BRANCH_FORBIDDEN")});it("requires explicit context when multiple Branches are usable",()=>{expect(resolveWriteBranch([one])).toBe("one");expect(()=>resolveWriteBranch([one,two])).toThrow("BRANCH_REQUIRED");expect(resolveWriteBranch([one,two],"two")).toBe("two")});it("cannot expand selected-Branch reads through query manipulation",()=>{expect(operationalBranchWhere(actor,["one"],undefined)).toEqual({in:["one"]});expect(()=>operationalBranchWhere(actor,["one"],"two")).toThrow("BRANCH_FORBIDDEN")})});
