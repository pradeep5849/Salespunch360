import{mkdtemp,readFile}from"node:fs/promises";import{tmpdir}from"node:os";import path from"node:path";import{describe,expect,it}from"vitest";import{HostingerStorage}from"./hostinger";import{assertSafeObjectKey}from"./types";import{visitPhotoKeys}from"./index";
describe("private check-in storage",()=>{it.each(["../secret","/absolute","a/../../b","a\\b"])("blocks traversal key %s",key=>expect(()=>assertSafeObjectKey(key)).toThrow("INVALID_STORAGE_KEY"));it("uses opaque identifiers under the private root",async()=>{const root=await mkdtemp(path.join(tmpdir(),"sp-storage-")),storage=new HostingerStorage(root),keys=visitPhotoKeys("company-id","user-id","visit-id");expect(keys.objectKey).toBe("companies/company-id/employees/user-id/check-ins/visit-id/photo.webp");await storage.put(keys.objectKey,Buffer.from("private"));expect((await readFile(path.join(root,keys.objectKey))).toString()).toBe("private")});});
