import {Prisma} from "@prisma/client";
export type TargetStatus="UPCOMING"|"IN_PROGRESS"|"ACHIEVED"|"MISSED";
export function wonActual(metric:"LEADS_CREATED_COUNT"|"WON_LEADS_COUNT"|"WON_LEADS_VALUE",leads:{estimatedValue:Prisma.Decimal|null}[]){return (metric==="LEADS_CREATED_COUNT"||metric==="WON_LEADS_COUNT")?new Prisma.Decimal(leads.length):leads.reduce((n,l)=>n.plus(l.estimatedValue||0),new Prisma.Decimal(0))}
export function targetProgress(target:Prisma.Decimal,actual:Prisma.Decimal,start:Date,endExclusive:Date,now=new Date()){const remaining=Prisma.Decimal.max(target.minus(actual),0),percentage=target.isZero()?new Prisma.Decimal(actual.isZero()?0:100):actual.div(target).mul(100);const status:TargetStatus=actual.greaterThanOrEqualTo(target)?"ACHIEVED":now<start?"UPCOMING":now>=endExclusive?"MISSED":"IN_PROGRESS";return {actual,remaining,percentage,status}}
