import{describe,expect,it}from"vitest";import{canAssign,transitionKind,visibilityWhere,LeadError}from"./policy";
const u=(id:string,role:"MANAGER"|"SALES",managerId:string|null=null,isActive=true)=>({id,role,isActive,managerId});
describe("lead authorization",()=>{it("enforces assignment hierarchy and active status",()=>{expect(canAssign({id:"s",role:"SALES"},u("s","SALES"))).toBe(true);expect(canAssign({id:"s",role:"SALES"},u("x","SALES"))).toBe(false);expect(canAssign({id:"m",role:"MANAGER"},u("s","SALES","m"))).toBe(true);expect(canAssign({id:"m",role:"MANAGER"},u("s","SALES","other"))).toBe(false);expect(canAssign({id:"m",role:"MANAGER"},u("other","MANAGER"))).toBe(false);expect(canAssign({id:"a",role:"COMPANY_ADMIN"},u("s","SALES",null,false))).toBe(false);expect(canAssign({id:"a",role:"COMPANY_ADMIN"},u("m","MANAGER"))).toBe(true)});it("derives visibility from current manager relationship",()=>{expect(visibilityWhere({id:"s",role:"SALES"})).toEqual({assignedUserId:"s"});expect(visibilityWhere({id:"m",role:"MANAGER"})).toEqual({OR:[{assignedUserId:"m"},{assignedUser:{role:"SALES",managerId:"m"}}]})});it("allows active movement and admin-only reopen",()=>{expect(transitionKind("SALES","NEW","WON")).toBe("WON");expect(transitionKind("MANAGER","PROPOSAL","LOST")).toBe("LOST");expect(()=>transitionKind("MANAGER","WON","NEW")).toThrow(LeadError);expect(transitionKind("COMPANY_ADMIN","LOST","QUALIFIED")).toBe("REOPENED")})});
