import{describe,expect,it}from"vitest";import{createLeadSchema,moneySchema,transitionLeadSchema}from"./validation";
const base={title:" Opportunity ",assignedUserId:"11111111-1111-4111-8111-111111111111",source:"MANUAL",currencyCode:"inr"};describe("lead validation",()=>{it("rejects mass assignment and browser lifecycle state",()=>{expect(()=>createLeadSchema.parse({...base,companyId:"x"})).toThrow();expect(()=>createLeadSchema.parse({...base,createdByUserId:"x"})).toThrow();expect(()=>createLeadSchema.parse({...base,stage:"WON"})).toThrow()});it("normalizes safe fields",()=>{const x=createLeadSchema.parse({...base,email:" TEST@EXAMPLE.COM "});expect(x.title).toBe("Opportunity");expect(x.email).toBe("test@example.com");expect(x.currencyCode).toBe("INR")});it("validates decimal strings without floating point",()=>{expect(moneySchema.parse("12.34")).toBe("12.34");expect(()=>moneySchema.parse("-1")).toThrow();expect(()=>moneySchema.parse("1.234")).toThrow()});it("requires a lost reason",()=>{expect(()=>transitionLeadSchema.parse({leadId:"11111111-1111-4111-8111-111111111111",version:1,toStage:"LOST"})).toThrow()})});
