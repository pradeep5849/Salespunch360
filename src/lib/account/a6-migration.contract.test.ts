import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("prisma/migrations/20260911120000_a6_projects/migration.sql","utf8");
describe("A6 migration contract",()=>{it("adds backward-compatible nullable strong links without text backfill",()=>{expect(sql).toContain('commercial_documents" ADD COLUMN     "projectId" UUID');expect(sql).toContain('quotation_documents" ADD COLUMN     "projectId" UUID');expect(sql).not.toMatch(/UPDATE\s+"?commercial_documents/i);expect(sql).not.toContain("session_replication_role")});it("uses same-company restricted foreign keys",()=>{expect(sql).toContain('FOREIGN KEY ("companyId", "projectId") REFERENCES "projects"("companyId", "id") ON DELETE RESTRICT');expect(sql).toContain('FOREIGN KEY ("companyId", "customerId") REFERENCES "customers"("companyId", "id") ON DELETE RESTRICT')});it("keeps branch-aware project numbers unique and financial history restricted",()=>{expect(sql).toContain('projects_companyId_branchId_projectNumber_key');expect(sql).not.toMatch(/ON DELETE CASCADE/)})});
