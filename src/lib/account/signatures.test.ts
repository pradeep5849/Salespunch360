import{describe,expect,it}from"vitest";import{processSignatureImage,signatureObjectKey,MAX_SIGNATURE_BYTES}from"./signatures";
describe("Y-03 signature image safety",()=>{it("uses immutable tenant-scoped private keys",()=>expect(signatureObjectKey("company","version")).toBe("Signatures/company/version.webp"));it("rejects MIME spoofing",async()=>await expect(processSignatureImage(new File(["not image"],"x.png",{type:"image/png"}))).rejects.toThrow("SIGNATURE_INVALID"));it("rejects unsupported and oversized files",async()=>{await expect(processSignatureImage(new File(["x"],"x.svg",{type:"image/svg+xml"}))).rejects.toThrow("SIGNATURE_INVALID");await expect(processSignatureImage(new File([new Uint8Array(MAX_SIGNATURE_BYTES+1)],"x.png",{type:"image/png"}))).rejects.toThrow("SIGNATURE_INVALID")})});
