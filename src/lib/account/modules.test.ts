import {LEGACY_DEFAULT_ACCOUNT_MODULES,describe,expect,it} from "vitest";import {ACCOUNT_MODULE_CATALOG,IMPLEMENTED_ACCOUNT_MODULES,recommendedModulesForBusinessType,validateEnabledModules} from "./modules";
describe("A4/A5 module policy",()=>{it.each(["INTERIOR_CONSTRUCTION","RETAIL_TRADING","SERVICE_BUSINESS","MANUFACTURING","OTHER_MIXED"] as const)("returns dependency-safe implemented defaults for %s",type=>{const result=recommendedModulesForBusinessType(type);expect(result.length).toBeGreaterThan(0);expect(result.every(x=>(IMPLEMENTED_ACCOUNT_MODULES as readonly string[]).includes(x))).toBe(true);for(const key of result){const parent=ACCOUNT_MODULE_CATALOG[key].parent;if(parent)expect(result).toContain(parent)}});it("keeps construction BOQ and subcontractors",()=>expect(recommendedModulesForBusinessType("INTERIOR_CONSTRUCTION")).toEqual(expect.arrayContaining(["QUOTATIONS_BOQ","SUBCONTRACTORS"])));it("keeps retail free of BOQ and subcontractors",()=>expect(recommendedModulesForBusinessType("RETAIL_TRADING")).not.toEqual(expect.arrayContaining(["QUOTATIONS_BOQ","SUBCONTRACTORS"])));it("gives OTHER_MIXED common sales and purchases",()=>expect(recommendedModulesForBusinessType("OTHER_MIXED")).toEqual(expect.arrayContaining(["SALES","PURCHASES","CUSTOMER_RECEIPTS","VENDOR_PAYMENTS"])));it("rejects an unknown business type",()=>expect(()=>recommendedModulesForBusinessType("UNKNOWN")).toThrow());it("rejects unknown keys",()=>expect(()=>validateEnabledModules(["SALES","BOGUS"])).toThrow());it("activates Project Costing with its Projects dependency",()=>{expect(validateEnabledModules(["PROJECTS","PROJECT_COSTING"])).toEqual(["PROJECTS","PROJECT_COSTING"]);expect(()=>validateEnabledModules(["PROJECT_COSTING"])).toThrow("MODULE_DEPENDENCY_REQUIRED")});it("does not auto-enable new financial modules for legacy companies",()=>expect(LEGACY_DEFAULT_ACCOUNT_MODULES).not.toEqual(expect.arrayContaining(["PROJECTS","PROJECT_COSTING","EXPENSES"])));it("enforces parent dependencies",()=>expect(()=>validateEnabledModules(["SALES_ORDER"])).toThrow("MODULE_DEPENDENCY_REQUIRED:SALES_ORDER:SALES"));it("accepts parent with child",()=>expect(validateEnabledModules(["SALES_ORDER","SALES"])).toEqual(["SALES","SALES_ORDER"]))});
