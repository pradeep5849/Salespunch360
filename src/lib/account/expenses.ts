import{randomUUID}from"node:crypto";import{ExpenseCategoryScope,ExpenseTransactionStatus,ExpenseTransactionType,Prisma,RecurringFrequency}from"@prisma/client";import{z}from"zod";import{db}from"@/lib/db";import{AuthorizationError,requirePermission,requirePermissionForMutation}from"@/lib/auth/authorization";import{requireAccountModules}from"./modules";import{authorizedProjectBranchIds,projectRecordScope,type ProjectActor}from"./projects";import{allocateDocumentNumberInTx}from"./numbering";import{postJournalInTx,reverseJournal}from"@/lib/accounting/service";import{privateStorage}from"@/lib/storage";const D=Prisma.Decimal,Z=new D(0),money=z.string().regex(/^\d{1,16}(\.\d{1,2})?$/);
export function expensePosting(input:{type:"PROJECT_EXPENSE"|"OFFICE_EXPENSE"|"OTHER_INCOME"|"REIMBURSEMENT";taxable:Prisma.Decimal;taxRate:Prisma.Decimal;categoryLedgerId:string;inputTaxLedgerId:string;destinationLedgerId:string}){if(input.taxable.lte(0)||input.taxRate.lt(0))throw new Error("INVALID_EXPENSE_AMOUNT");const tax=input.taxable.mul(input.taxRate).div(100).toDecimalPlaces(2),total=input.taxable.add(tax);return input.type==="OTHER_INCOME"?{total,lines:[{ledgerAccountId:input.destinationLedgerId,debit:total,credit:Z},{ledgerAccountId:input.categoryLedgerId,debit:Z,credit:total}]}:{total,lines:[{ledgerAccountId:input.categoryLedgerId,debit:input.taxable,credit:Z},...(tax.gt(0)?[{ledgerAccountId:input.inputTaxLedgerId,debit:tax,credit:Z}]:[]),{ledgerAccountId:input.destinationLedgerId,debit:Z,credit:total}]}}
export const requiresExpenseApproval=(total:Prisma.Decimal,required:boolean,threshold:Prisma.Decimal)=>required&&total.gt(threshold);export const recurringOccurrenceKey=(templateId:string,due:Date)=>`${templateId}:${due.toISOString().slice(0,10)}`;
type Actor=ProjectActor;async function actor(permission:"ACCOUNT_EXPENSE_VIEW"|"ACCOUNT_EXPENSE_ENTRY"|"ACCOUNT_EXPENSE_APPROVE",mutation=false){const a=(mutation?await requirePermissionForMutation(permission):await requirePermission(permission))as Actor;await requireAccountModules(a,"EXPENSES");return a}const branchOk=(a:Actor,id:string)=>a.branchAccessScope!=="SELECTED_BRANCHES"||a.branchIds?.includes(id);
async function audit(tx:Prisma.TransactionClient,a:Actor,eventType:string,entityType:string,entityId:string,metadata?:Prisma.InputJsonValue){await tx.accountOperationalAudit.create({data:{companyId:a.companyId,actorUserId:a.id,eventType,entityType,entityId,metadata}})}
const categoryInput=z.object({name:z.string().trim().min(1).max(160),scope:z.nativeEnum(ExpenseCategoryScope),defaultLedgerAccountId:z.string().uuid()});export async function saveExpenseCategory(raw:unknown,id?:string){const a=await actor("ACCOUNT_EXPENSE_APPROVE",true);if(a.accountRole!=="ACCOUNT_ADMIN")throw new AuthorizationError();const d=categoryInput.parse(raw),ledger=await db.ledgerAccount.findFirst({where:{id:d.defaultLedgerAccountId,companyId:a.companyId,isActive:true,allowPosting:true,accountClass:d.scope==="INCOME"?"INCOME":"EXPENSE"}});if(!ledger)throw new Error("INVALID_CATEGORY_LEDGER");if(id){const changed=await db.expenseCategory.updateMany({where:{id,companyId:a.companyId},data:d});if(changed.count!==1)throw new AuthorizationError();return}return db.expenseCategory.create({data:{...d,companyId:a.companyId}})}export async function deactivateExpenseCategory(id:string){const a=await actor("ACCOUNT_EXPENSE_APPROVE",true);if(a.accountRole!=="ACCOUNT_ADMIN")throw new AuthorizationError();const changed=await db.expenseCategory.updateMany({where:{id,companyId:a.companyId},data:{isActive:false}});if(changed.count!==1)throw new AuthorizationError()}
const expenseInput=z.object({branchId:z.string().uuid(),projectId:z.string().uuid().optional(),categoryId:z.string().uuid(),type:z.nativeEnum(ExpenseTransactionType),transactionDate:z.coerce.date(),taxableAmount:money,taxRate:money.default("0"),moneyAccountId:z.string().uuid().optional(),employeeReimbursementId:z.string().uuid().optional(),reference:z.string().max(160).optional(),notes:z.string().max(5000).optional()}).strict();async function validateExpenseRelations(a:Actor,d:z.infer<typeof expenseInput>){if(!branchOk(a,d.branchId)||!await db.branch.findFirst({where:{id:d.branchId,companyId:a.companyId,isActive:true}}))throw new Error("INVALID_BRANCH");const category=await db.expenseCategory.findFirst({where:{id:d.categoryId,companyId:a.companyId,isActive:true}});if(!category)throw new Error("INVALID_EXPENSE_CATEGORY");if(d.type==="PROJECT_EXPENSE"&&!d.projectId)throw new Error("PROJECT_REQUIRED");if(d.projectId){await requireAccountModules(a,"PROJECTS");const ids=await authorizedProjectBranchIds(a),project=await db.project.findFirst({where:{id:d.projectId,branchId:d.branchId,status:{notIn:["CLOSED","CANCELLED"]},...projectRecordScope(a,ids)}});if(!project)throw new AuthorizationError()}if(d.type!=="REIMBURSEMENT"&&!d.moneyAccountId)throw new Error("MONEY_ACCOUNT_REQUIRED");if(d.moneyAccountId&&!await db.moneyAccount.findFirst({where:{id:d.moneyAccountId,companyId:a.companyId,isActive:true,OR:[{branchId:null},{branchId:d.branchId}]}}))throw new Error("INVALID_MONEY_ACCOUNT");if(d.employeeReimbursementId&&!await db.employeeReimbursement.findFirst({where:{id:d.employeeReimbursementId,companyId:a.companyId,branchId:d.branchId,status:"APPROVED"}}))throw new Error("INVALID_REIMBURSEMENT");return category}
export async function createExpense(raw:unknown,occurrenceKey?:string){const a=await actor("ACCOUNT_EXPENSE_ENTRY",true),d=expenseInput.parse(raw),category=await validateExpenseRelations(a,d),taxable=new D(d.taxableAmount),tax=taxable.mul(d.taxRate).div(100).toDecimalPlaces(2),total=taxable.add(tax),settings=await db.accountSettings.findUnique({where:{companyId:a.companyId}}),approval=requiresExpenseApproval(total,settings?.expenseApprovalRequired??false,settings?.expenseApprovalThreshold??Z);return db.$transaction(async tx=>{const transactionNumber=await allocateDocumentNumberInTx(tx,{companyId:a.companyId,branchId:d.branchId,seriesKey:"EXPENSE",defaults:{prefix:d.type==="OTHER_INCOME"?"OI-":"EXP-",padding:6}}),row=await tx.expenseTransaction.create({data:{...d,companyId:a.companyId,transactionNumber,taxableAmount:taxable,taxRate:new D(d.taxRate),taxAmount:tax,totalAmount:total,status:approval?"DRAFT":"APPROVED",approvedAt:approval?null:new Date(),approvedById:approval?null:a.id,occurrenceKey}});await audit(tx,a,"EXPENSE_CREATED","EXPENSE",row.id,{categoryId:category.id});return row},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
export async function updateExpense(id:string,raw:unknown){const a=await actor("ACCOUNT_EXPENSE_ENTRY",true),d=expenseInput.parse(raw);await validateExpenseRelations(a,d);const taxable=new D(d.taxableAmount),tax=taxable.mul(d.taxRate).div(100).toDecimalPlaces(2),changed=await db.expenseTransaction.updateMany({where:{id,companyId:a.companyId,status:"DRAFT",createdById:a.id},data:{...d,taxableAmount:taxable,taxRate:new D(d.taxRate),taxAmount:tax,totalAmount:taxable.add(tax)}});if(changed.count!==1)throw new Error("EXPENSE_NOT_EDITABLE")}
export async function transitionExpense(id:string,to:ExpenseTransactionStatus){const permission=["APPROVED","REJECTED"].includes(to)?"ACCOUNT_EXPENSE_APPROVE":"ACCOUNT_EXPENSE_ENTRY",a=await actor(permission,true);return db.$transaction(async tx=>{await tx.$queryRaw`SELECT "id" FROM "expense_transactions" WHERE "id"=${id}::uuid AND "companyId"=${a.companyId}::uuid FOR UPDATE`;const row=await tx.expenseTransaction.findFirst({where:{id,companyId:a.companyId}});if(!row)throw new AuthorizationError();const allowed:Partial<Record<ExpenseTransactionStatus,ExpenseTransactionStatus[]>>={DRAFT:["PENDING_APPROVAL","CANCELLED"],PENDING_APPROVAL:["APPROVED","REJECTED","CANCELLED"]};if(!allowed[row.status]?.includes(to))throw new Error("INVALID_EXPENSE_TRANSITION");if(["APPROVED","REJECTED"].includes(to)&&a.accountRole!=="ACCOUNT_ADMIN")throw new AuthorizationError();const updated=await tx.expenseTransaction.update({where:{id},data:{status:to,...(to==="APPROVED"?{approvedById:a.id,approvedAt:new Date()}:{})}});await audit(tx,a,`EXPENSE_${to}`,"EXPENSE",id);return updated},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
export async function postExpense(id:string){const a=await actor("ACCOUNT_EXPENSE_ENTRY",true);if(!["ACCOUNT_ADMIN","ACCOUNTANT"].includes(a.accountRole??""))throw new AuthorizationError();return db.$transaction(async tx=>{await tx.$queryRaw`SELECT "id" FROM "expense_transactions" WHERE "id"=${id}::uuid AND "companyId"=${a.companyId}::uuid FOR UPDATE`;const row=await tx.expenseTransaction.findFirst({where:{id,companyId:a.companyId},include:{}});if(!row)throw new AuthorizationError();if(row.status==="POSTED"&&row.journalEntryId)return tx.journalEntry.findFirstOrThrow({where:{id:row.journalEntryId,companyId:a.companyId}});if(row.status!=="APPROVED")throw new Error("EXPENSE_NOT_APPROVED");const [category,money,fy,system]=await Promise.all([tx.expenseCategory.findFirst({where:{id:row.categoryId,companyId:a.companyId,isActive:true}}),row.moneyAccountId?tx.moneyAccount.findFirst({where:{id:row.moneyAccountId,companyId:a.companyId,isActive:true}}):null,tx.financialYear.findFirst({where:{companyId:a.companyId,isActive:true,startDate:{lte:row.transactionDate},endDate:{gte:row.transactionDate}}}),tx.ledgerAccount.findMany({where:{companyId:a.companyId,systemKey:{in:["INPUT_TAX_CREDIT","ACCOUNTS_PAYABLE"]},isActive:true,allowPosting:true}})]);if(!category||!fy)throw new Error("EXPENSE_POSTING_CONTEXT_INVALID");const byKey=new Map(system.map(x=>[x.systemKey,x.id])),destination=money?.ledgerAccountId??byKey.get("ACCOUNTS_PAYABLE");if(!destination)throw new Error("EXPENSE_DESTINATION_MISSING");const posting=expensePosting({type:row.type,taxable:row.taxableAmount,taxRate:row.taxRate,categoryLedgerId:category.defaultLedgerAccountId,inputTaxLedgerId:byKey.get("INPUT_TAX_CREDIT")??"",destinationLedgerId:destination}),journal=await postJournalInTx(tx,a,{financialYearId:fy.id,branchId:row.branchId,entryDate:row.transactionDate,reference:row.reference,narration:row.notes,sourceType:row.type,sourceId:row.id,postingPurpose:"PRIMARY",lines:posting.lines.map(l=>({ledgerAccountId:l.ledgerAccountId,debit:l.debit.toFixed(2),credit:l.credit.toFixed(2)}))}),changed=await tx.expenseTransaction.updateMany({where:{id:row.id,companyId:a.companyId,status:"APPROVED",journalEntryId:null},data:{status:"POSTED",journalEntryId:journal.id}});if(changed.count!==1)throw new Error("STALE_EXPENSE");await audit(tx,a,row.type==="OTHER_INCOME"?"OTHER_INCOME_POSTED":"EXPENSE_POSTED","EXPENSE",row.id,{journalEntryId:journal.id});return journal},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
export async function reverseExpense(id:string,entryDate:Date,reason:string){const a=await actor("ACCOUNT_EXPENSE_APPROVE",true),row=await db.expenseTransaction.findFirst({where:{id,companyId:a.companyId,status:"POSTED"}});if(!row?.journalEntryId)throw new Error("EXPENSE_NOT_REVERSIBLE");const reversal=await reverseJournal({journalEntryId:row.journalEntryId,entryDate,reason});await db.$transaction(async tx=>{const changed=await tx.expenseTransaction.updateMany({where:{id,companyId:a.companyId,status:"POSTED",reversalJournalId:null},data:{status:"REVERSED",reversalJournalId:reversal.id}});if(changed.count!==1)throw new Error("EXPENSE_ALREADY_REVERSED");await audit(tx,a,"EXPENSE_REVERSED","EXPENSE",id,{reversalJournalId:reversal.id})});return reversal}
export async function listExpenses(){const a=await actor("ACCOUNT_EXPENSE_VIEW"),branch=a.branchAccessScope==="SELECTED_BRANCHES"?{branchId:{in:a.branchIds??[]}}:{};return db.expenseTransaction.findMany({where:{companyId:a.companyId,...branch},orderBy:{transactionDate:"desc"},take:200})}export async function getExpense(id:string){const a=await actor("ACCOUNT_EXPENSE_VIEW"),row=await db.expenseTransaction.findFirst({where:{id,companyId:a.companyId,...(a.branchAccessScope==="SELECTED_BRANCHES"?{branchId:{in:a.branchIds??[]}}:{})}});if(!row)throw new AuthorizationError();return row}export async function expenseOptions(){const a=await actor("ACCOUNT_EXPENSE_VIEW"),branchIds=a.branchAccessScope==="SELECTED_BRANCHES"?a.branchIds??[]:(await db.branch.findMany({where:{companyId:a.companyId,isActive:true},select:{id:true}})).map(x=>x.id);return{categories:await db.expenseCategory.findMany({where:{companyId:a.companyId,isActive:true}}),branches:await db.branch.findMany({where:{companyId:a.companyId,id:{in:branchIds},isActive:true}}),moneyAccounts:await db.moneyAccount.findMany({where:{companyId:a.companyId,isActive:true}}),projects:await db.project.findMany({where:{companyId:a.companyId,branchId:{in:branchIds},status:{notIn:["CLOSED","CANCELLED"]}}})}}
const templateInput=z.object({branchId:z.string().uuid(),projectId:z.string().uuid().optional(),categoryId:z.string().uuid(),frequency:z.nativeEnum(RecurringFrequency),nextDueDate:z.coerce.date(),amount:money});export async function createRecurringTemplate(raw:unknown){const a=await actor("ACCOUNT_EXPENSE_ENTRY",true),d=templateInput.parse(raw);if(!branchOk(a,d.branchId))throw new AuthorizationError();return db.recurringExpenseTemplate.create({data:{...d,amount:new D(d.amount),companyId:a.companyId,createdById:a.id}})}const next=(date:Date,f:RecurringFrequency)=>{const x=new Date(date);if(f==="WEEKLY")x.setUTCDate(x.getUTCDate()+7);else if(f==="MONTHLY")x.setUTCMonth(x.getUTCMonth()+1);else if(f==="QUARTERLY")x.setUTCMonth(x.getUTCMonth()+3);else x.setUTCFullYear(x.getUTCFullYear()+1);return x};export async function generateRecurringExpense(templateId:string,moneyAccountId:string){const a=await actor("ACCOUNT_EXPENSE_ENTRY",true);return db.$transaction(async tx=>{await tx.$queryRaw`SELECT "id" FROM "recurring_expense_templates" WHERE "id"=${templateId}::uuid AND "companyId"=${a.companyId}::uuid FOR UPDATE`;const t=await tx.recurringExpenseTemplate.findFirst({where:{id:templateId,companyId:a.companyId,isActive:true}});if(!t)throw new AuthorizationError();const key=recurringOccurrenceKey(t.id,t.nextDueDate),existing=await tx.expenseTransaction.findFirst({where:{companyId:a.companyId,occurrenceKey:key}});if(existing)return existing;const number=await allocateDocumentNumberInTx(tx,{companyId:a.companyId,branchId:t.branchId,seriesKey:"EXPENSE",defaults:{prefix:"EXP-",padding:6}}),money=await tx.moneyAccount.findFirst({where:{id:moneyAccountId,companyId:a.companyId,isActive:true}});if(!money)throw new Error("INVALID_MONEY_ACCOUNT");const row=await tx.expenseTransaction.create({data:{companyId:a.companyId,branchId:t.branchId,projectId:t.projectId,categoryId:t.categoryId,type:t.projectId?"PROJECT_EXPENSE":"OFFICE_EXPENSE",status:"DRAFT",transactionNumber:number,transactionDate:t.nextDueDate,taxableAmount:t.amount,taxAmount:Z,taxRate:Z,totalAmount:t.amount,moneyAccountId,occurrenceKey:key,createdById:a.id}});await tx.recurringExpenseTemplate.update({where:{id:t.id},data:{nextDueDate:next(t.nextDueDate,t.frequency)}});await audit(tx,a,"RECURRING_OCCURRENCE_CREATED","EXPENSE",row.id,{templateId:t.id});return row},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
const allowedMime=new Set(["application/pdf","image/jpeg","image/png"]);export async function addExpenseAttachment(expenseId:string,file:File){const a=await actor("ACCOUNT_EXPENSE_ENTRY",true);if(!file.size||file.size>10*1024*1024||!allowedMime.has(file.type))throw new Error("INVALID_EXPENSE_ATTACHMENT");await getExpense(expenseId);const id=randomUUID(),key=`companies/${a.companyId}/expenses/${expenseId}/${id}`;await privateStorage().put(key,Buffer.from(await file.arrayBuffer()));try{return await db.$transaction(async tx=>{const row=await tx.expenseAttachment.create({data:{id,companyId:a.companyId,expenseId,storageKey:key,displayName:file.name.slice(0,240),mimeType:file.type,sizeBytes:file.size,uploadedById:a.id}});await audit(tx,a,"ATTACHMENT_ADDED","EXPENSE",expenseId,{attachmentId:id});return row})}catch(e){await privateStorage().delete(key).catch(()=>undefined);throw e}}export async function downloadExpenseAttachment(id:string){const a=await actor("ACCOUNT_EXPENSE_VIEW"),row=await db.expenseAttachment.findFirst({where:{id,companyId:a.companyId}});if(!row)throw new AuthorizationError();await getExpense(row.expenseId);return{data:await privateStorage().get(row.storageKey),name:row.displayName,mimeType:row.mimeType}}
