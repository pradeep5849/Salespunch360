import {Prisma} from "@prisma/client";const D=Prisma.Decimal;
export function expensePosting(input:{type:"PROJECT_EXPENSE"|"OFFICE_EXPENSE"|"OTHER_INCOME"|"REIMBURSEMENT";taxable:Prisma.Decimal;taxRate:Prisma.Decimal;categoryLedgerId:string;inputTaxLedgerId:string;destinationLedgerId:string}){if(input.taxable.lte(0)||input.taxRate.lt(0))throw new Error("INVALID_EXPENSE_AMOUNT");const tax=input.taxable.mul(input.taxRate).div(100).toDecimalPlaces(2),total=input.taxable.add(tax);return input.type==="OTHER_INCOME"?{total,lines:[{ledgerAccountId:input.destinationLedgerId,debit:total,credit:new D(0)},{ledgerAccountId:input.categoryLedgerId,debit:new D(0),credit:total}]}:{total,lines:[{ledgerAccountId:input.categoryLedgerId,debit:input.taxable,credit:new D(0)},...(tax.gt(0)?[{ledgerAccountId:input.inputTaxLedgerId,debit:tax,credit:new D(0)}]:[]),{ledgerAccountId:input.destinationLedgerId,debit:new D(0),credit:total}]}}
export const requiresExpenseApproval=(total:Prisma.Decimal,required:boolean,threshold:Prisma.Decimal)=>required&&total.gt(threshold);
export const recurringOccurrenceKey=(templateId:string,due:Date)=>`${templateId}:${due.toISOString().slice(0,10)}`;
