import{Prisma}from"@prisma/client";const D=Prisma.Decimal,Z=new D(0);export function transferLines(sourceLedgerId:string,destinationLedgerId:string,amount:Prisma.Decimal){if(amount.lte(0))throw new Error("INVALID_AMOUNT");if(sourceLedgerId===destinationLedgerId)throw new Error("SAME_MONEY_ACCOUNT");return[{ledgerAccountId:destinationLedgerId,debit:amount,credit:Z},{ledgerAccountId:sourceLedgerId,debit:Z,credit:amount}]};export function loanPaymentLines(liability:string,interestExpense:string,chargesExpense:string,money:string,principal:Prisma.Decimal,interest:Prisma.Decimal,charges=new D(0)){if(principal.lt(0)||interest.lt(0)||charges.lt(0)||principal.add(interest).add(charges).lte(0))throw new Error("INVALID_LOAN_PAYMENT");return[{ledgerAccountId:liability,debit:principal,credit:Z},{ledgerAccountId:interestExpense,debit:interest,credit:Z},...(charges.gt(0)?[{ledgerAccountId:chargesExpense,debit:charges,credit:Z}]:[]),{ledgerAccountId:money,debit:Z,credit:principal.add(interest).add(charges)}]};export function ledgerBalance(lines:Array<{debit:Prisma.Decimal;credit:Prisma.Decimal}>){return lines.reduce((x,l)=>x.add(l.debit).sub(l.credit),Z)}
