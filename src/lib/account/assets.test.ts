import{describe,expect,it}from"vitest";import{Prisma}from"@prisma/client";import{depreciationPreview}from"./assets";describe("A13 depreciation foundation",()=>{it("calculates straight-line monthly preview with Decimal",()=>expect(depreciationPreview(new Prisma.Decimal(1200),new Prisma.Decimal(0),12).toString()).toBe("100"));it("rejects invalid salvage value",()=>expect(()=>depreciationPreview(new Prisma.Decimal(100),new Prisma.Decimal(101),12)).toThrow("INVALID_DEPRECIATION"))});
