import{describe,it,expect}from"vitest";import{Prisma}from"@prisma/client";import{ledgerBalance,loanPaymentLines,transferLines}from"./money";const d=(x:number)=>new Prisma.Decimal(x);describe("A10 money",()=>{it("posts an internal transfer without income or expense",()=>{const l=transferLines("cash","bank",d(100));expect(l).toHaveLength(2);expect(ledgerBalance(l).isZero()).toBe(true);expect(()=>transferLines("cash","cash",d(1))).toThrow("SAME_MONEY_ACCOUNT")});it("splits principal and interest",()=>{const l=loanPaymentLines("loan","interest","charges","bank",d(80),d(20));expect(l[0].debit.toString()).toBe("80");expect(l[1].debit.toString()).toBe("20");expect(ledgerBalance(l).isZero()).toBe(true)})});
