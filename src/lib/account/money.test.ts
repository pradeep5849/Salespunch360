import{describe,it,expect}from"vitest";import{Prisma}from"@prisma/client";import{ledgerBalance,loanPaymentLines,transferLines}from"./money";const d=(x:number|string)=>new Prisma.Decimal(x);describe("A10 money",()=>{it("posts an internal transfer without income or expense",()=>{const l=transferLines("cash","bank",d(100));expect(l).toHaveLength(2);expect(ledgerBalance(l).isZero()).toBe(true);expect(()=>transferLines("cash","cash",d(1))).toThrow("SAME_MONEY_ACCOUNT");expect(()=>transferLines("cash","bank",d(0))).toThrow("INVALID_AMOUNT")});it("splits principal, interest and charges without expensing principal",()=>{const l=loanPaymentLines("loan","interest","charges","bank",d(80),d(20),d(5));expect(l[0]).toMatchObject({ledgerAccountId:"loan",debit:d(80)});expect(l[1]).toMatchObject({ledgerAccountId:"interest",debit:d(20)});expect(l[2]).toMatchObject({ledgerAccountId:"charges",debit:d(5)});expect(ledgerBalance(l).isZero()).toBe(true)});it("derives debit-normal money balances from journal lines",()=>expect(ledgerBalance([{debit:d("100.01"),credit:d("20.00")}]).toString()).toBe("80.01"))});
