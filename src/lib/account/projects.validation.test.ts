import{describe,expect,it}from"vitest";import{Prisma}from"@prisma/client";import{budgetInput,projectInput,taskInput}from"./projects";
const id="11111111-1111-4111-8111-111111111111";describe("A6 project input contracts",()=>{it("accepts Company-level customer and Decimal project value without browser tenant fields",()=>{const p=projectInput.parse({branchId:id,customerId:id,name:"Site fitout",projectValue:"123456.78"});expect(new Prisma.Decimal(p.projectValue).toString()).toBe("123456.78");expect(()=>projectInput.parse({...p,companyId:id})).toThrow()});it("rejects floating point and malformed budget money",()=>{expect(()=>budgetInput.parse({projectId:id,lines:[{category:"Labour",title:"Crew",amount:12.34}]})).toThrow();expect(()=>budgetInput.parse({projectId:id,lines:[{category:"Labour",title:"Crew",amount:"1.234"}]})).toThrow()});it("keeps project tasks distinct and validates identifiers",()=>{expect(taskInput.parse({projectId:id,title:"Handover",status:"TODO"}).title).toBe("Handover");expect(()=>taskInput.parse({projectId:"other",title:"Task"})).toThrow()})});
