import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const commercial=readFileSync("src/lib/account/commercial.ts","utf8"),projects=readFileSync("src/lib/account/projects.ts","utf8"),editor=readFileSync("src/app/workspace/account/transactions/new/commercial-editor.tsx","utf8");describe("A6 closure integration contract",()=>{it("uses authoritative Project access instead of duplicating record scope in commercial",()=>{expect(commercial).toContain("authorizeProjectForCommercial(actor");expect(commercial).not.toContain("tx.project.findFirst");expect(projects).toContain('canUsePermission(actor, company.productEdition, "ACCOUNT_PROJECTS")')});it("requires projectId for new enabled Project purchases and preserves legacy reference",()=>{expect(commercial).toContain("PROJECT_ID_REQUIRED");expect(commercial).toContain("d.projectReference");expect(editor).toContain("Legacy project reference")});it("inherits adjustment ID and snapshot and rejects client adjustment IDs",()=>{expect(commercial).toContain('["CREDIT_NOTE","DEBIT_NOTE"].includes(d.type)?source?.projectId:d.projectId');expect(commercial).toContain('["CREDIT_NOTE","DEBIT_NOTE"].includes(d.type)?source?.projectReference');expect(commercial).toContain("Adjustment project is inherited from its source")});it("supports authoritative selectors for Sales and Purchases",()=>{for(const type of["SALES_ORDER","PROFORMA_INVOICE","SALES_INVOICE","DELIVERY_CHALLAN"])expect(editor).toContain(type);expect(editor).toContain("projectId:projectAllowed&&projectId");expect(editor).not.toContain("Existing BOQ ID")})});
