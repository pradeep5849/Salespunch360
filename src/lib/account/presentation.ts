import type { Decimal } from "@prisma/client/runtime/library";
export const formatInr = (value: Decimal | string | number) => `₹${new Intl.NumberFormat("en-IN", { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(Number(value.toString()))}`;
export const formatIndiaDate = (value: Date | string) => new Intl.DateTimeFormat("en-IN", { timeZone: "Asia/Kolkata", day: "2-digit", month: "short", year: "numeric" }).format(new Date(value));
export const ACCOUNT_ERROR_MESSAGES: Record<string, string> = { PERIOD_LOCKED: "This date is locked for posting.", INVALID_FINANCIAL_YEAR: "Choose an open Financial Year containing this date.", INSUFFICIENT_STOCK: "There is not enough available stock.", MODULE_DISABLED: "This module is disabled for the Company.", UNAUTHORIZED: "You do not have permission for this action.", SOURCE_FULLY_ADJUSTED: "The source document is already fully adjusted.", NEGATIVE_OUTSTANDING: "This allocation would make the outstanding amount negative." };
