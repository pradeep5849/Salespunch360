import { describe, expect, it } from "vitest"; import { buildAccountNavigation } from "./navigation";
const base = { id: "u", companyId: "c", role: "COMPANY_ADMIN", isActive: true, managerType: null, salesRole: null, accountAccessActive: true, salesAccessActive: false, branchAccessScope: "ALL_BRANCHES", branchIds: ["b"] } as const;
describe("A17 role and module navigation", () => { it("keeps Settings in Menu and hides disabled Inventory/Projects", () => { const nav = buildAccountNavigation({ ...base, accountRole: "ACCOUNT_ADMIN" }, "SALESPUNCH360_ACCOUNT", ["SALES"]), text = nav.flatMap((g) => g.items); expect(text.some((x) => x.label === "Settings")).toBe(true); expect(text.some((x) => x.href.includes("/inventory"))).toBe(false); expect(text.some((x) => x.href.includes("/projects"))).toBe(false); }); it("does not leak Company settings or financial KPIs to Data Entry", () => { const nav = buildAccountNavigation({ ...base, accountRole: "DATA_ENTRY" }, "SALESPUNCH360_ACCOUNT", ["SALES", "PURCHASES"]), items = nav.flatMap((g) => g.items); expect(items.some((x) => x.label === "Settings")).toBe(false); expect(items.some((x) => x.label === "Profit & Loss")).toBe(false); }); it("shows Project navigation only when enabled and authorized", () => { const nav = buildAccountNavigation({ ...base, accountRole: "PROJECT_MANAGER" }, "SALESPUNCH360_ACCOUNT", ["PROJECTS", "PROJECT_COSTING"]); expect(nav.flatMap((g) => g.items).some((x) => x.label === "Projects")).toBe(true); }); });
