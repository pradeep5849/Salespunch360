import{describe,expect,it}from"vitest";import{commercialDocumentInput}from"./commercial";const id="11111111-1111-4111-8111-111111111111",line={lineType:"CUSTOM",itemName:"Item",quantity:"1",rate:"1"};const input=(type:string,extra:Record<string,unknown>={})=>({type,branchId:id,partyId:id,issueDate:"2026-09-11",lines:[line],...(type.startsWith("PURCHASE")||type==="SUBCONTRACT_PURCHASE"?{purchasePurpose:"PROJECT",purchaseClassification:"PURCHASE_COST"}:{}),...extra});describe("commercial authoritative Project input",()=>{it.each(["SALES_ORDER","PROFORMA_INVOICE","SALES_INVOICE","DELIVERY_CHALLAN","PURCHASE_ORDER","PURCHASE_BILL","SUBCONTRACT_PURCHASE"])("accepts projectId for %s",type=>expect(commercialDocumentInput.parse(input(type,{projectId:id})).projectId).toBe(id));it.each(["CREDIT_NOTE","DEBIT_NOTE"])("rejects independent %s projectId",type=>expect(()=>commercialDocumentInput.parse(input(type,{sourceDocumentId:id,projectId:id,...(type==="DEBIT_NOTE"?{purchasePurpose:"PROJECT",purchaseClassification:"PURCHASE_COST"}:{})}))).toThrow("Adjustment project is inherited"));it("keeps legacy Project reference input parseable for module-aware server compatibility",()=>expect(commercialDocumentInput.parse(input("PURCHASE_BILL",{projectReference:"Legacy PRJ-9"})).projectReference).toBe("Legacy PRJ-9"))});
