import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const pkg=JSON.parse(readFileSync("package.json","utf8")),repair=readFileSync("scripts/repair-a11-a14-runtime.mjs","utf8"),safe=readFileSync("scripts/migrate-deploy-safe.mjs","utf8");describe("Y-06 production prebuild contract",()=>{it("preserves the reviewed production order",()=>expect(pkg.scripts.prebuild).toBe("prisma generate && node scripts/repair-a11-a14-runtime.mjs && node scripts/migrate-deploy-safe.mjs"));it("fails closed and only resolves the fixed failed repair target",()=>{expect(repair).toContain("failedMigrationExists");expect(repair).toContain("process.exitCode = 1");expect(repair).toContain('"--applied", MIGRATION');expect(repair).not.toMatch(/DELETE FROM|DROP TABLE/i)});it("bounds retries and surfaces failure",()=>{expect(safe).toContain("attempt <= 4");expect(safe).toContain("process.exitCode = 1");expect(safe).not.toMatch(/DELETE FROM|DROP TABLE/i)})});
