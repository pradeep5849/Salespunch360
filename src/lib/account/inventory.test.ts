import {describe,expect,it} from "vitest";import {Prisma} from "@prisma/client";import {chooseProductRate,itemProfitability,signedQuantity,stockValuation} from "./inventory";const d=(x:string|number)=>new Prisma.Decimal(x);
describe("A11 inventory calculations",()=>{it("derives stock and weighted-average value from immutable movements",()=>{const x=stockValuation([{movementType:"OPENING",quantity:d(10),unitCost:d(5)},{movementType:"PURCHASE",quantity:d(10),unitCost:d(7)},{movementType:"SALE",quantity:d(5),unitCost:d(0)}]);expect(x.quantity.toString()).toBe("15");expect(x.averageUnitCost.toString()).toBe("6");expect(x.stockValue.toString()).toBe("90")});it("signs every required movement",()=>{expect(signedQuantity("SALES_RETURN",d(1)).toString()).toBe("1");expect(signedQuantity("PURCHASE_RETURN",d(1)).toString()).toBe("-1")});it("prioritizes customer, tier, then default pricing",()=>{expect(chooseProductRate({customerRate:d(8),tierRate:d(9),defaultRate:d(10)}).toString()).toBe("8")});it("calculates tax-exclusive profitability",()=>expect(itemProfitability(d(100),d(60)).profit.toString()).toBe("40"));});
