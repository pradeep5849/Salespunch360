import {CommercialAuditEventType,CommercialDocumentType,CommercialLineType,DiscountType,PaymentMode,PaymentReminderStatus,Prisma,PurchaseClassification,PurchasePurpose,SettlementType} from "@prisma/client";
import {z} from "zod";
import {db} from "@/lib/db";
import {AuthorizationError,requirePermission,requirePermissionForMutation} from "@/lib/auth/authorization";
import type {Permission} from "@/lib/auth/permissions";
import {postJournalInTx} from "@/lib/accounting/service";
import {allocateDocumentNumberInTx} from "./numbering";
import {enabledModulesForCompany,requireAccountModules,type ModuleKey} from "./modules";

const D=Prisma.Decimal;
type Actor={id:string;companyId:string;accountRole?:string|null;branchAccessScope?:string;branchIds?:string[]};
const policies:Record<CommercialDocumentType,{modules:ModuleKey[];permission:Permission;party:"customer"|"vendor";prefix:string;financial:boolean}>={
 SALES_ORDER:{modules:["SALES","SALES_ORDER"],permission:"ACCOUNT_SALES_ENTRY",party:"customer",prefix:"SO-",financial:false},PROFORMA_INVOICE:{modules:["SALES","PROFORMA_INVOICE"],permission:"ACCOUNT_SALES_ENTRY",party:"customer",prefix:"PI-",financial:false},SALES_INVOICE:{modules:["SALES"],permission:"ACCOUNT_SALES_ENTRY",party:"customer",prefix:"INV-",financial:true},DELIVERY_CHALLAN:{modules:["SALES","DELIVERY_CHALLAN"],permission:"ACCOUNT_SALES_ENTRY",party:"customer",prefix:"DC-",financial:false},CREDIT_NOTE:{modules:["SALES","CREDIT_NOTE"],permission:"ACCOUNT_SALES_ENTRY",party:"customer",prefix:"CN-",financial:true},
 PURCHASE_ORDER:{modules:["PURCHASES","PURCHASE_ORDER"],permission:"ACCOUNT_PURCHASE_ENTRY",party:"vendor",prefix:"PO-",financial:false},PURCHASE_BILL:{modules:["PURCHASES","PURCHASE_BILLS"],permission:"ACCOUNT_PURCHASE_ENTRY",party:"vendor",prefix:"PB-",financial:true},DEBIT_NOTE:{modules:["PURCHASES","DEBIT_NOTE"],permission:"ACCOUNT_PURCHASE_ENTRY",party:"vendor",prefix:"DN-",financial:true},SUBCONTRACT_PURCHASE:{modules:["PURCHASES","SUBCONTRACTORS"],permission:"ACCOUNT_PURCHASE_ENTRY",party:"vendor",prefix:"SC-",financial:false}};

export function resolveCommercialTaxRate(explicit:string|undefined,master:Prisma.Decimal|null){return new Prisma.Decimal(explicit??master??0)}
export function selectMasterRate(purchase:boolean,sellingRate:Prisma.Decimal|null,costRate:Prisma.Decimal|null){return purchase?costRate:sellingRate}
export function purchaseClassificationForPosting(type:CommercialDocumentType,documentClassification:PurchaseClassification|null,sourceClassification:PurchaseClassification|null){return type==="DEBIT_NOTE"?sourceClassification:documentClassification}
export function assertAdjustmentWithinSource(current:{taxableTotal:Prisma.Decimal;taxTotal:Prisma.Decimal;grandTotal:Prisma.Decimal},source:{taxableTotal:Prisma.Decimal;taxTotal:Prisma.Decimal;grandTotal:Prisma.Decimal},used:{taxableTotal:Prisma.Decimal;taxTotal:Prisma.Decimal;grandTotal:Prisma.Decimal},outstanding:Prisma.Decimal){const limits=[[current.taxableTotal,source.taxableTotal,used.taxableTotal,"TAXABLE"],[current.taxTotal,source.taxTotal,used.taxTotal,"TAX"],[current.grandTotal,source.grandTotal,used.grandTotal,"GRAND_TOTAL"]] as const;for(const [amount,maximum,prior,label] of limits)if(prior.add(amount).gt(maximum))throw new Error(`ADJUSTMENT_${label}_EXCEEDS_SOURCE`);if(current.grandTotal.gt(outstanding))throw new Error("ADJUSTMENT_EXCEEDS_OUTSTANDING")}
const decimal=z.union([z.string(),z.number()]).transform(String);
const lineSchema=z.object({lineType:z.nativeEnum(CommercialLineType),sourceId:z.string().uuid().optional(),itemName:z.string().trim().min(1).max(240).optional(),itemCode:z.string().max(80).optional(),description:z.string().max(10_000).optional(),specification:z.string().max(20_000).optional(),unitName:z.string().max(80).optional(),unitSymbol:z.string().max(20).optional(),quantity:decimal,rate:decimal.optional(),discountType:z.nativeEnum(DiscountType).nullable().optional(),discountValue:decimal.nullable().optional(),taxRate:decimal.optional()});
export const commercialDocumentInput=z.object({type:z.nativeEnum(CommercialDocumentType),branchId:z.string().uuid(),partyId:z.string().uuid(),sourceDocumentId:z.string().uuid().optional(),purchasePurpose:z.nativeEnum(PurchasePurpose).optional(),purchaseClassification:z.nativeEnum(PurchaseClassification).optional(),projectReference:z.string().trim().max(240).optional(),issueDate:z.coerce.date(),dueDate:z.coerce.date().optional(),notes:z.string().max(5000).optional(),lines:z.array(lineSchema).min(1).max(500)}).strict().superRefine((v,c)=>{const purchase=policies[v.type].party==="vendor";if(purchase&&!v.purchasePurpose)c.addIssue({code:"custom",message:"Purchase purpose is required",path:["purchasePurpose"]});if(v.purchasePurpose==="PROJECT"&&!v.projectReference)c.addIssue({code:"custom",message:"Project reference is required",path:["projectReference"]});if(!purchase&&v.purchasePurpose)c.addIssue({code:"custom",message:"Purchase fields are not valid for Sales",path:["purchasePurpose"]})});
function scope(a:Actor){return a.branchAccessScope==="SELECTED_BRANCHES"?{branchId:{in:a.branchIds??[]}}:{}}
function assertBranch(a:Actor,branchId:string){if(a.branchAccessScope==="SELECTED_BRANCHES"&&!a.branchIds?.includes(branchId))throw new Error("INVALID_BRANCH")}
function financialRole(a:Actor){if(a.accountRole!=="ACCOUNT_ADMIN"&&a.accountRole!=="ACCOUNTANT")throw new AuthorizationError()}
async function systemContext(tx:Prisma.TransactionClient,a:Actor,date:Date,keys:string[]){const [fy,accounts,lock]=await Promise.all([tx.financialYear.findFirst({where:{companyId:a.companyId,isActive:true,startDate:{lte:date},endDate:{gte:date}},orderBy:{startDate:"desc"}}),tx.ledgerAccount.findMany({where:{companyId:a.companyId,isActive:true,allowPosting:true,systemKey:{in:keys}}}),tx.accountingPeriodLock.findFirst({where:{companyId:a.companyId,financialYear:{startDate:{lte:date},endDate:{gte:date}}}})]);if(!fy)throw new Error("INVALID_FINANCIAL_YEAR");if(lock?.lockedThrough&&date<=lock.lockedThrough)throw new Error("PERIOD_LOCKED");const byKey=new Map(accounts.map(x=>[x.systemKey!,x.id]));for(const key of keys)if(!byKey.has(key))throw new Error(`SYSTEM_LEDGER_MISSING:${key}`);return{fy,account:(key:string)=>byKey.get(key)!}}
async function audit(tx:Prisma.TransactionClient,a:Actor,eventType:CommercialAuditEventType,entityType:string,entityId:string,metadata?:Prisma.InputJsonValue){await tx.commercialAuditEvent.create({data:{companyId:a.companyId,actorId:a.id,eventType,entityType,entityId,metadata}})}
async function snapshotLine(tx:Prisma.TransactionClient,a:Actor,type:CommercialDocumentType,row:z.infer<typeof lineSchema>,position:number){const purchase=policies[type].party==="vendor",allowed=purchase?["MATERIAL","SERVICE","SUBCONTRACT","CUSTOM"]:["PRODUCT","SERVICE","WORK_PACKAGE","CUSTOM"];if(!allowed.includes(row.lineType))throw new Error("INVALID_LINE_TYPE");let master:{id:string;name:string;code:string|null;description:string|null;rate:Prisma.Decimal|null;tax:Prisma.Decimal|null;unit:{name:string;symbol:string}|null}|null=null;if(row.lineType==="PRODUCT"||row.lineType==="MATERIAL"){if(!row.sourceId)throw new Error("LINE_SOURCE_REQUIRED");const x=await tx.accountProduct.findFirst({where:{companyId:a.companyId,id:row.sourceId,isActive:true},include:{unit:true}});if(x)master={id:x.id,name:x.name,code:x.code,description:x.description,rate:selectMasterRate(purchase,x.salePrice,x.costPrice),tax:x.taxRate,unit:x.unit}}if(row.lineType==="SERVICE"){if(!row.sourceId)throw new Error("LINE_SOURCE_REQUIRED");const x=await tx.accountService.findFirst({where:{companyId:a.companyId,id:row.sourceId,isActive:true},include:{unit:true}});if(x)master={id:x.id,name:x.name,code:x.code,description:x.description,rate:selectMasterRate(purchase,x.sellingRate,x.estimatedCost),tax:x.taxRate,unit:x.unit}}if(row.lineType==="WORK_PACKAGE"||row.lineType==="SUBCONTRACT"){if(!row.sourceId)throw new Error("LINE_SOURCE_REQUIRED");const x=await tx.workPackage.findFirst({where:{companyId:a.companyId,id:row.sourceId,isActive:true},include:{unit:true}});if(x)master={id:x.id,name:x.name,code:x.code,description:x.description,rate:selectMasterRate(purchase,x.sellingRate,x.estimatedCost),tax:null,unit:x.unit}}if(row.lineType!=="CUSTOM"&&!master)throw new Error("INVALID_LINE_SOURCE");const quantity=new D(row.quantity),rate=new D(row.rate??master?.rate??0),taxRate=resolveCommercialTaxRate(row.taxRate,master?.tax??null),discountValue=new D(row.discountValue??0);if(quantity.lte(0)||rate.lt(0)||taxRate.lt(0)||discountValue.lt(0))throw new Error("INVALID_AMOUNT");const baseAmount=quantity.mul(rate).toDecimalPlaces(2),discountAmount=row.discountType==="PERCENTAGE"?baseAmount.mul(discountValue).div(100).toDecimalPlaces(2):discountValue.toDecimalPlaces(2);if(discountAmount.gt(baseAmount)||row.discountType==="PERCENTAGE"&&discountValue.gt(100))throw new Error("INVALID_DISCOUNT");const taxableAmount=baseAmount.sub(discountAmount),taxAmount=taxableAmount.mul(taxRate).div(100).toDecimalPlaces(2);return{companyId:a.companyId,position,lineType:row.lineType,productId:["PRODUCT","MATERIAL"].includes(row.lineType)?master?.id:null,serviceId:row.lineType==="SERVICE"?master?.id:null,workPackageId:["WORK_PACKAGE","SUBCONTRACT"].includes(row.lineType)?master?.id:null,itemName:row.itemName??master?.name??"Custom",itemCode:row.itemCode??master?.code,description:row.description??master?.description,specification:row.specification,unitName:row.unitName??master?.unit?.name,unitSymbol:row.unitSymbol??master?.unit?.symbol,quantity,rate,discountType:row.discountType,discountValue:row.discountValue,taxRate,baseAmount,discountAmount,taxableAmount,taxAmount,lineTotal:taxableAmount.add(taxAmount)}}
export async function createCommercialDocument(raw:unknown){const d=commercialDocumentInput.parse(raw),p=policies[d.type],actor=await requirePermissionForMutation(p.permission) as Actor;await requireAccountModules(actor,...p.modules);assertBranch(actor,d.branchId);return db.$transaction(async tx=>{if(!await tx.branch.findFirst({where:{companyId:actor.companyId,id:d.branchId,isActive:true}}))throw new Error("INVALID_BRANCH");const party=p.party==="customer"?await tx.customer.findFirst({where:{companyId:actor.companyId,id:d.partyId,isActive:true,isAccountCustomer:true},select:{id:true,name:true}}):await tx.vendor.findFirst({where:{companyId:actor.companyId,id:d.partyId,isActive:true},select:{id:true,name:true}});if(!party)throw new Error(`INVALID_${p.party.toUpperCase()}`);let source:null|{id:string;branchId:string;partyId:string;status:string;type:CommercialDocumentType;grandTotal:Prisma.Decimal;taxableTotal:Prisma.Decimal;taxTotal:Prisma.Decimal;purchasePurpose:PurchasePurpose|null;purchaseClassification:PurchaseClassification|null;projectReference:string|null}=null;if(d.sourceDocumentId){await tx.$queryRaw`SELECT "id" FROM "commercial_documents" WHERE "id"=${d.sourceDocumentId}::uuid AND "companyId"=${actor.companyId}::uuid FOR UPDATE`;source=await tx.commercialDocument.findFirst({where:{id:d.sourceDocumentId,companyId:actor.companyId}});if(!source||source.status!=="POSTED"||source.branchId!==d.branchId||source.partyId!==d.partyId||d.type==="CREDIT_NOTE"&&source.type!=="SALES_INVOICE"||d.type==="DEBIT_NOTE"&&source.type!=="PURCHASE_BILL")throw new Error("INVALID_SOURCE_DOCUMENT");const used=await tx.commercialDocument.aggregate({where:{companyId:actor.companyId,sourceDocumentId:source.id,status:"POSTED"},_sum:{grandTotal:true}});if(new D(used._sum.grandTotal??0).gte(source.grandTotal))throw new Error("SOURCE_FULLY_ADJUSTED")}if(["CREDIT_NOTE","DEBIT_NOTE"].includes(d.type)&&!source)throw new Error("SOURCE_DOCUMENT_REQUIRED");const lines=await Promise.all(d.lines.map((x,i)=>snapshotLine(tx,actor,d.type,x,i))),subtotal=lines.reduce((n,x)=>n.add(x.baseAmount),new D(0)),discountTotal=lines.reduce((n,x)=>n.add(x.discountAmount),new D(0)),taxableTotal=lines.reduce((n,x)=>n.add(x.taxableAmount),new D(0)),taxTotal=lines.reduce((n,x)=>n.add(x.taxAmount),new D(0)),grandTotal=taxableTotal.add(taxTotal);if(source){const used=await tx.commercialDocument.aggregate({where:{companyId:actor.companyId,sourceDocumentId:source.id,status:"POSTED"},_sum:{grandTotal:true}});if(grandTotal.add(used._sum.grandTotal??0).gt(source.grandTotal))throw new Error("ADJUSTMENT_EXCEEDS_SOURCE")}const documentNumber=await allocateDocumentNumberInTx(tx,{companyId:actor.companyId,branchId:d.branchId,seriesKey:d.type,defaults:{prefix:p.prefix,padding:6}}),document=await tx.commercialDocument.create({data:{companyId:actor.companyId,branchId:d.branchId,type:d.type,documentNumber,customerId:p.party==="customer"?party.id:null,vendorId:p.party==="vendor"?party.id:null,partyId:party.id,partyName:party.name,sourceDocumentId:d.sourceDocumentId,purchasePurpose:d.type==="DEBIT_NOTE"?source?.purchasePurpose:d.purchasePurpose,purchaseClassification:d.type==="DEBIT_NOTE"?source?.purchaseClassification:d.purchaseClassification,projectReference:d.type==="DEBIT_NOTE"?source?.projectReference:d.projectReference,issueDate:d.issueDate,dueDate:d.dueDate,notes:d.notes,subtotal,discountTotal,taxableTotal,taxTotal,grandTotal,balanceDue:grandTotal,createdById:actor.id,lines:{create:lines}}});await audit(tx,actor,"DOCUMENT_CREATED","COMMERCIAL_DOCUMENT",document.id,{type:d.type});return document},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
export async function postCommercialDocument(raw:unknown){const {documentId}=z.object({documentId:z.string().uuid()}).strict().parse(raw),actor=await requirePermissionForMutation("ACCOUNT_JOURNAL_POST") as Actor;financialRole(actor);return db.$transaction(async tx=>{await tx.$queryRaw`SELECT "id" FROM "commercial_documents" WHERE "id"=${documentId}::uuid AND "companyId"=${actor.companyId}::uuid FOR UPDATE`;const doc=await tx.commercialDocument.findFirst({where:{id:documentId,companyId:actor.companyId},include:{lines:true}});if(!doc)throw new Error("NOT_FOUND");assertBranch(actor,doc.branchId);const p=policies[doc.type];await requireAccountModules(actor,...p.modules);if(doc.status==="POSTED"&&doc.journalEntryId)return tx.journalEntry.findFirstOrThrow({where:{id:doc.journalEntryId,companyId:actor.companyId}});if(doc.status!=="DRAFT"||!p.financial)throw new Error("DOCUMENT_NOT_POSTABLE");let debitSourceClassification:PurchaseClassification|null=null;if(doc.type==="CREDIT_NOTE"||doc.type==="DEBIT_NOTE"){if(!doc.sourceDocumentId)throw new Error("SOURCE_DOCUMENT_REQUIRED");await tx.$queryRaw`SELECT "id" FROM "commercial_documents" WHERE "id"=${doc.sourceDocumentId}::uuid AND "companyId"=${actor.companyId}::uuid FOR UPDATE`;const expected=doc.type==="CREDIT_NOTE"?"SALES_INVOICE":"PURCHASE_BILL",source=await tx.commercialDocument.findFirst({where:{id:doc.sourceDocumentId,companyId:actor.companyId,type:expected,status:"POSTED"}});if(!source||source.branchId!==doc.branchId||source.customerId!==doc.customerId||source.vendorId!==doc.vendorId)throw new Error("INVALID_SOURCE_DOCUMENT");const used=await tx.commercialDocument.aggregate({where:{companyId:actor.companyId,sourceDocumentId:source.id,status:"POSTED",type:doc.type,id:{not:doc.id}},_sum:{taxableTotal:true,taxTotal:true,grandTotal:true}}),outstanding=await documentOutstandingInTx(tx,actor.companyId,source.id);assertAdjustmentWithinSource(doc,source,{taxableTotal:new D(used._sum.taxableTotal??0),taxTotal:new D(used._sum.taxTotal??0),grandTotal:new D(used._sum.grandTotal??0)},outstanding.outstanding);if(doc.type==="DEBIT_NOTE")debitSourceClassification=source.purchaseClassification}const party=p.party==="customer"?await tx.customer.findFirst({where:{id:doc.customerId!,companyId:actor.companyId,isActive:true}}):await tx.vendor.findFirst({where:{id:doc.vendorId!,companyId:actor.companyId,isActive:true}});if(!party)throw new Error("INVALID_PARTY");const keys=doc.type==="SALES_INVOICE"||doc.type==="CREDIT_NOTE"?["ACCOUNTS_RECEIVABLE","SALES_INCOME","TAXES_PAYABLE"]:["ACCOUNTS_PAYABLE",purchaseClassificationForPosting(doc.type,doc.purchaseClassification,debitSourceClassification)==="GENERAL_EXPENSES"?"GENERAL_EXPENSES":"PURCHASE_COST","INPUT_TAX_CREDIT"],ctx=await systemContext(tx,actor,doc.issueDate,keys),creditSide=doc.type==="SALES_INVOICE"||doc.type==="DEBIT_NOTE",counterDebit=doc.type==="PURCHASE_BILL"||doc.type==="CREDIT_NOTE",money=(x:Prisma.Decimal)=>x.toFixed(2),main=keys[0],counter=keys[1],tax=keys[2],lines=[{ledgerAccountId:ctx.account(main),debit:creditSide?money(doc.grandTotal):"0",credit:creditSide?"0":money(doc.grandTotal),description:doc.documentNumber},{ledgerAccountId:ctx.account(counter),debit:counterDebit?money(doc.taxableTotal):"0",credit:counterDebit?"0":money(doc.taxableTotal),description:doc.documentNumber}];if(doc.taxTotal.gt(0))lines.push({ledgerAccountId:ctx.account(tax),debit:counterDebit?money(doc.taxTotal):"0",credit:counterDebit?"0":money(doc.taxTotal),description:doc.documentNumber});const journal=await postJournalInTx(tx,actor,{financialYearId:ctx.fy.id,branchId:doc.branchId,entryDate:doc.issueDate,sourceType:doc.type,sourceId:doc.id,postingPurpose:"PRIMARY",lines}),changed=await tx.commercialDocument.updateMany({where:{id:doc.id,companyId:actor.companyId,status:"DRAFT",journalEntryId:null},data:{status:"POSTED",journalEntryId:journal.id}});if(changed.count!==1)throw new Error("STALE");await audit(tx,actor,({SALES_INVOICE:"INVOICE_POSTED",CREDIT_NOTE:"CREDIT_NOTE_POSTED",PURCHASE_BILL:"PURCHASE_BILL_POSTED",DEBIT_NOTE:"DEBIT_NOTE_POSTED"} as const)[doc.type as "SALES_INVOICE"|"CREDIT_NOTE"|"PURCHASE_BILL"|"DEBIT_NOTE"],"COMMERCIAL_DOCUMENT",doc.id,{journalEntryId:journal.id});return journal},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
export async function documentOutstandingInTx(tx:Prisma.TransactionClient,companyId:string,documentId:string){const doc=await tx.commercialDocument.findFirst({where:{id:documentId,companyId,status:"POSTED"},select:{grandTotal:true,type:true}});if(!doc||!["SALES_INVOICE","PURCHASE_BILL"].includes(doc.type))throw new Error("INVALID_FINANCIAL_DOCUMENT");const [adjustments,allocations,applications]=await Promise.all([tx.commercialDocument.aggregate({where:{companyId,sourceDocumentId:documentId,status:"POSTED",type:doc.type==="SALES_INVOICE"?"CREDIT_NOTE":"DEBIT_NOTE"},_sum:{grandTotal:true}}),tx.settlementAllocation.aggregate({where:{companyId,documentId},_sum:{amount:true}}),tx.advanceApplication.aggregate({where:{companyId,documentId},_sum:{amount:true}})]),outstanding=new D(doc.grandTotal).sub(adjustments._sum.grandTotal??0).sub(allocations._sum.amount??0).sub(applications._sum.amount??0);if(outstanding.lt(0))throw new Error("NEGATIVE_OUTSTANDING");return{outstanding,paymentStatus:outstanding.isZero()?"PAID" as const:outstanding.eq(doc.grandTotal)?"UNPAID" as const:"PARTIALLY_PAID" as const}}
export async function documentOutstanding(documentId:string){const a=await requirePermission("ACCOUNT_LEDGER_VIEW") as Actor;return db.$transaction(tx=>documentOutstandingInTx(tx,a.companyId,documentId))}
export async function partyOutstanding(partyId:string,party:"customer"|"vendor"){const a=await requirePermission("ACCOUNT_LEDGER_VIEW") as Actor,docs=await db.commercialDocument.findMany({where:{companyId:a.companyId,partyId,type:party==="customer"?"SALES_INVOICE":"PURCHASE_BILL",status:"POSTED",...scope(a)},select:{id:true}}),rows=await db.$transaction(tx=>Promise.all(docs.map(x=>documentOutstandingInTx(tx,a.companyId,x.id))));return rows.reduce((n,x)=>n.add(x.outstanding),new D(0))}
const settlementInput=z.object({idempotencyKey:z.string().trim().min(8).max(120),type:z.nativeEnum(SettlementType),branchId:z.string().uuid(),partyId:z.string().uuid(),paymentMode:z.nativeEnum(PaymentMode),amount:decimal,transactionDate:z.coerce.date(),reference:z.string().max(160).optional(),notes:z.string().max(5000).optional(),allocations:z.array(z.object({documentId:z.string().uuid(),amount:decimal})).default([])}).strict();
const settlementPolicy:Record<SettlementType,{modules:ModuleKey[];party:"customer"|"vendor";prefix:string;advance:boolean}>={CUSTOMER_RECEIPT:{modules:["SALES","CUSTOMER_RECEIPTS"],party:"customer",prefix:"CR-",advance:false},CUSTOMER_ADVANCE:{modules:["SALES","CUSTOMER_ADVANCES"],party:"customer",prefix:"CA-",advance:true},VENDOR_PAYMENT:{modules:["PURCHASES","VENDOR_PAYMENTS"],party:"vendor",prefix:"VP-",advance:false},VENDOR_ADVANCE:{modules:["PURCHASES","VENDOR_ADVANCES"],party:"vendor",prefix:"VA-",advance:true}};
export async function createSettlement(raw:unknown){const actor=await requirePermissionForMutation("ACCOUNT_SETTLEMENT_ENTRY") as Actor;financialRole(actor);const d=settlementInput.parse(raw);const p=settlementPolicy[d.type];await requireAccountModules(actor,...p.modules);assertBranch(actor,d.branchId);const amount=new D(d.amount);if(amount.lte(0))throw new Error("INVALID_AMOUNT");if(p.advance&&d.allocations.length||!p.advance&&!d.allocations.length)throw new Error(p.advance?"ADVANCE_CANNOT_ALLOCATE":"RECEIPT_MUST_BE_ALLOCATED");if(!p.advance&&!d.allocations.reduce((n,x)=>n.add(x.amount),new D(0)).eq(amount))throw new Error("ALLOCATION_TOTAL_MISMATCH");if(new Set(d.allocations.map(x=>x.documentId)).size!==d.allocations.length)throw new Error("DUPLICATE_ALLOCATION");return db.$transaction(async tx=>{const existing=await tx.accountSettlement.findUnique({where:{companyId_idempotencyKey:{companyId:actor.companyId,idempotencyKey:d.idempotencyKey}}});if(existing)return existing;const party=p.party==="customer"?await tx.customer.findFirst({where:{id:d.partyId,companyId:actor.companyId,isActive:true,isAccountCustomer:true}}):await tx.vendor.findFirst({where:{id:d.partyId,companyId:actor.companyId,isActive:true}});if(!party)throw new Error("INVALID_PARTY");const expected=p.party==="customer"?"SALES_INVOICE":"PURCHASE_BILL";for(const allocation of d.allocations){await tx.$queryRaw`SELECT "id" FROM "commercial_documents" WHERE "id"=${allocation.documentId}::uuid AND "companyId"=${actor.companyId}::uuid FOR UPDATE`;const doc=await tx.commercialDocument.findFirst({where:{id:allocation.documentId,companyId:actor.companyId,type:expected,status:"POSTED",partyId:d.partyId,branchId:d.branchId}});if(!doc)throw new Error("INVALID_ALLOCATION_DOCUMENT");const out=await documentOutstandingInTx(tx,actor.companyId,doc.id);if(new D(allocation.amount).lte(0)||new D(allocation.amount).gt(out.outstanding))throw new Error("ALLOCATION_EXCEEDS_OUTSTANDING")}const moneyKey=d.paymentMode==="CASH"?"CASH":"BANK",balanceKey=p.advance?(p.party==="customer"?"CUSTOMER_ADVANCES":"ADVANCES"):(p.party==="customer"?"ACCOUNTS_RECEIVABLE":"ACCOUNTS_PAYABLE"),ctx=await systemContext(tx,actor,d.transactionDate,[moneyKey,balanceKey]),settlementNumber=await allocateDocumentNumberInTx(tx,{companyId:actor.companyId,branchId:d.branchId,seriesKey:d.type,defaults:{prefix:p.prefix,padding:6}}),money=amount.toFixed(2),cashDebit=p.party==="customer",lines=p.advance||p.party==="customer"?[{ledgerAccountId:ctx.account(moneyKey),debit:cashDebit?money:"0",credit:cashDebit?"0":money},{ledgerAccountId:ctx.account(balanceKey),debit:cashDebit?"0":money,credit:cashDebit?money:"0"}]:[{ledgerAccountId:ctx.account(balanceKey),debit:money,credit:"0"},{ledgerAccountId:ctx.account(moneyKey),debit:"0",credit:money}],placeholder=crypto.randomUUID();await tx.$executeRaw`SELECT set_config('app.financial_write_company_id', ${actor.companyId}, true)`;const journal=await postJournalInTx(tx,actor,{financialYearId:ctx.fy.id,branchId:d.branchId,entryDate:d.transactionDate,sourceType:d.type,sourceId:placeholder,postingPurpose:"PRIMARY",lines}),settlement=await tx.accountSettlement.create({data:{id:placeholder,companyId:actor.companyId,branchId:d.branchId,type:d.type,settlementNumber,partyId:d.partyId,customerId:p.party==="customer"?d.partyId:null,vendorId:p.party==="vendor"?d.partyId:null,idempotencyKey:d.idempotencyKey,paymentMode:d.paymentMode,amount,remainingAmount:p.advance?amount:0,transactionDate:d.transactionDate,reference:d.reference,notes:d.notes,journalEntryId:journal.id,createdById:actor.id,allocations:{create:d.allocations.map(x=>({companyId:actor.companyId,documentId:x.documentId,amount:x.amount}))}}});await audit(tx,actor,({CUSTOMER_RECEIPT:"CUSTOMER_RECEIPT_POSTED",CUSTOMER_ADVANCE:"CUSTOMER_ADVANCE_POSTED",VENDOR_PAYMENT:"VENDOR_PAYMENT_POSTED",VENDOR_ADVANCE:"VENDOR_ADVANCE_POSTED"} as const)[d.type],"SETTLEMENT",settlement.id,{journalEntryId:journal.id});return settlement},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
export async function applyAdvance(raw:unknown){const d=z.object({advanceId:z.string().uuid(),documentId:z.string().uuid(),amount:decimal,applicationDate:z.coerce.date(),idempotencyKey:z.string().trim().min(8).max(120)}).strict().parse(raw),actor=await requirePermissionForMutation("ACCOUNT_SETTLEMENT_ENTRY") as Actor;financialRole(actor);const amount=new D(d.amount);if(amount.lte(0))throw new Error("INVALID_AMOUNT");return db.$transaction(async tx=>{await tx.$queryRaw`SELECT "id" FROM "account_settlements" WHERE "id"=${d.advanceId}::uuid AND "companyId"=${actor.companyId}::uuid FOR UPDATE`;await tx.$queryRaw`SELECT "id" FROM "commercial_documents" WHERE "id"=${d.documentId}::uuid AND "companyId"=${actor.companyId}::uuid FOR UPDATE`;const advance=await tx.accountSettlement.findFirst({where:{id:d.advanceId,companyId:actor.companyId,type:{in:["CUSTOMER_ADVANCE","VENDOR_ADVANCE"]}}}),doc=await tx.commercialDocument.findFirst({where:{id:d.documentId,companyId:actor.companyId,status:"POSTED"}});if(!advance||!doc||advance.partyId!==doc.partyId||advance.branchId!==doc.branchId||advance.type==="CUSTOMER_ADVANCE"&&doc.type!=="SALES_INVOICE"||advance.type==="VENDOR_ADVANCE"&&doc.type!=="PURCHASE_BILL")throw new Error("INVALID_ADVANCE_APPLICATION");assertBranch(actor,doc.branchId);await requireAccountModules(actor,...settlementPolicy[advance.type].modules);const existing=await tx.advanceApplication.findUnique({where:{companyId_idempotencyKey:{companyId:actor.companyId,idempotencyKey:d.idempotencyKey}}});if(existing)return existing;const outstanding=await documentOutstandingInTx(tx,actor.companyId,doc.id);if(amount.gt(advance.remainingAmount)||amount.gt(outstanding.outstanding))throw new Error("ADVANCE_APPLICATION_EXCEEDS_BALANCE");const customer=advance.type==="CUSTOMER_ADVANCE",keys=customer?["CUSTOMER_ADVANCES","ACCOUNTS_RECEIVABLE"]:["ACCOUNTS_PAYABLE","ADVANCES"],ctx=await systemContext(tx,actor,d.applicationDate,keys),money=amount.toFixed(2),id=crypto.randomUUID(),journal=await postJournalInTx(tx,actor,{financialYearId:ctx.fy.id,branchId:doc.branchId,entryDate:d.applicationDate,sourceType:"ADVANCE_APPLICATION",sourceId:id,postingPurpose:"PRIMARY",lines:[{ledgerAccountId:ctx.account(keys[0]),debit:money,credit:"0"},{ledgerAccountId:ctx.account(keys[1]),debit:"0",credit:money}]}),application=await tx.advanceApplication.create({data:{id,companyId:actor.companyId,advanceId:advance.id,documentId:doc.id,amount,applicationDate:d.applicationDate,idempotencyKey:d.idempotencyKey,journalEntryId:journal.id,createdById:actor.id}});await tx.accountSettlement.update({where:{id:advance.id},data:{remainingAmount:{decrement:amount}}});await audit(tx,actor,customer?"CUSTOMER_ADVANCE_APPLIED":"VENDOR_ADVANCE_APPLIED","ADVANCE_APPLICATION",application.id,{journalEntryId:journal.id});return application},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
export async function schedulePaymentReminder(raw:unknown){const d=z.object({documentId:z.string().uuid(),remindAt:z.coerce.date(),message:z.string().trim().min(1).max(1000)}).strict().parse(raw),actor=await requirePermissionForMutation("ACCOUNT_SALES_ENTRY") as Actor;financialRole(actor);await requireAccountModules(actor,"SALES","PAYMENT_REMINDERS");const doc=await db.commercialDocument.findFirst({where:{id:d.documentId,companyId:actor.companyId,type:"SALES_INVOICE",status:"POSTED",...scope(actor)}});if(!doc||!doc.customerId)throw new Error("INVALID_INVOICE");const reminder=await db.paymentReminder.create({data:{companyId:actor.companyId,documentId:doc.id,customerId:doc.customerId,createdById:actor.id,remindAt:d.remindAt,message:d.message}});await db.commercialAuditEvent.create({data:{companyId:actor.companyId,actorId:actor.id,eventType:"REMINDER_CREATED",entityType:"PAYMENT_REMINDER",entityId:reminder.id}});return reminder}
export async function setPaymentReminderStatus(raw:unknown){const d=z.object({id:z.string().uuid(),status:z.enum(["SENT","DISMISSED"])}).strict().parse(raw),actor=await requirePermissionForMutation("ACCOUNT_SALES_ENTRY") as Actor;financialRole(actor);await requireAccountModules(actor,"SALES","PAYMENT_REMINDERS");return db.$transaction(async tx=>{const row=await tx.paymentReminder.findFirst({where:{id:d.id,companyId:actor.companyId,status:"PENDING",document:scope(actor)}});if(!row)throw new Error("REMINDER_NOT_PENDING");const updated=await tx.paymentReminder.update({where:{id:row.id},data:{status:d.status as PaymentReminderStatus,sentAt:d.status==="SENT"?new Date():null}});await audit(tx,actor,"REMINDER_STATUS_CHANGED","PAYMENT_REMINDER",row.id,{status:d.status});return updated})}
export async function listCommercialDocuments(){const actor=await requirePermission("ACCOUNT_LEDGER_VIEW") as Actor;return db.commercialDocument.findMany({where:{companyId:actor.companyId,...scope(actor)},include:{lines:true},orderBy:{createdAt:"desc"}})}
export async function getCommercialDocument(id:string){const actor=await requirePermission("ACCOUNT_LEDGER_VIEW") as Actor,row=await db.commercialDocument.findFirst({where:{id,companyId:actor.companyId,...scope(actor)},include:{branch:true,lines:{orderBy:{position:"asc"}},reminders:{orderBy:{createdAt:"desc"}},adjustments:{where:{status:"POSTED"}},allocations:true,advanceApplications:true}});if(!row)throw new Error("NOT_FOUND");const financial=["SALES_INVOICE","PURCHASE_BILL"].includes(row.type)?await documentOutstanding(row.id):null;return{...row,financial}}
export async function commercialEditorOptions(){const actor=await requirePermission("ACCOUNT_LEDGER_VIEW") as Actor;const sourceRows=await db.commercialDocument.findMany({where:{companyId:actor.companyId,status:"POSTED",type:{in:["SALES_INVOICE","PURCHASE_BILL"]},...scope(actor)},select:{id:true,type:true,branchId:true,customerId:true,vendorId:true,documentNumber:true,grandTotal:true,adjustments:{where:{status:"POSTED"},select:{grandTotal:true}},allocations:{select:{amount:true}},advanceApplications:{select:{amount:true}}}}),sourceDocuments=sourceRows.map(x=>({...x,remainingAmount:x.advanceApplications.reduce((n,a)=>n.sub(a.amount),x.allocations.reduce((n,a)=>n.sub(a.amount),x.adjustments.reduce((n,a)=>n.sub(a.grandTotal),new D(x.grandTotal))))})).filter(x=>x.remainingAmount.gt(0));return{sourceDocuments,enabledModules:await enabledModulesForCompany(actor.companyId),branches:await db.branch.findMany({where:{companyId:actor.companyId,isActive:true,...(actor.branchAccessScope==="SELECTED_BRANCHES"?{id:{in:actor.branchIds??[]}}:{})},select:{id:true,name:true,code:true}}),customers:await db.customer.findMany({where:{companyId:actor.companyId,isActive:true,isAccountCustomer:true},select:{id:true,name:true}}),vendors:await db.vendor.findMany({where:{companyId:actor.companyId,isActive:true},select:{id:true,name:true}}),products:await db.accountProduct.findMany({where:{companyId:actor.companyId,isActive:true},select:{id:true,name:true,code:true,salePrice:true,taxRate:true}}),services:await db.accountService.findMany({where:{companyId:actor.companyId,isActive:true},select:{id:true,name:true,code:true,sellingRate:true,taxRate:true}}),workPackages:await db.workPackage.findMany({where:{companyId:actor.companyId,isActive:true},select:{id:true,name:true,code:true,sellingRate:true}})}}
export async function commercialDocumentControls(documentId:string){const actor=await requirePermission("ACCOUNT_LEDGER_VIEW") as Actor,document=await db.commercialDocument.findFirst({where:{id:documentId,companyId:actor.companyId,status:"POSTED",...scope(actor)},select:{id:true,type:true,branchId:true,customerId:true,vendorId:true,balanceDue:true}});if(!document)return{canManage:false,canRemind:false,advances:[],enabledModules:[] as ModuleKey[]};const enabledModules=await enabledModulesForCompany(actor.companyId),canManage=actor.accountRole==="ACCOUNT_ADMIN"||actor.accountRole==="ACCOUNTANT",customer=document.type==="SALES_INVOICE",advanceType=customer?"CUSTOMER_ADVANCE":"VENDOR_ADVANCE",advanceModule=customer?"CUSTOMER_ADVANCES":"VENDOR_ADVANCES",advances=canManage&&enabledModules.includes(advanceModule)?await db.accountSettlement.findMany({where:{companyId:actor.companyId,branchId:document.branchId,type:advanceType,remainingAmount:{gt:0},...(customer?{customerId:document.customerId}:{vendorId:document.vendorId})},select:{id:true,settlementNumber:true,remainingAmount:true}}):[];return{canManage,canRemind:canManage&&customer&&enabledModules.includes("SALES")&&enabledModules.includes("PAYMENT_REMINDERS"),advances,enabledModules}}
