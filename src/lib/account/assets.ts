import {AssetStatus,AssetType,DepreciationMethod,Prisma}from"@prisma/client";import{z}from"zod";import{db}from"@/lib/db";import{AuthorizationError,requirePermission,requirePermissionForMutation}from"@/lib/auth/authorization";import{requireAccountModules}from"./modules";import{allocateDocumentNumberInTx}from"./numbering";import type{ProjectActor}from"./projects";
const D=Prisma.Decimal,money=z.string().regex(/^\d{1,16}(\.\d{1,2})?$/),base=z.object({name:z.string().trim().min(1).max(240),assetType:z.nativeEnum(AssetType),category:z.string().max(120).optional(),purchaseDate:z.coerce.date(),purchaseValue:money,vendorId:z.string().uuid().optional(),purchaseDocumentId:z.string().uuid().optional(),purchaseDocumentLineId:z.string().uuid().optional(),description:z.string().max(5000).optional(),serialNumber:z.string().max(160).optional(),registrationNumber:z.string().max(80).optional(),makeModel:z.string().max(160).optional(),manufactureYear:z.coerce.number().int().min(1900).max(2200).optional(),location:z.string().max(240).optional(),depreciationMethod:z.nativeEnum(DepreciationMethod).default("NONE"),usefulLifeMonths:z.coerce.number().int().positive().optional(),salvageValue:money.default("0"),depreciationStartDate:z.coerce.date().optional(),assetLedgerId:z.string().uuid().optional(),accumulatedDepreciationLedgerId:z.string().uuid().optional(),depreciationExpenseLedgerId:z.string().uuid().optional()}).strict(),createInput=base.extend({branchId:z.string().uuid()});
async function actor(write=false){const a=(write?await requirePermissionForMutation("ACCOUNT_ACCOUNTS"):await requirePermission("ACCOUNT_ACCOUNTS"))as ProjectActor;await requireAccountModules(a,"ASSETS");if(!["ACCOUNT_ADMIN","ACCOUNTANT"].includes(a.accountRole??""))throw new AuthorizationError();return a}const branchWhere=(a:ProjectActor)=>a.branchAccessScope==="SELECTED_BRANCHES"?{branchId:{in:a.branchIds??[]}}:{};
async function validateLinks(tx:Prisma.TransactionClient,a:ProjectActor,branchId:string,d:z.infer<typeof base>){let value=new D(d.purchaseValue);if(d.purchaseDocumentId){const doc=await tx.commercialDocument.findFirst({where:{id:d.purchaseDocumentId,companyId:a.companyId,branchId,type:"PURCHASE_BILL",status:"POSTED",...(d.vendorId?{vendorId:d.vendorId}:{})}});if(!doc)throw new AuthorizationError();if(d.purchaseDocumentLineId){const line=await tx.commercialDocumentLine.findFirst({where:{id:d.purchaseDocumentLineId,companyId:a.companyId,documentId:doc.id}});if(!line)throw new AuthorizationError();value=line.taxableAmount;}}else if(d.purchaseDocumentLineId)throw new Error("PURCHASE_DOCUMENT_REQUIRED");const specs=[[d.assetLedgerId,"ASSET"],[d.accumulatedDepreciationLedgerId,"ASSET"],[d.depreciationExpenseLedgerId,"EXPENSE"]]as const;for(const[id,accountClass]of specs)if(id&&!await tx.ledgerAccount.findFirst({where:{id,companyId:a.companyId,accountClass,isActive:true,allowPosting:true}}))throw new Error("INVALID_ASSET_LEDGER");if(d.depreciationMethod!=="NONE"&&(!d.usefulLifeMonths||!d.depreciationStartDate))throw new Error("DEPRECIATION_CONFIGURATION_REQUIRED");if(new D(d.salvageValue).gt(value))throw new Error("SALVAGE_EXCEEDS_VALUE");return value;}
export async function assetOptions(){const a=await actor();const branches=await db.branch.findMany({where:{companyId:a.companyId,isActive:true,...(a.branchAccessScope==="SELECTED_BRANCHES"?{id:{in:a.branchIds??[]}}:{})}});return{branches,vendors:await db.vendor.findMany({where:{companyId:a.companyId,isActive:true}}),purchases:await db.commercialDocument.findMany({where:{companyId:a.companyId,status:"POSTED",type:"PURCHASE_BILL",branchId:{in:branches.map(x=>x.id)}},include:{lines:true}}),users:await db.user.findMany({where:{companyId:a.companyId,isActive:true},select:{id:true,name:true,branchAccessScope:true,branchAccesses:{select:{branchId:true}}}}),ledgers:await db.ledgerAccount.findMany({where:{companyId:a.companyId,isActive:true,allowPosting:true,accountClass:{in:["ASSET","EXPENSE"]}}})};}
export async function createAsset(raw:unknown){const a=await actor(true),d=createInput.parse(raw);if(!await db.branch.findFirst({where:{id:d.branchId,companyId:a.companyId,isActive:true,...(a.branchAccessScope==="SELECTED_BRANCHES"?{id:{in:a.branchIds??[]}}:{})}}))throw new AuthorizationError();return db.$transaction(async tx=>{const purchaseValue=await validateLinks(tx,a,d.branchId,d),assetNumber=await allocateDocumentNumberInTx(tx,{companyId:a.companyId!,branchId:d.branchId,seriesKey:"ASSET",defaults:{prefix:"AST-",padding:6}}),row=await tx.asset.create({data:{...d,companyId:a.companyId!,assetNumber,purchaseValue,salvageValue:new D(d.salvageValue),createdById:a.id}});await audit(tx,a,"ASSET_CREATED",row.id);return row;},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable});}
async function scoped(a:ProjectActor,id:string){const row=await db.asset.findFirst({where:{id,companyId:a.companyId,...branchWhere(a)}});if(!row)throw new AuthorizationError();return row;}
export async function getAsset(id:string){const a=await actor(),asset=await scoped(a,id);return{asset,history:await db.assetAssignmentHistory.findMany({where:{companyId:a.companyId,assetId:id},orderBy:{assignedAt:"desc"}})};}
export async function updateAsset(id:string,raw:unknown){const a=await actor(true),current=await scoped(a,id),d=base.parse(raw);return db.$transaction(async tx=>{const purchaseValue=await validateLinks(tx,a,current.branchId,d);const row=await tx.asset.update({where:{id},data:{...d,purchaseValue,salvageValue:new D(d.salvageValue)}});await audit(tx,a,"ASSET_UPDATED",id);return row;});}
export async function assignAsset(assetId:string,userId:string,notes?:string){const a=await actor(true),asset=await scoped(a,assetId);if(!["ACTIVE","ASSIGNED"].includes(asset.status))throw new Error("ASSET_NOT_ASSIGNABLE");return db.$transaction(async tx=>{await tx.$queryRaw`SELECT "id" FROM "assets" WHERE "id"=${assetId}::uuid FOR UPDATE`;if(!await tx.user.findFirst({where:{id:userId,companyId:a.companyId,isActive:true,OR:[{branchAccessScope:"ALL_BRANCHES"},{branchAccesses:{some:{branchId:asset.branchId}}}]}}))throw new AuthorizationError();await tx.assetAssignmentHistory.updateMany({where:{companyId:a.companyId,assetId,returnedAt:null},data:{returnedAt:new Date()}});const history=await tx.assetAssignmentHistory.create({data:{companyId:a.companyId!,assetId,assignedToId:userId,assignedById:a.id,notes}});await tx.asset.update({where:{id:assetId},data:{assignedUserId:userId,status:"ASSIGNED"}});await audit(tx,a,"ASSET_ASSIGNED",assetId,{userId});return history;});}
export async function returnAsset(assetId:string,notes?:string){const a=await actor(true);await scoped(a,assetId);return db.$transaction(async tx=>{const changed=await tx.assetAssignmentHistory.updateMany({where:{companyId:a.companyId,assetId,returnedAt:null},data:{returnedAt:new Date(),...(notes?{notes}:{})}});if(changed.count!==1)throw new Error("ASSET_NOT_ASSIGNED");await tx.asset.update({where:{id:assetId},data:{assignedUserId:null,status:"ACTIVE"}});await audit(tx,a,"ASSET_RETURNED",assetId);});}
export async function setAssetStatus(assetId:string,status:AssetStatus){const a=await actor(true),asset=await scoped(a,assetId);if(status==="ASSIGNED")throw new Error("USE_ASSIGNMENT_FLOW");if(asset.assignedUserId)throw new Error("RETURN_ASSET_FIRST");const row=await db.asset.update({where:{id:assetId},data:{status}});await db.accountOperationalAudit.create({data:{companyId:a.companyId!,actorUserId:a.id,eventType:"ASSET_STATUS_CHANGED",entityType:"ASSET",entityId:assetId,metadata:{status}}});return row;}
export function depreciationPreview(value:Prisma.Decimal,salvage:Prisma.Decimal,months:number){if(months<=0||salvage.gt(value))throw new Error("INVALID_DEPRECIATION");return value.sub(salvage).div(months).toDecimalPlaces(2);}
async function audit(tx:Prisma.TransactionClient,a:ProjectActor,eventType:string,id:string,metadata?:Prisma.InputJsonValue){await tx.accountOperationalAudit.create({data:{companyId:a.companyId!,actorUserId:a.id,eventType,entityType:"ASSET",entityId:id,metadata}})}
