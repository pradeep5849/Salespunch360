import {Prisma} from "@prisma/client";import {db} from "@/lib/db";import {calculateOrder} from "@/lib/billing/math";import {currentPrices,createMobileBillingOrder,createMobileAccountPackageOrder} from "@/lib/billing/service";import {ACCOUNT_PACKAGE_YEARLY_PRICE_INR} from "@/lib/billing/account-package";import {editionAllowsAccount,editionAllowsSales} from "@/lib/product/entitlements";import {mobileCan,type MobilePrincipal} from "./auth";import {MobileCompanyError} from "./company";
function access(p:MobilePrincipal){if(p.salesRole!=="PRIMARY_ADMIN"||!mobileCan(p,"SALES_BILLING"))throw new MobileCompanyError("FORBIDDEN",403);return p.companyId}
export async function mobileBillingContext(p:MobilePrincipal){const companyId=access(p),company=await db.company.findUnique({where:{id:companyId},select:{productEdition:true,teamStructure:true}});if(!company)throw new MobileCompanyError("NOT_FOUND",404);const prices=await currentPrices(),orders=await db.billingOrder.findMany({where:{companyId},orderBy:{createdAt:"desc"},take:30,select:{id:true,status:true,billingPeriod:true,adminSeats:true,managerSeats:true,salesSeats:true,totalAmount:true,currency:true,provider:true,createdAt:true,expiresAt:true}});return{hasSales:editionAllowsSales(company.productEdition),hasAccount:editionAllowsAccount(company.productEdition),teamStructure:company.teamStructure,prices:prices.map(x=>({...x,amount:x.amount.toFixed(2)})),orders:orders.map(x=>({...x,totalAmount:x.totalAmount.toFixed(2)})),onlinePaymentAvailable:false}}
export async function mobileBillingQuote(p:MobilePrincipal,raw:unknown){access(p);const input=(raw&&typeof raw==="object"?raw:{}) as Record<string,unknown>;const prices=await currentPrices();if(input.kind==="ACCOUNT_PACKAGE"){const quantity=Number(input.quantity);if(!Number.isInteger(quantity)||quantity<1||quantity>100)throw new MobileCompanyError("INVALID_INPUT");const unit=prices.find(x=>x.role==="ACCOUNT_PACKAGE"&&x.period==="YEARLY")?.amount??new Prisma.Decimal(ACCOUNT_PACKAGE_YEARLY_PRICE_INR);return{kind:"ACCOUNT_PACKAGE",billingPeriod:"YEARLY",quantity,unitPrice:unit.toFixed(2),totalAmount:unit.mul(quantity).toFixed(2),currency:"INR"}}const period=input.billingPeriod;if(typeof period!=="string"||!["MONTHLY","SIX_MONTH","YEARLY"].includes(period))throw new MobileCompanyError("INVALID_INPUT");const a=Number(input.adminSeats??0),m=Number(input.managerSeats??0),s=Number(input.salesSeats??0);if(![a,m,s].every(Number.isInteger)||Math.min(a,m,s)<0||a+m+s<1)throw new MobileCompanyError("INVALID_INPUT");const ap=prices.find(x=>x.role==="ADMIN"&&x.period===period),mp=prices.find(x=>x.role==="MANAGER"&&x.period===period),sp=prices.find(x=>x.role==="SALES"&&x.period===period);if(!ap||!mp||!sp)throw new MobileCompanyError("PRICING_UNAVAILABLE",409);const money=calculateOrder(a,m,s,ap.amount,mp.amount,sp.amount);return{kind:"SALES",billingPeriod:period,adminSeats:a,managerSeats:m,salesSeats:s,totalAmount:money.totalAmount.toFixed(2),currency:"INR"}}

export async function mobileManualOrder(p:MobilePrincipal,raw:unknown){access(p);const input=(raw&&typeof raw==="object"?raw:{}) as Record<string,unknown>;if(input.paymentMethod!=="MANUAL")throw new MobileCompanyError("INVALID_PAYMENT_METHOD");if(input.kind==="ACCOUNT_PACKAGE"){const quantity=Number(input.quantity);return createMobileAccountPackageOrder({id:p.id,companyId:p.companyId},quantity)}const key=typeof input.idempotencyKey==="string"?input.idempotencyKey:"";return createMobileBillingOrder({id:p.id,companyId:p.companyId},{billingPeriod:input.billingPeriod as "MONTHLY"|"SIX_MONTH"|"YEARLY",adminSeats:Number(input.adminSeats??0),managerSeats:Number(input.managerSeats??0),salesSeats:Number(input.salesSeats??0),retainAdminUserIds:Array.isArray(input.retainAdminUserIds)?input.retainAdminUserIds.filter((x):x is string=>typeof x==="string"):[],retainManagerUserIds:Array.isArray(input.retainManagerUserIds)?input.retainManagerUserIds.filter((x):x is string=>typeof x==="string"):[],retainSalesUserIds:Array.isArray(input.retainSalesUserIds)?input.retainSalesUserIds.filter((x):x is string=>typeof x==="string"):[],idempotencyKey:key})}
