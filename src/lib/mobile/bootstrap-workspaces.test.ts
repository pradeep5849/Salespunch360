import{describe,expect,it,vi}from"vitest";
const mocks=vi.hoisted(()=>({company:vi.fn(),attendance:vi.fn(),entitlement:vi.fn()}));vi.mock("@/lib/db",()=>({db:{company:{findUnique:mocks.company},attendance:{findFirst:mocks.attendance}}}));vi.mock("@/lib/billing/entitlement",()=>({effectiveEntitlement:mocks.entitlement}));
import{mobileBootstrap,type MobileAppPrincipal}from"./auth";
const entitlement={state:"ACTIVE",operationalWritesAllowed:true,adminLimit:0,adminUsage:0,managerLimit:0,salesLimit:0,managerUsage:0,salesUsage:0};
describe("mobile workspace bootstrap",()=>{it("keeps Account-only bootstrap free of Sales field and GPS authority",async()=>{mocks.company.mockResolvedValue({name:"Acme",addressLine1:null,addressLine2:null,locality:null,city:null,state:null,postalCode:null,country:null,teamStructure:"SALES_ONLY",attendanceEnabled:true,gpsTrackingEnabled:true});mocks.entitlement.mockResolvedValue(entitlement);const principal={id:"user",name:"Account User",email:"a@example.test",companyId:"company",role:"ACCOUNT_USER",salesRole:null,accountRole:"ACCOUNTANT",managerType:null,branchIds:["branch"],mobileSessionId:"mobile",productEdition:"SALESPUNCH360_ACCOUNT",authorizedWorkspaces:["ACCOUNT"]}satisfies MobileAppPrincipal;const result=await mobileBootstrap(principal);expect(result).toMatchObject({productEdition:"SALESPUNCH360_ACCOUNT",authorizedWorkspaces:["ACCOUNT"],canSwitchWorkspace:false,user:{salesRole:null,accountRole:"ACCOUNTANT"},features:{attendanceEnabled:false,gpsTrackingEnabled:false,fieldWorkEnabled:false},capabilities:{canManageEmployees:false,canManageSalesSettings:false,canAccessSalesBilling:false,canViewReports:false},attendance:null});expect(mocks.attendance).not.toHaveBeenCalled()})});
