import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
const schema=readFileSync(new URL("../../../prisma/schema.prisma",import.meta.url),"utf8"),migration=readFileSync(new URL("../../../prisma/migrations/20260902140000_manager_types/migration.sql",import.meta.url),"utf8");
describe("manager type persistence",()=>{it("defines both manager modes",()=>{expect(schema).toMatch(/enum ManagerType[\s\S]*FIELD_MANAGER[\s\S]*MANAGER_ONLY/);expect(schema).toMatch(/managerType\s+ManagerType\?/)});it("preserves existing managers as Field Manager",()=>{expect(migration).toContain('SET "managerType" = \'FIELD_MANAGER\'');expect(migration).toContain('WHERE "role" = \'MANAGER\'')});it("enforces role/type consistency",()=>{expect(migration).toContain('users_manager_type_consistency');expect(migration).toContain('"role" = \'MANAGER\' AND "managerType" IS NOT NULL');expect(migration).toContain('"role" <> \'MANAGER\' AND "managerType" IS NULL')})});
