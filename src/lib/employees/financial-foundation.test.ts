import{describe,expect,it}from"vitest";import{assertEmployeeFinancialTenant,compensationProfileSchema,employeeAdvanceSchema}from"./financial-foundation";const id="11111111-1111-4111-8111-111111111111";
describe("payroll-ready employee foundation",()=>{it("accepts decimal money without floating-point coercion",()=>expect(compensationProfileSchema.parse({employeeId:id,baseAmount:"12345.67",currencyCode:"INR",effectiveFrom:"2026-09-09"}).baseAmount).toBe("12345.67"));it("rejects excess precision and forged fields",()=>{expect(()=>employeeAdvanceSchema.parse({employeeId:id,branchId:id,amount:"1.001",currencyCode:"INR",advanceDate:new Date()})).toThrow();expect(()=>compensationProfileSchema.parse({employeeId:id,baseAmount:"1",effectiveFrom:new Date(),companyId:"forged"})).toThrow()});it("enforces employee and Branch tenant identity",()=>{expect(()=>assertEmployeeFinancialTenant("a",{companyId:"b"},{companyId:"a",isActive:true})).toThrow("NOT_FOUND");expect(()=>assertEmployeeFinancialTenant("a",{companyId:"a"},{companyId:"b",isActive:true})).toThrow("NOT_FOUND")})});
