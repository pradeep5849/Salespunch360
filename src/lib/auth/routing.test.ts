import{describe,expect,it}from"vitest";import{authenticatedHome}from"./routing";
describe("product-aware authenticated routing",()=>{it("routes platform, Sales, and Account-only identities",()=>{expect(authenticatedHome({role:"SUPER_ADMIN",companyId:null})).toBe("/admin");expect(authenticatedHome({role:"COMPANY_ADMIN",salesRole:"PRIMARY_ADMIN",salesAccessActive:true})).toBe("/workspace");expect(authenticatedHome({role:"COMPANY_ADMIN",accountRole:"ACCOUNT_ADMIN",accountAccessActive:true})).toBe("/workspace/account")});it("never routes a tenant-attached legacy SUPER_ADMIN value into platform administration",()=>{expect(authenticatedHome({role:"SUPER_ADMIN",companyId:"tenant",isActive:true,salesRole:null,accountRole:null,salesAccessActive:false,accountAccessActive:false,managerType:null},"SALESPUNCH360_PLUS")).toBe("/sign-in")})});
