import{describe,expect,it}from"vitest";import{authenticatedHome}from"./routing";
describe("product-aware authenticated routing",()=>{it("routes platform, Sales, and Account-only identities",()=>{expect(authenticatedHome({role:"SUPER_ADMIN"})).toBe("/admin");expect(authenticatedHome({role:"COMPANY_ADMIN",salesRole:"PRIMARY_ADMIN",salesAccessActive:true})).toBe("/workspace");expect(authenticatedHome({role:"COMPANY_ADMIN",accountRole:"ACCOUNT_ADMIN",accountAccessActive:true})).toBe("/workspace/employees")})});
