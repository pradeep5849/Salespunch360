import{describe,expect,it}from"vitest";import{canUsePermission,canUsePermissionForMutation}from"./permissions";import{mobileCan,mobileFieldWorkEnabled}from"@/lib/mobile/auth";
const base={id:"u",name:"User",email:"u@example.test",companyId:"c",role:"SALES"as const,isActive:true,salesAccessActive:true,accountAccessActive:false,accountRole:null,branchAccessScope:"ALL_BRANCHES"as const,branchIds:[],mobileSessionId:"s"};const office={...base,salesRole:"MANAGER"as const,managerType:"MANAGER_ONLY"as const};const field={...base,salesRole:"MANAGER"as const,managerType:"FIELD_MANAGER"as const};const sales={...base,salesRole:"SALES"as const,managerType:null};
describe("Office Manager field-work closure",()=>{it.each(["SALES_ATTENDANCE","SALES_CHECK_INS","SALES_CUSTOMERS","SALES_LEADS","SALES_FOLLOW_UPS","SALES_TARGETS","SALES_TRAVEL"]as const)("rejects direct web/API and mobile %s",permission=>{expect(canUsePermissionForMutation(office,"SALESPUNCH360",permission)).toBe(false);expect(mobileCan(office,permission)).toBe(false)});it("keeps supervisory dashboard/report visibility",()=>{expect(canUsePermission(office,"SALESPUNCH360","SALES_DASHBOARD")).toBe(true);expect(canUsePermission(office,"SALESPUNCH360","SALES_REPORTS")).toBe(true)});it("keeps Sales Manager and Sales Employee field-enabled",()=>{expect(mobileFieldWorkEnabled(field)).toBe(true);expect(mobileFieldWorkEnabled(sales)).toBe(true);expect(mobileCan(field,"SALES_ATTENDANCE")).toBe(true);expect(mobileCan(sales,"SALES_CHECK_INS")).toBe(true)});it("marks Office Manager mobile bootstrap policy field-disabled",()=>expect(mobileFieldWorkEnabled(office)).toBe(false))});
