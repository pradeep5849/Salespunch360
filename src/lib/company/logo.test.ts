import sharp from"sharp";import{describe,expect,it}from"vitest";import{companyLogoKey,MAX_COMPANY_LOGO_BYTES,processCompanyLogo}from"./logo";
describe("company logos",()=>{it("uses the exact deterministic object key",()=>expect(companyLogoKey("company-id")).toBe("Logo/company-id.webp"));it("validates and normalizes images without enlargement",async()=>{const input=await sharp({create:{width:20,height:10,channels:3,background:"red"}}).png().toBuffer(),result=await processCompanyLogo(new File([input.buffer.slice(input.byteOffset,input.byteOffset+input.byteLength) as ArrayBuffer],"logo.png",{type:"image/png"})),meta=await sharp(result).metadata();expect(meta.format).toBe("webp");expect(meta.width).toBe(20);expect(meta.height).toBe(10)});it("rejects invalid and oversized inputs",async()=>{await expect(processCompanyLogo(new File(["bad"],"logo.png",{type:"image/png"}))).rejects.toThrow("LOGO_INVALID");const oversized={size:MAX_COMPANY_LOGO_BYTES+1,type:"image/png",arrayBuffer:async()=>new ArrayBuffer(0)} as File;await expect(processCompanyLogo(oversized)).rejects.toThrow("LOGO_INVALID")})});
