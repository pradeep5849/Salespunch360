import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const read=(p:string)=>readFileSync(p,"utf8");
describe("company logo contracts",()=>{it("keeps registration optional and compensates a failed registration",()=>{const form=read("src/app/register/registration-form.tsx"),service=read("src/lib/auth/registration.ts");expect(form).toContain("Company Logo (optional)");expect(form).not.toMatch(/companyLogo[^>]+required/);expect(service).toContain("if (logo)");expect(service).toContain("privateStorage().delete(writtenLogoKey)")});it("derives private logo access from the session company",()=>{const route=read("src/app/api/company-logo/route.ts");expect(route).toContain("requireUser()");expect(route).toContain("user.companyId");expect(route).not.toMatch(/params|searchParams|companyId:string/);expect(route).toContain('"Cache-Control":"private, no-store"')});it("limits replacement to admins and keeps profile completion independent",()=>{expect(read("src/app/actions/company-profile.ts")).toContain('requireRole("COMPANY_ADMIN")');expect(read("src/lib/company/profile.ts")).not.toContain("logoObjectKey")});it("renders initial fallback and image failure fallback",()=>{const source=read("src/components/workspace/company-identity-logo.tsx");expect(source).toContain("!hasLogo||failed");expect(source).toContain("onError")})});
