import {Prisma} from '@prisma/client';import {db} from '@/lib/db';import {requireRole} from '@/lib/auth/authorization';import {calculateOrder,addBillingPeriod,renewalWindow} from './math';import {orderRequestSchema,priceChangeSchema,overrideSchema} from './validation';import type {VerifiedPayment} from './provider';import {effectiveEntitlement} from './entitlement';import {assertManagerSeatsAllowed} from './policy';
const admin=async()=>{const u=await requireRole('COMPANY_ADMIN');if(!u.companyId)throw new Error('NOT_AUTHORIZED');return{...u,companyId:u.companyId}};
export async function currentPrices(){return db.billingPrice.findMany({where:{effectiveUntil:null,currency:'INR'},orderBy:[{role:'asc'},{period:'asc'}]})}
export async function changePrice(raw:unknown){const u=await requireRole('SUPER_ADMIN'),d=priceChangeSchema.parse(raw),now=new Date();return db.$transaction(async tx=>{await tx.billingPrice.updateMany({where:{role:d.role,period:d.period,currency:d.currency,effectiveUntil:null},data:{effectiveUntil:now}});const p=await tx.billingPrice.create({data:{...d,amount:new Prisma.Decimal(d.amount),effectiveFrom:now,createdByUserId:u.id}});await tx.billingAuditEvent.create({data:{actorUserId:u.id,type:'PRICING_CHANGED',entityId:p.id,metadata:{role:d.role,period:d.period,currency:d.currency}}});return p})}
export async function createBillingOrder(raw:unknown){const u=await admin(),d=orderRequestSchema.parse(raw),existing=await db.billingOrder.findUnique({where:{idempotencyKey:d.idempotencyKey}});if(existing){if(existing.companyId!==u.companyId)throw new Error('NOT_FOUND');return existing}const company=await db.company.findUnique({where:{id:u.companyId},select:{teamStructure:true}});if(!company)throw new Error('NOT_FOUND');assertManagerSeatsAllowed(company.teamStructure,d.managerSeats);const e=await effectiveEntitlement(u.companyId);if(d.managerSeats<e.managerUsage||d.salesSeats<e.salesUsage)throw new Error('SEATS_BELOW_USAGE');const prices=await currentPrices(),mp=prices.find(p=>p.role==='MANAGER'&&p.period===d.billingPeriod),sp=prices.find(p=>p.role==='SALES'&&p.period===d.billingPeriod);if(!mp||!sp||mp.currency!==sp.currency)throw new Error('PRICING_UNAVAILABLE');const money=calculateOrder(d.managerSeats,d.salesSeats,mp.amount,sp.amount);return db.$transaction(async tx=>{const o=await tx.billingOrder.create({data:{companyId:u.companyId,createdByUserId:u.id,...d,managerUnitPrice:mp.amount,salesUnitPrice:sp.amount,currency:'INR',...money,provider:'UNCONFIGURED',expiresAt:new Date(Date.now()+86400000)}});await tx.billingAuditEvent.create({data:{companyId:u.companyId,actorUserId:u.id,type:'ORDER_CREATED',entityId:o.id,metadata:{period:d.billingPeriod,managerSeats:d.managerSeats,salesSeats:d.salesSeats}}});return o})}
export async function activateVerifiedPayment(v:VerifiedPayment){return db.$transaction(async tx=>{const prior=await tx.paymentTransaction.findUnique({where:{providerPaymentId:v.providerPaymentId}});if(prior)return prior;const order=await tx.billingOrder.findUnique({where:{id:v.orderId}});if(!order||order.status!=='PENDING'||order.totalAmount.toFixed(2)!==new Prisma.Decimal(v.amount).toFixed(2)||order.currency!==v.currency)throw new Error('PAYMENT_MISMATCH');const current=await tx.companySubscription.findFirst({where:{companyId:order.companyId,status:'ACTIVE'},orderBy:{endsAt:'desc'}}),start=renewalWindow(v.capturedAt,current?.endsAt??null),end=addBillingPeriod(start,order.billingPeriod);const payment=await tx.paymentTransaction.create({data:{companyId:order.companyId,orderId:order.id,provider:v.provider,providerPaymentId:v.providerPaymentId,amount:order.totalAmount,currency:order.currency,status:'CAPTURED',capturedAt:v.capturedAt}});await tx.billingOrder.update({where:{id:order.id},data:{status:'PAID',paidAt:v.capturedAt}});const sub=await tx.companySubscription.create({data:{companyId:order.companyId,status:'ACTIVE',billingPeriod:order.billingPeriod,managerSeats:order.managerSeats,salesSeats:order.salesSeats,startsAt:start,endsAt:end,sourceOrderId:order.id}});await tx.company.update({where:{id:order.companyId},data:{subscriptionStatus:'ACTIVE'}});await tx.billingAuditEvent.createMany({data:[{companyId:order.companyId,type:'PAYMENT_CAPTURED',entityId:payment.id},{companyId:order.companyId,type:current?'SUBSCRIPTION_RENEWED':'SUBSCRIPTION_ACTIVATED',entityId:sub.id}]});return payment})}
export async function billingDashboard(){const u=await admin();const [company,entitlement,prices,orders]=await Promise.all([db.company.findUnique({where:{id:u.companyId},select:{name:true,subscriptionStatus:true,trialEndsAt:true,teamStructure:true}}),effectiveEntitlement(u.companyId),currentPrices(),db.billingOrder.findMany({where:{companyId:u.companyId},include:{payments:true},orderBy:{createdAt:'desc'},take:50})]);return{company,entitlement,prices,orders}}
export async function platformBilling(){await requireRole('SUPER_ADMIN');return{prices:await currentPrices(),companies:await db.company.findMany({select:{id:true,name:true,subscriptionStatus:true,trialStartedAt:true,trialEndsAt:true,subscriptions:{orderBy:{endsAt:'desc'},take:1},_count:{select:{users:true,billingOrders:true,payments:true}}},orderBy:{name:'asc'},take:500})}}
export async function manualOverride(raw:unknown){const u=await requireRole('SUPER_ADMIN'),d=overrideSchema.parse(raw),now=new Date(),end=new Date(d.endsAt);return db.$transaction(async tx=>{const usage=await tx.user.groupBy({by:['role'],where:{companyId:d.companyId,isActive:true,role:{in:['MANAGER','SALES']}},_count:true});const m=usage.find(x=>x.role==='MANAGER')?._count??0,s=usage.find(x=>x.role==='SALES')?._count??0;if(d.managerSeats<m||d.salesSeats<s)throw new Error('SEATS_BELOW_USAGE');const sub=await tx.companySubscription.create({data:{companyId:d.companyId,status:d.status,billingPeriod:d.billingPeriod,managerSeats:d.managerSeats,salesSeats:d.salesSeats,startsAt:now,endsAt:end}});await tx.company.update({where:{id:d.companyId},data:{subscriptionStatus:d.status==='ACTIVE'?'ACTIVE':'SUSPENDED'}});await tx.billingAuditEvent.create({data:{companyId:d.companyId,actorUserId:u.id,type:'SUPER_ADMIN_OVERRIDE',entityId:sub.id,reason:d.reason}});return sub})}
