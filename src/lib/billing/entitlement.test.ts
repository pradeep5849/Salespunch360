import {describe,expect,it} from 'vitest';import {salesEntitlementTerm} from './entitlement';
const d=(s:string)=>new Date(s);const sub=(adminSeats:number,managerSeats:number,salesSeats:number,endsAt:string,provider='UNCONFIGURED')=>({adminSeats,managerSeats,salesSeats,endsAt:d(endsAt),sourceOrder:{provider}});
describe('Sales entitlement term compatibility',()=>{it('combines historical same-expiry incremental purchases',()=>{const rows=salesEntitlementTerm([sub(2,0,0,'2026-10-01'),sub(0,1,1,'2026-10-01')]);expect(rows.reduce((n,s)=>n+s.adminSeats,0)).toBe(2);expect(rows.reduce((n,s)=>n+s.managerSeats,0)).toBe(1);expect(rows.reduce((n,s)=>n+s.salesSeats,0)).toBe(1)});it('does not double count an older overlapping term',()=>{const rows=salesEntitlementTerm([sub(2,0,0,'2026-10-01'),sub(1,1,2,'2027-04-01')]);expect(rows).toHaveLength(1);expect(rows[0]?.salesSeats).toBe(2)});it('ignores Account-only subscription rows',()=>{const rows=salesEntitlementTerm([sub(4,0,0,'2027-04-01','ACCOUNT_PACKAGE'),sub(0,1,1,'2027-04-01')]);expect(rows).toHaveLength(1);expect(rows[0]?.managerSeats).toBe(1)})});
