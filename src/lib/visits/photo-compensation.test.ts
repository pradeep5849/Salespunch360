import{describe,expect,it,vi}from"vitest";import{compensateWrittenPhoto}from"./photo-compensation";
describe("photo transaction compensation",()=>{it("removes both written objects and preserves the DB error",async()=>{const error=new Error("LATER_TRANSACTION_FAILURE"),storage={put:vi.fn(),get:vi.fn(),delete:vi.fn().mockResolvedValue(undefined)};await expect(compensateWrittenPhoto(storage,{objectKey:"photo.webp",thumbnailObjectKey:"thumb.webp"},error)).rejects.toBe(error);expect(storage.delete).toHaveBeenCalledTimes(2)});it("cleanup failure never hides the original error",async()=>{const error=new Error("DB_ROLLBACK"),storage={put:vi.fn(),get:vi.fn(),delete:vi.fn().mockRejectedValue(new Error("storage"))};await expect(compensateWrittenPhoto(storage,{objectKey:"photo.webp",thumbnailObjectKey:"thumb.webp"},error)).rejects.toBe(error)});});
