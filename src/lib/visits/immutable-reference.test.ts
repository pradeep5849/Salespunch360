import{describe,expect,it}from"vitest";import{assertWithinImmutableReference,hasImmutableReference}from"./service";
const original={checkInReferenceLatitude:19,checkInReferenceLongitude:72,checkInReferenceSetAt:new Date("2026-01-01"),checkInReferenceVisitId:null};
describe("detached immutable visit references",()=>{it("survives repeated Lead delete/recreate cycles without walking Customer coordinates",()=>{let customer={...original};for(const nearby of[{latitude:19.0001,longitude:72},{latitude:19.0002,longitude:72}]){expect(hasImmutableReference(customer)).toBe(true);expect(()=>assertWithinImmutableReference({latitude:customer.checkInReferenceLatitude,longitude:customer.checkInReferenceLongitude},nearby)).not.toThrow();customer={...customer,checkInReferenceVisitId:null};expect(customer).toMatchObject(original)}expect(()=>assertWithinImmutableReference({latitude:19,longitude:72},{latitude:19.001,longitude:72})).toThrow("REPEAT_VISIT_OUTSIDE_RADIUS")});it("retention cannot replace A with oldest retained B after provenance deletion",()=>{const retainedB={latitude:19.0001,longitude:72};expect(hasImmutableReference(original)).toBe(true);expect(original.checkInReferenceLatitude).toBe(19);expect(original.checkInReferenceLongitude).toBe(72);expect(retainedB.latitude).not.toBe(original.checkInReferenceLatitude)});it("standalone Lead reference remains established after source provenance expires",()=>expect(hasImmutableReference(original)).toBe(true))});
