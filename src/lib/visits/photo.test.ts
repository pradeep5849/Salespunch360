import sharp from"sharp";import{describe,expect,it}from"vitest";import{processVisitPhoto}from"./photo";
describe("visit photo processing",()=>{it("produces bounded WebP main and thumbnail variants without an original",async()=>{const original=await sharp({create:{width:1200,height:800,channels:3,background:"red"}}).jpeg().toBuffer(),result=await processVisitPhoto(new File([Uint8Array.from(original)],"camera.jpg",{type:"image/jpeg"})),main=await sharp(result.main).metadata(),thumb=await sharp(result.thumbnail).metadata();expect(result.mimeType).toBe("image/webp");expect(main.format).toBe("webp");expect(Math.max(main.width!,main.height!)).toBeLessThanOrEqual(720);expect(thumb.format).toBe("webp");expect(Math.max(thumb.width!,thumb.height!)).toBeLessThanOrEqual(256);expect(result).not.toHaveProperty("original")});it("rejects corrupt and non-image uploads",async()=>{await expect(processVisitPhoto(new File(["not image"],"bad.jpg",{type:"image/jpeg"}))).rejects.toThrow("PHOTO_INVALID");await expect(processVisitPhoto(new File(["x"],"x.txt",{type:"text/plain"}))).rejects.toThrow("PHOTO_INVALID")});});
