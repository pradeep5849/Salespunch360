import{describe,expect,it}from"vitest";import{assigneesForBranch}from"./options";import{resolveWriteBranch}from"@/lib/branches/operational-scope";import{readFileSync}from"node:fs";
const branch=(id:string)=>({id,companyId:"company",isActive:true});
describe("Sales Customer branch correction",()=>{it("automatically resolves one authorized branch",()=>expect(resolveWriteBranch([branch("a")])).toBe("a"));it("requires selection with multiple authorized branches",()=>expect(()=>resolveWriteBranch([branch("a"),branch("b")])).toThrow("BRANCH_REQUIRED"));it("accepts an authorized selection and rejects unauthorized selection",()=>{expect(resolveWriteBranch([branch("a"),branch("b")],"a")).toBe("a");expect(()=>resolveWriteBranch([branch("a")],"b")).toThrow("BRANCH_FORBIDDEN")});it("filters assignment candidates by customer branch without N+1",()=>{const users=[{id:"all",branchAccessScope:"ALL_BRANCHES",branchAccesses:[]},{id:"a",branchAccessScope:"SELECTED_BRANCHES",branchAccesses:[{branchId:"a"}]},{id:"b",branchAccessScope:"SELECTED_BRANCHES",branchAccesses:[{branchId:"b"}]}];expect(assigneesForBranch(users,"a").map(x=>x.id)).toEqual(["all","a"])});it("enforces project/customer branch equality and scoped Customer list queries",()=>{const projects=readFileSync("src/lib/account/projects.ts","utf8"),customers=readFileSync("src/app/workspace/customers/page.tsx","utf8");expect(projects).toContain("projectCustomerWhere(actor.companyId,data.branchId,data.customerId)");expect(customers).toContain('searchCustomersForCompany(user.companyId,"",null,branches.branchIds)')});it("does not offer Manager-only or inactive assignment candidates",()=>{const pages=readFileSync("src/app/workspace/customers/new/page.tsx","utf8")+readFileSync("src/app/workspace/customers/page.tsx","utf8");expect(pages).toContain('isActive:true,salesAccessActive:true');expect(pages).toContain('{salesRole:"MANAGER",managerType:"FIELD_MANAGER"}');expect(pages).not.toContain('managerType:"MANAGER_ONLY"')})});
