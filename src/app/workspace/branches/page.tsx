import{WorkspacePageHeader}from"@/components/workspace/workspace-page-header";import{listBranches}from"@/lib/branches/management";import{manageBranch}from"@/app/actions/branches-management";import{requireUser}from"@/lib/auth/authorization";import{authenticatedHome}from"@/lib/auth/routing";
const Fields=({branch}:{branch?:{name:string;code:string;addressLine1:string|null;city:string|null;state:string|null;postalCode:string|null;country:string|null;phone:string|null;email:string|null}})=><div className="field-grid"><label>Name<input name="name" defaultValue={branch?.name} required/></label><label>Code<input name="code" defaultValue={branch?.code} required/></label>{[["addressLine1","Address"],["city","City"],["state","State"],["postalCode","Postal code"],["country","Country"],["phone","Phone"],["email","Email"]].map(([name,label])=><label key={name}>{label}<input name={name} type={name==="email"?"email":"text"} defaultValue={branch?.[name as keyof typeof branch]??""}/></label>)}</div>;
export default async function BranchesPage(){const [branches,user]=await Promise.all([listBranches(),requireUser()]);const backHref=authenticatedHome(user);return <main className="employees-shell"><section className="employees-content"><WorkspacePageHeader title="Branches" backHref={backHref}/><form action={manageBranch} className="employee-form"><input type="hidden" name="operation" value="create"/><h2>Add Branch</h2><Fields/><button>Create Branch</button></form>{branches.map(branch=><article className="employee-card" key={branch.id}><h2>{branch.name} ({branch.code})</h2><p>{branch.isPrimary?"Primary Head Office":branch.isActive?"Active":"Inactive"}</p><details><summary>Manage Branch</summary><form action={manageBranch} className="employee-form"><input type="hidden" name="operation" value="edit"/><input type="hidden" name="branchId" value={branch.id}/><Fields branch={branch}/><button>Save</button></form>{!branch.isPrimary&&<form action={manageBranch}><input type="hidden" name="branchId" value={branch.id}/><input type="hidden" name="operation" value={branch.isActive?"deactivate":"activate"}/><button>{branch.isActive?"Deactivate":"Activate"}</button></form>}</details></article>)}</section></main>}
