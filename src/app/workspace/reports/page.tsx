import Link from "next/link";import { reportActor } from "@/lib/reports/scope";
const reports=[['Check-in Report','Visits, completion, duration and visit-generated leads.','check-ins'],['Advanced Check-in Report','First and repeat customer visit analysis.','check-ins/advanced'],['Attendance Report','Historical work sessions, durations and route totals.','attendance'],['GPS Report','Authorized attendance routes and customer check-in events.','gps'],['Lead Report','Pipeline stages, value, conversion and follow-ups.','leads'],['Geofence Breach Report','Attendance and customer enforcement audit events.','geofence-breaches'],['Target Analysis','Won-lead count and value progress against targets.','targets']];
export default async function ReportsPage(){await reportActor();return <main className="reports-content"><p className="eyebrow">SalesPunch360</p><h1>Reports</h1><p className="muted">Secure historical reporting for your current role and team scope.</p><section className="report-hub">{reports.map(([name,copy,path])=><Link href={`/workspace/reports/${path}`} key={path}><strong>{name}</strong><span>{copy}</span><b>Open report →</b></Link>)}</section></main>}
