import {DownloadExcel} from "@/components/workspace/download-excel";import {gpsReport,type GpsTimelineEvent} from "@/lib/reports/gps";import type{SearchParams}from"@/lib/reports/validation";import{dateTime,distance,ReportHeader,Summary}from"../report-ui";import{GoogleRouteMap}from"../google-route-map";
const names:Record<GpsTimelineEvent["type"],string>={ATTENDANCE_STARTED:"Attendance Started",GPS_STARTED:"GPS Started",CHECK_IN:"Check-in",CHECK_OUT:"Check-out",GPS_ENDED:"GPS Ended",ATTENDANCE_ENDED:"Attendance Ended"};
export default async function Page({searchParams}:{searchParams:Promise<SearchParams>}){const raw=await searchParams,r=await gpsReport(raw);return <main className="reports-content"><ReportHeader title="GPS Route Report" description="Choose an authorized employee and one calendar date. All legitimate attendance sessions are resolved automatically."/><DownloadExcel report="gps" params={raw} ready={Boolean(r.employee)}/><form className="report-filters gps-daily-filters">{r.actor.role!=="SALES"&&<label>Employee<select name="employeeId" defaultValue={r.employeeId||""} required><option value="">Select employee</option>{r.employees.map(e=><option value={e.id} key={e.id}>{e.name} · {e.role}</option>)}</select></label>}<label>Date<input type="date" name="date" defaultValue={r.date||""} required/></label><button>View Report</button></form>{r.employee&&<><Summary items={[{label:"Employee",value:r.employee.name},{label:"Attendance sessions",value:r.sessions.length},{label:"Accepted GPS points",value:r.points.length},{label:"Total Distance Covered",value:distance(r.routeDistanceMeters)}]}/><GoogleRouteMap points={r.points} segments={r.segments} markers={r.markers}/><section className="gps-timeline"><h2>Daily timeline</h2>{r.events.length?r.events.map((e,index)=><article key={e.id}><i>{index+1}</i><div><strong>{names[e.type]}{e.customer?` · ${e.customer}`:""}</strong><time>{dateTime(e.at)}</time>{e.detail&&<p>{e.detail}</p>}{e.latitude!=null&&e.longitude!=null&&<small>{e.latitude.toFixed(6)}, {e.longitude.toFixed(6)}</small>}</div></article>):<div className="empty-state">No stored attendance, GPS, or visit events exist for this date.</div>}</section><section className="gps-warning"><strong>Historical stored GPS data</strong><p>Distance is summed within each attendance session from accepted points. Unrelated sessions are never joined into one artificial segment.</p></section></>}{!r.employee&&<div className="empty-state">Choose an employee and date to view the authorized daily route.</div>}</main>}
