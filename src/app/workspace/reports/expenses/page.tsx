import {DownloadExcel} from "@/components/workspace/download-excel";import {expenseReport} from "@/lib/reports/expenses";import type {SearchParams} from "@/lib/reports/validation";import {distance,Filters,ReportHeader} from "../report-ui";
export default async function Page({searchParams}:{searchParams:Promise<SearchParams>}){const raw=await searchParams,r=await expenseReport(raw);return <main className="reports-content"><ReportHeader title="Expense Report" description="Verified distance comes only from accepted stored GPS points. Financial calculation remains unavailable until a persistent company travel rate is approved."/><DownloadExcel report="expenses" params={raw}/><Filters filters={r.filters} employees={r.employees}/><div className="report-table-wrap"><table className="report-table"><thead><tr><th>Employee</th><th>Date</th><th>Verified GPS distance</th><th>Travel rate</th><th>Travel expense</th></tr></thead><tbody>{r.rows.map(row=><tr key={row.key}><td>{row.employee}</td><td>{row.date}</td><td>{distance(row.distanceMeters)}</td><td>Not configured</td><td>—</td></tr>)}</tbody></table></div>{!r.rows.length&&<div className="empty-state">No accepted GPS tracking points match these authorized filters.</div>}<p className="gps-warning">Expense approval statuses and a company travel rate do not exist in persistent storage, so no status or amount is fabricated.</p></main>}
