import {notFound}from"next/navigation";import{requirePermission}from"@/lib/auth/authorization";import{requireAccountModules}from"@/lib/account/modules";export default async function Page({params}:{params:Promise<{section:string}>}){const{section}=await params;if(!["settings","reports"].includes(section))notFound();const a=await requirePermission(section==="settings"?"ACCOUNT_SETTINGS":"ACCOUNT_REPORTS");await requireAccountModules(a,"GST_ADVANCED");if(section==="settings"&&a.accountRole!=="ACCOUNT_ADMIN")throw new Error("UNAUTHORIZED");return <main className="employees-shell"><section className="employees-content"><h1>Tax · {section}</h1><p>Company and branch GST profiles, component taxes, withholding, and future integration payload foundations.</p></section></main>}
