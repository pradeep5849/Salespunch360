import Link from "next/link"; import { AccountCard, AccountEmptyState, AccountPageHeader } from "@/components/account/account-shell"; import { requirePermission } from "@/lib/auth/authorization"; import { db } from "@/lib/db";
export default async function Page() { const actor = await requirePermission("ACCOUNT_DASHBOARD"), branch = actor.branchAccessScope === "SELECTED_BRANCHES" ? { branchId: { in: actor.branchIds ?? [] } } : {}, pending = await db.expenseTransaction.count({ where: { companyId: actor.companyId!, status: "PENDING_APPROVAL", ...branch } }); return <><AccountPageHeader title="Attention" subtitle="Real, authorized Account items that may need action."/><AccountCard>{pending ? <div className="account-list"><Link href="/workspace/account/expenses?status=PENDING_APPROVAL"><span><strong>{pending} expenses await approval</strong><small>Review submitted expense records</small></span><span>→</span></Link></div> : <AccountEmptyState title="All caught up" detail="There are no current Account alerts in your authorized Branch scope."/>}</AccountCard></>; }
