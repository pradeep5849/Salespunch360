import { AccountCard, AccountPageHeader } from "@/components/account/account-shell"; import { requirePermission } from "@/lib/auth/authorization";
export default async function Page() { const actor = await requirePermission("ACCOUNT_SETTINGS"); if (actor.accountRole !== "ACCOUNT_ADMIN") throw new Error("UNAUTHORIZED"); return <><AccountPageHeader title="Backup & recovery" subtitle="Application exports complement—but never replace—provider PostgreSQL backups."/><AccountCard><h2>Company backup bundle</h2><p>Generate a checksum-protected JSON bundle containing recoverable business masters and a manifest. Passwords, sessions, tokens and provider secrets are excluded.</p><a className="account-primary" href="/api/account/utilities/backup">Generate & download</a></AccountCard><AccountCard><h2>Recovery responsibility</h2><p>Use Recycle Bin for accidental archive recovery and validated imports for application-level recovery. Maintain tested physical PostgreSQL backups with the hosting/database provider for disaster recovery.</p></AccountCard></>; }
