import { restoreMasterAction } from "@/app/actions/account-finalization"; import { AccountCard, AccountEmptyState, AccountPageHeader } from "@/components/account/account-shell"; import { requirePermission } from "@/lib/auth/authorization"; import { db } from "@/lib/db"; import { formatIndiaDate } from "@/lib/account/presentation";
export default async function Page() { const actor = await requirePermission("ACCOUNT_SETTINGS"), rows = await db.accountRecycleRecord.findMany({ where: { companyId: actor.companyId!, restoredAt: null }, orderBy: { archivedAt: "desc" }, take: 100 }); return <><AccountPageHeader title="Recycle Bin" subtitle="Restore inactive masters. Posted financial history is never placed here."/><AccountCard>{rows.length ? <div className="account-list">{rows.map((x) => <div key={x.id}><span><strong>{x.displayName}</strong><small>{x.entityType} · archived {formatIndiaDate(x.archivedAt)} · {x.reason}</small></span><form action={restoreMasterAction}><input type="hidden" name="recycleId" value={x.id}/><button>Restore</button></form></div>)}</div> : <AccountEmptyState title="Recycle Bin is empty" detail="Safely archived masters will appear here."/>}</AccountCard></>; }
