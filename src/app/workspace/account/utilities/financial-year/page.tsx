import { closeYearAction } from "@/app/actions/account-finalization"; import { AccountCard, AccountPageHeader } from "@/components/account/account-shell"; import { requirePermission } from "@/lib/auth/authorization"; import { db } from "@/lib/db";
export default async function Page() { const actor = await requirePermission("ACCOUNT_PERIOD_LOCK"), years = await db.financialYear.findMany({ where: { companyId: actor.companyId! }, orderBy: { startDate: "desc" } }); return <><AccountPageHeader title="Financial Years" subtitle="Close only after verification. Closing preserves history and locks posting."/><div className="account-list-cards">{years.map((x) => <AccountCard key={x.id}><h2>{x.name} <i>{x.status}</i></h2><p>{x.startDate.toISOString().slice(0,10)} – {x.endDate.toISOString().slice(0,10)}</p>{x.status === "OPEN" && actor.accountRole === "ACCOUNT_ADMIN" && <form action={closeYearAction} className="account-form"><input type="hidden" name="financialYearId" value={x.id}/><label>Early-close reason, if applicable<input name="reason"/></label><button className="account-danger">Verify & close Financial Year</button></form>}</AccountCard>)}</div></>; }
