import{categoryAction}from"@/app/actions/expenses";import{expenseOptions}from"@/lib/account/expenses";import{WorkspacePageHeader}from"@/components/workspace/workspace-page-header";import{accountingOverview}from"@/lib/accounting/service";export default async function Page(){const[o,a]=await Promise.all([expenseOptions(),accountingOverview()]);return <main className="employees-shell"><section className="employees-content"><WorkspacePageHeader title="Expense Categories" backHref="/workspace/account/expenses"/><form action={categoryAction}><input name="name" required/><select name="scope"><option>EXPENSE</option><option>INCOME</option><option>BOTH</option></select><select name="defaultLedgerAccountId">{a.ledgerAccounts.filter(x=>["EXPENSE","INCOME"].includes(x.accountClass)).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button>Create</button></form><ul>{o.categories.map(x=><li key={x.id}>{x.name} ({x.scope}) <form action={categoryAction}><input type="hidden" name="id" value={x.id}/><button name="operation" value="deactivate">Deactivate</button></form></li>)}</ul></section></main>}
