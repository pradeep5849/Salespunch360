import{recurringAction}from"@/app/actions/expenses";import{expenseOptions}from"@/lib/account/expenses";import{WorkspacePageHeader}from"@/components/workspace/workspace-page-header";export default async function Page(){const o=await expenseOptions();return <main className="employees-shell"><section className="employees-content"><WorkspacePageHeader title="Recurring Expenses" backHref="/workspace/account/expenses"/><form action={recurringAction} className="stack"><select name="branchId">{o.branches.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="categoryId">{o.categories.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="projectId"><option value="">Office</option>{o.projects.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="frequency"><option>WEEKLY</option><option>MONTHLY</option><option>QUARTERLY</option><option>YEARLY</option></select><input type="date" name="nextDueDate" required/><input name="amount" required/><button>Create template</button></form><p>Due templates are generated explicitly and always create a draft; occurrence keys prevent duplicates.</p></section></main>}
