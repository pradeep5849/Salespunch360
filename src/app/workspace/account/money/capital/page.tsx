import{ownerTransactionAction,setupOwnerAction}from"@/app/actions/money";import{listMoneyData}from"@/lib/account/money";import{WorkspacePageHeader}from"@/components/workspace/workspace-page-header";export default async function Page(){const d=await listMoneyData();return <main className="employees-shell"><section className="employees-content"><WorkspacePageHeader title="Owner Capital, Drawings & Loans" backHref="/workspace/account/money"/><form action={setupOwnerAction}><select name="userId">{d.users.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="branchId">{d.branches.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button>Set up separate owner ledgers</button></form><form action={ownerTransactionAction} className="stack"><select name="ownerFinancialAccountId">{d.owners.map(x=><option key={x.id} value={x.id}>{x.userId}</option>)}</select><select name="moneyAccountId">{d.dashboard.accounts.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="branchId">{d.branches.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="type"><option>INVESTMENT</option><option>WITHDRAWAL</option><option>OWNER_LOAN</option></select><input name="amount" required/><input type="date" name="transactionDate" required/><input name="reference"/><input type="hidden" name="idempotencyKey" value={crypto.randomUUID()}/><button>Post owner transaction</button></form></section></main>}
