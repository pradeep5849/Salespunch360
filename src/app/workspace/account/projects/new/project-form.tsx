"use client";
import { useState } from "react";
import { createProjectAction } from "@/app/actions/projects";
type Branch={id:string;name:string};type Customer={id:string;name:string};type Manager={id:string;name:string;branchAccessScope:string;branchAccesses:{branchId:string}[]};
export function ProjectForm({branches,customers,managers,accountAdmin}:{branches:Branch[];customers:Customer[];managers:Manager[];accountAdmin:boolean}){const[branchId,setBranchId]=useState(branches[0]?.id??"");const validManagers=managers.filter(manager=>manager.branchAccessScope==="ALL_BRANCHES"||manager.branchAccesses.some(access=>access.branchId===branchId));return <form action={createProjectAction} className="form-grid"><label>Project name<input name="name" required/></label><label>Branch<select name="branchId" value={branchId} onChange={event=>setBranchId(event.target.value)} required>{branches.map(branch=><option key={branch.id} value={branch.id}>{branch.name}</option>)}</select></label><label>Customer<select name="customerId" required>{customers.map(customer=><option key={customer.id} value={customer.id}>{customer.name}</option>)}</select></label>{accountAdmin&&<label>Project manager<select name="projectManagerId"><option value="">Unassigned</option>{validManagers.map(manager=><option key={manager.id} value={manager.id}>{manager.name}</option>)}</select></label>}<label>Project value<input name="projectValue" inputMode="decimal" defaultValue="0"/></label><label>Site name<input name="siteName"/></label><label>Site address<textarea name="siteAddress"/></label><label>Site contact<input name="siteContactName"/></label><label>Site phone<input name="siteContactPhone"/></label><label>Start date<input type="date" name="startDate"/></label><label>Target end<input type="date" name="targetEndDate"/></label><button type="submit">Create project</button></form>}
