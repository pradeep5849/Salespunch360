import Link from"next/link";import{listProjects}from"@/lib/account/projects";import{WorkspacePageHeader}from"@/components/workspace/workspace-page-header";
const statusLabel=(status:string)=>status==="ON_HOLD"?"Hold":["COMPLETED","CLOSED","CANCELLED"].includes(status)?"Completed":"Active";
export const metadata={title:"Projects"};export default async function Page({searchParams}:{searchParams:Promise<{page?:string;pageSize?:string}>}){const result=await listProjects(await searchParams),rows=result.rows;return <main className="employees-shell"><section className="employees-content"><WorkspacePageHeader title="Projects" backHref="/workspace/account"/><p><Link href="/workspace/account/projects/new">Create project</Link></p><div className="table-wrap"><table><thead><tr><th>Number</th><th>Project</th><th>Customer</th><th>Branch</th><th>Manager</th><th>Status</th><th>Value</th><th>Start date</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><Link href={`/workspace/account/projects/${p.id}`}>{p.projectNumber}</Link></td><td>{p.name}</td><td>{p.customer.name}</td><td>{p.branch.name}</td><td>{p.projectManager?.name??"—"}</td><td>{statusLabel(p.status)}</td><td>{p.projectValue.toString()}</td><td>{p.startDate?.toLocaleDateString()??"—"}</td></tr>)}</tbody></table></div><nav aria-label="Project pages"><span>Page {result.page} of {result.totalPages} · {result.total} projects</span>{result.page>1&&<Link href={`?page=${result.page-1}&pageSize=${result.pageSize}`}>Previous</Link>}{result.page<result.totalPages&&<Link href={`?page=${result.page+1}&pageSize=${result.pageSize}`}>Next</Link>}</nav></section></main>}
