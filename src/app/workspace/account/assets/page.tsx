import Link from"next/link";import{requirePermission}from"@/lib/auth/authorization";import{requireAccountModules}from"@/lib/account/modules";import{db}from"@/lib/db";export default async function Page(){const a=await requirePermission("ACCOUNT_ACCOUNTS");await requireAccountModules(a,"ASSETS");const rows=await db.asset.findMany({where:{companyId:a.companyId,...(a.branchAccessScope==="SELECTED_BRANCHES"?{branchId:{in:a.branchIds}}:{})},take:200,orderBy:{createdAt:"desc"}});return <main className="employees-shell"><section className="employees-content"><h1>Assets</h1><Link href="/workspace/account/assets/new">New asset</Link>{rows.map(x=><article key={x.id}><Link href={`/workspace/account/assets/${x.id}`}>{x.assetNumber} · {x.name}</Link><p>{x.assetType} · {x.status} · ₹{x.purchaseValue.toFixed(2)}</p></article>)}</section></main>}
