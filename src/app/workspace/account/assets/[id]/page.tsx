import Link from"next/link";import{notFound}from"next/navigation";import{requirePermission}from"@/lib/auth/authorization";import{requireAccountModules}from"@/lib/account/modules";import{db}from"@/lib/db";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,a=await requirePermission("ACCOUNT_ACCOUNTS");await requireAccountModules(a,"ASSETS");const x=await db.asset.findFirst({where:{id,companyId:a.companyId!,...(a.branchAccessScope==="SELECTED_BRANCHES"?{branchId:{in:a.branchIds}}:{})}});if(!x)notFound();return <main className="employees-shell"><section className="employees-content"><h1>{x.assetNumber} · {x.name}</h1><p>{x.assetType} · {x.status} · ₹{x.purchaseValue.toFixed(2)}</p><p>Depreciation: {x.depreciationMethod} · Useful life {x.usefulLifeMonths??"—"} months</p><Link href={`/workspace/account/assets/${id}/edit`}>Edit</Link></section></main>}
