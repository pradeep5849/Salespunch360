import Link from"next/link";import{notFound}from"next/navigation";import{assignAssetAction,assetStatusAction,returnAssetAction}from"@/app/actions/assets";import{assetOptions,getAsset}from"@/lib/account/assets";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const[{asset,history},options]=await Promise.all([getAsset(id),assetOptions()]).catch(()=>notFound()),eligibleUsers=options.users.filter(x=>x.branchAccessScope==="ALL_BRANCHES"||x.branchAccesses.some(b=>b.branchId===asset.branchId)),users=new Map(options.users.map(x=>[x.id,x.name]));return <main className="employees-shell"><section className="employees-content"><h1>{asset.assetNumber} · {asset.name}</h1><p>{asset.assetType} · {asset.status} · ₹{asset.purchaseValue.toFixed(2)}</p><p>Assigned to: {asset.assignedUserId?users.get(asset.assignedUserId):"Unassigned"}</p><Link href={`/workspace/account/assets/${id}/edit`}>Edit asset</Link>{asset.assignedUserId?<form action={returnAssetAction} className="stack"><input type="hidden" name="id" value={id}/><textarea name="notes" placeholder="Return notes"/><button>Return asset</button></form>:<form action={assignAssetAction} className="stack"><input type="hidden" name="id" value={id}/><select name="userId" required><option value="">Assign to</option>{eligibleUsers.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><textarea name="notes"/><button>Assign asset</button></form>} {!asset.assignedUserId&&<form action={assetStatusAction}><input type="hidden" name="id" value={id}/><select name="status" defaultValue={asset.status}><option>ACTIVE</option><option>UNDER_MAINTENANCE</option><option>RETIRED</option><option>DISPOSED</option></select><button>Update status</button></form>}<h2>Assignment history</h2>{history.map(x=><p key={x.id}>{users.get(x.assignedToId)} · {x.assignedAt.toISOString()} — {x.returnedAt?.toISOString()??"Current"}</p>)}</section></main>}
