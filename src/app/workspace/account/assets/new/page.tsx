import{requirePermission}from"@/lib/auth/authorization";import{requireAccountModules}from"@/lib/account/modules";export default async function Page(){const a=await requirePermission("ACCOUNT_ACCOUNTS");await requireAccountModules(a,"ASSETS");return <main className="employees-shell"><section className="employees-content"><h1>New asset</h1><p>Register vehicles, equipment, furniture, computers, tools, and other long-term assets with branch and depreciation foundations.</p></section></main>}
