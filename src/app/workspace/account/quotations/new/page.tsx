import { saveQuotation } from "@/app/actions/quotations";import { requireAccountModules } from "@/lib/account/modules";import { requirePermission } from "@/lib/auth/authorization";import { quotationEditorOptions } from "@/lib/quotations/service";import { QuotationEditor } from "../quotation-editor";
export default async function Page(){const actor=await requirePermission("ACCOUNT_QUOTATION_VIEW");await requireAccountModules(actor,"QUOTATIONS_BOQ");const options=await quotationEditorOptions();return <main><h1>New quotation document</h1><QuotationEditor action={saveQuotation} options={options}/></main>}
