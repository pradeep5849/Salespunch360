import{commercialEditorOptions}from"@/lib/account/commercial";import{requirePermission}from"@/lib/auth/authorization";import{enabledModulesForCompany}from"@/lib/account/modules";import{db}from"@/lib/db";import{MoneyForm}from"./money-form";
const policies={CUSTOMER_RECEIPT:["SALES","CUSTOMER_RECEIPTS"],CUSTOMER_ADVANCE:["SALES","CUSTOMER_ADVANCES"],VENDOR_PAYMENT:["PURCHASES","VENDOR_PAYMENTS"],VENDOR_ADVANCE:["PURCHASES","VENDOR_ADVANCES"]} as const;
export default async function Page(){const a=await requirePermission("ACCOUNT_SETTLEMENT_ENTRY");if(a.accountRole!=="ACCOUNT_ADMIN"&&a.accountRole!=="ACCOUNTANT")throw new Error("Not authorized");const [o,enabled]=await Promise.all([commercialEditorOptions(),enabledModulesForCompany(a.companyId!)]),allowedTypes=Object.entries(policies).filter(([,required])=>required.every(x=>enabled.includes(x))).map(([type])=>type),branchWhere=a.branchAccessScope==="SELECTED_BRANCHES"?{branchId:{in:a.branchIds??[]}}:{},moneyAccounts=await db.moneyAccount.findMany({where:{companyId:a.companyId!,isActive:true},select:{id:true,name:true}}),documents=await db.commercialDocument.findMany({where:{companyId:a.companyId!,...branchWhere,status:"POSTED",type:{in:["SALES_INVOICE","PURCHASE_BILL"]}},select:{id:true,documentNumber:true,partyId:true,branchId:true,grandTotal:true}});return <main><h1>Receipts, payments and advances</h1>{allowedTypes.length?<MoneyForm allowedTypes={allowedTypes} branches={o.branches} customers={o.customers} vendors={o.vendors} documents={documents} moneyAccounts={moneyAccounts}/>:<p>No money transaction module is enabled for this Company.</p>}</main>}
