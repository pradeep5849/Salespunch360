import { AccountShell } from "@/components/account/account-shell";
import { requireAccountWorkspace } from "@/lib/auth/authorization";
import { db } from "@/lib/db";
import { enabledModulesForCompany } from "@/lib/account/modules";
import { buildAccountNavigation } from "@/lib/account/navigation";
import { resolveAccountBranchContext } from "@/lib/account/branch-context";

export default async function Layout({ children }: { children: React.ReactNode }) { const actor = await requireAccountWorkspace(); const [company, modules, branch] = await Promise.all([db.company.findUniqueOrThrow({ where: { id: actor.companyId }, select: { name: true, productEdition: true, logoObjectKey:true, updatedAt:true, addressLine1:true,addressLine2:true,city:true,state:true,postalCode:true } }), enabledModulesForCompany(actor.companyId),resolveAccountBranchContext(actor)]);const filter=branch.context.mode==="BRANCH"?{branchId:branch.context.branchId}:{};const pendingExpenses=await db.expenseTransaction.count({where:{companyId:actor.companyId,status:"PENDING_APPROVAL",...filter}}); const navigation = buildAccountNavigation(actor, company.productEdition, modules); const companyAddress=[company.addressLine1,company.addressLine2,company.city,company.state,company.postalCode].filter(Boolean).join(", "); return <AccountShell companyName={company.name} companyAddress={companyAddress} hasCompanyLogo={Boolean(company.logoObjectKey)} companyLogoVersion={company.updatedAt.getTime()} userName={actor.name} role={actor.accountRole ?? "ACCOUNT"} isPlusPrimaryAdmin={company.productEdition==="SALESPUNCH360_PLUS"&&actor.salesRole==="PRIMARY_ADMIN"&&actor.salesAccessActive} notifications={pendingExpenses} navigation={navigation} showItems={modules.includes("INVENTORY")} showProjects={modules.includes("PROJECTS")}>{children}</AccountShell>; }
