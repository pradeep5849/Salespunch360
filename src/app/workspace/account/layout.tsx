import { AccountShell } from "@/components/account/account-shell";
import { requireAccountWorkspace } from "@/lib/auth/authorization";
import { db } from "@/lib/db";
import { enabledModulesForCompany } from "@/lib/account/modules";
import { buildAccountNavigation } from "@/lib/account/navigation";

export default async function Layout({ children }: { children: React.ReactNode }) { const actor = await requireAccountWorkspace(); const [company, modules, branchCount, pendingExpenses] = await Promise.all([db.company.findUniqueOrThrow({ where: { id: actor.companyId }, select: { name: true, productEdition: true } }), enabledModulesForCompany(actor.companyId), db.branch.count({ where: { companyId: actor.companyId, isActive: true, ...(actor.branchAccessScope === "SELECTED_BRANCHES" ? { id: { in: actor.branchIds ?? [] } } : {}) } }), db.expenseTransaction.count({ where: { companyId: actor.companyId, status: "PENDING_APPROVAL", ...(actor.branchAccessScope === "SELECTED_BRANCHES" ? { branchId: { in: actor.branchIds ?? [] } } : {}) } })]); const navigation = buildAccountNavigation(actor, company.productEdition, modules), context = actor.branchAccessScope === "ALL_BRANCHES" ? "All Authorized Branches" : `${branchCount} authorized ${branchCount === 1 ? "Branch" : "Branches"}`; return <AccountShell companyName={company.name} branchContext={context} userName={actor.name} role={actor.accountRole ?? "ACCOUNT"} notifications={pendingExpenses} navigation={navigation} showItems={modules.includes("INVENTORY")} showProjects={modules.includes("PROJECTS")}>{children}</AccountShell>; }
