import {WorkspacePageHeader} from "@/components/workspace/workspace-page-header";import Link from"next/link";import{cancelFollowUpTaskAction}from"@/app/actions/follow-up-tasks";import{listFollowUpTasks,taskActor,taskEmployeeOptions}from"@/lib/follow-up-tasks/service";
const states=["PENDING","OVERDUE","COMPLETED","CANCELLED"] as const;const label=(s:string)=>s[0]+s.slice(1).toLowerCase();const fmt=(d:Date)=>d.toLocaleString("en-IN",{timeZone:"Asia/Kolkata",dateStyle:"medium",timeStyle:"short"});
export default async function FollowUpTasksPage({searchParams}:{searchParams:Promise<{status?:string;employeeId?:string}>}){const q=await searchParams,[actor,tasks,employees]=await Promise.all([taskActor(),listFollowUpTasks(q),taskEmployeeOptions()]);const selected=states.includes(q.status as typeof states[number])?q.status!:q.status==="TODAY"?"PENDING":"PENDING";return <main className="leads-shell"><section className="leads-content"><WorkspacePageHeader title="Follow-up Tasks" backHref="/workspace"/><nav className="task-tabs">{states.map(s=><Link className={selected===s?"active":""} key={s} href={`/workspace/follow-up-tasks?status=${s}`}>{label(s)}</Link>)}</nav>{actor.role!=="SALES"&&<form className="lead-filters"><input type="hidden" name="status" value={selected}/><select name="employeeId" defaultValue={q.employeeId||""}><option value="">All permitted employees</option>{employees.map(e=><option key={e.id} value={e.id}>{e.name}</option>)}</select><button>Filter</button></form>}<div className="task-list">{tasks.length===0&&<p className="pending-empty">No tasks in this view.</p>}{tasks.map(t=>{const own=t.assignedUserId===actor.id,last=t.status==="COMPLETED"?"Checkout completed":t.status==="CANCELLED"?"Cancelled":t.completedVisitId?"Follow-up check-in started":"No action yet";return <article key={t.id}><div className="card-title"><h2>{t.lead.customer?.name||t.lead.companyName||t.lead.title}</h2><b>{t.status}</b></div><p><strong>Lead:</strong> {t.lead.title} · <strong>Assigned:</strong> {t.assignedUser.name}</p><p><strong>Due:</strong> {t.dueDate.toISOString().slice(0,10)} · <strong>Last action:</strong> {last}</p>{t.notes&&<p>{t.notes}</p>}<small>Task created by {t.createdByUser.name} · {fmt(t.createdAt)}</small><div className="task-activity"><span>Task created by {t.createdByUser.name}</span><span>→ Assigned to {t.assignedUser.name}</span>{t.completedVisit&&<><span>→ Follow-up check-in done by {t.completedVisit.user.name} · {fmt(t.completedVisit.checkedInAt)}</span>{t.completedVisit.checkedOutAt&&<span>→ Checkout completed · {fmt(t.completedVisit.checkedOutAt)}</span>}</>}{t.completedAt&&<span>→ Task completed · {fmt(t.completedAt)}</span>}{t.status==="CANCELLED"&&<span>→ Cancelled</span>}</div><div className="task-actions"><Link href={`/workspace/leads/${t.leadId}`}>View Lead</Link>{t.completedVisitId&&<Link href={`/workspace/leads/${t.leadId}#visit-${t.completedVisitId}`}>View Visit</Link>}{own&&t.status==="PENDING"&&!t.completedVisitId&&<Link href={`/workspace/check-ins?leadId=${t.leadId}&followUpTaskId=${t.id}`}>Start Follow-up Check-in</Link>}{t.status==="PENDING"&&!t.completedVisitId&&<form action={cancelFollowUpTaskAction}><input type="hidden" name="taskId" value={t.id}/><button>Cancel</button></form>}</div></article>})}</div></section></main>}
