"use client";import{useState}from"react";import{deleteLeadAction}from"@/app/actions/leads";
export function DeleteLead({leadId}:{leadId:string}){const[open,setOpen]=useState(false);return <><button type="button" className="danger-button" onClick={()=>setOpen(true)}>Delete Lead</button>{open&&<div className="confirm-backdrop" role="presentation"><div role="alertdialog" aria-modal="true" aria-labelledby={`delete-${leadId}`}><h2 id={`delete-${leadId}`}>Delete this Lead?</h2><p>This will permanently delete this Lead and all check-ins, visit history and stored photos related to this Lead.</p><strong>This action cannot be undone.</strong><form action={deleteLeadAction}><input type="hidden" name="leadId" value={leadId}/><button autoFocus type="button" onClick={()=>setOpen(false)}>Cancel</button><button className="danger-button">Delete Permanently</button></form></div></div>}</>}
