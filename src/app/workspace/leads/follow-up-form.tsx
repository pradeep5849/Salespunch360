"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export function FollowUpForm({leadId,assignee}:{leadId:string;assignee:string}){const router=useRouter(),[pending,setPending]=useState(false),[error,setError]=useState(""),[success,setSuccess]=useState("");async function submit(form:FormData){setPending(true);setError("");setSuccess("");try{const res=await fetch("/api/workspace/follow-ups",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({leadId,dueDate:form.get("dueDate"),notes:form.get("notes")})}),data=await res.json();if(!res.ok||!data.ok)throw new Error();setSuccess("Follow-up added.");router.refresh()}catch{setError("Unable to add this follow-up. Check the date and your access.")}finally{setPending(false)}}return <form id="add-follow-up" action={submit} className="lead-form follow-up-form"><input type="hidden" name="leadId" value={leadId}/><h2>+ Add Follow-up</h2><p>Assigned employee: <strong>{assignee}</strong></p><label>Due date<input type="date" name="dueDate" required/></label><label>Notes (optional)<textarea name="notes" maxLength={2000}/></label><button disabled={pending}>Add Follow-up</button>{error&&<p className="form-error">{error}</p>}{success&&<p className="form-success">{success}</p>}</form>}