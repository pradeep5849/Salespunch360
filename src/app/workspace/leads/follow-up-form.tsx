"use client";
import{useActionState}from"react";import{addFollowUpTask}from"@/app/actions/follow-up-tasks";
export function FollowUpForm({leadId,assignee}:{leadId:string;assignee:string}){const[state,action,pending]=useActionState(addFollowUpTask,{});return <form id="add-follow-up" action={action} className="lead-form follow-up-form"><input type="hidden" name="leadId" value={leadId}/><h2>+ Add Follow-up</h2><p>Assigned employee: <strong>{assignee}</strong></p><label>Due date<input type="date" name="dueDate" required/></label><label>Notes (optional)<textarea name="notes" maxLength={2000}/></label><button disabled={pending}>Add Follow-up</button>{state.error&&<p className="form-error">{state.error}</p>}{state.success&&<p className="form-success">{state.success}</p>}</form>}
