import Link from "next/link";import {LeadStage}from "@prisma/client";import {requireRole}from "@/lib/auth/authorization";import {leadOptions,listLeads}from "@/lib/leads/service";import {LeadForm}from "./lead-form";import {StageForm}from "./stage-form";
export const metadata={title:"Leads pipeline"};export default async function LeadsPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const user=await requireRole("COMPANY_ADMIN","MANAGER","SALES");const q=await searchParams;const filters={stage:LeadStage[q.stage as keyof typeof LeadStage],assignedUserId:q.assignedUserId||undefined,followUp:q.followUp as "overdue"|"today"|"upcoming"|undefined,q:q.q||undefined};const[leads,o]=await Promise.all([listLeads(filters),leadOptions()]);return <main className="leads-shell"><header className="employees-header"><Link href="/workspace">← Workspace</Link><div className="logo"><span>SP</span> SalesPunch360</div></header><section className="leads-content"><p className="eyebrow">Tenant-secure sales pipeline</p><h1>Leads</h1>{(user.role!=="SALES"||q.visitId)&&<LeadForm users={o.users} customers={o.customers} visitId={q.visitId}/>}<form className="lead-filters"><input name="q" defaultValue={q.q} placeholder="Search lead, customer, prospect"/><select name="stage" defaultValue={q.stage||""}><option value="">All stages</option>{Object.values(LeadStage).map(x=><option key={x}>{x}</option>)}</select>{user.role!=="SALES"&&<select name="assignedUserId" defaultValue={q.assignedUserId||""}><option value="">All permitted assignees</option>{o.users.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select>}<select name="followUp" defaultValue={q.followUp||""}><option value="">Any follow-up</option><option value="overdue">Overdue</option><option value="today">Due today</option><option value="upcoming">Upcoming</option></select><button>Filter</button></form><div className="pipeline">{Object.values(LeadStage).map(stage=><section key={stage}><h2>{stage.replace("_"," ")}</h2>{leads.filter(l=>l.stage===stage).map(l=><article key={l.id}><Link href={`/workspace/leads/${l.id}`}><strong>{l.title}</strong></Link><span>{l.customer?.name||l.companyName||"Prospect"}</span><span>{l.assignedUser.name} · {l.source}</span>{l.estimatedValue&&<b>{l.currencyCode} {l.estimatedValue.toFixed(2)}</b>}<span>{l.followUpAt?`Follow up ${l.followUpAt.toLocaleString()}`:"No follow-up"}</span><small>Updated {l.updatedAt.toLocaleDateString()}</small><StageForm id={l.id} version={l.version} terminal={stage==="WON"||stage==="LOST"} admin={user.role==="COMPANY_ADMIN"}/></article>)}</section>)}</div></section></main>}
