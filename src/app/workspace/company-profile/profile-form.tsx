"use client";
import { useActionState } from "react";
import { saveCompanyProfile } from "@/app/actions/company-profile";
type Profile=Record<string,string|null>;
const fields:[string,string,boolean][]=[["name","Company / business name",true],["addressLine1","Address line 1",true],["addressLine2","Address line 2",false],["locality","Locality",false],["city","City",true],["state","State",true],["postalCode","PIN / postal code",true],["country","Country",true],["primaryContactName","Primary contact name",true],["primaryPhone","Primary phone",true],["contactEmail","Company contact email",true],["alternatePhone","Alternate phone",false],["website","Website",false],["gstin","GSTIN",false],["pan","PAN",false],["registrationNumber","Registration number",false]];
export function ProfileForm({profile}:{profile:Profile}){const[state,action,pending]=useActionState(saveCompanyProfile,{});return <form action={action} className="profile-form"><div className="field-grid">{fields.map(([name,label,required])=><label className="field" key={name}>{label}<input name={name} defaultValue={profile[name]??""} required={required} type={name.includes("Email")||name==="contactEmail"?"email":name==="website"?"url":"text"}/>{state.fieldErrors?.[name]&&<span className="field-error">{state.fieldErrors[name][0]}</span>}</label>)}<label className="field full">Company description<textarea name="description" defaultValue={profile.description??""} maxLength={2000}/></label></div>{state.error&&<p className="form-error">{state.error}</p>}{state.success&&<p className="form-success">{state.success}</p>}<button className="primary-button" disabled={pending}>{pending?"Saving…":"Save company profile"}</button></form>}
