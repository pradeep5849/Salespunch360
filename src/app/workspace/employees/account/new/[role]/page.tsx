import type { AccountRole } from "@prisma/client";
import Link from "next/link";
import { manageAccountUserForm } from "@/app/actions/product-users";
import { getProductUserManagementContext } from "@/lib/users/product-user-management";
import { accountRoleLabel } from "../../../directory-policy";
const roles:Record<string,AccountRole>={"account-admin":"ACCOUNT_ADMIN",accountant:"ACCOUNTANT","project-manager":"PROJECT_MANAGER","data-entry":"DATA_ENTRY"};
export default async function Page({params}:{params:Promise<{role:string}>}) {const {role:key}=await params,role=roles[key],ctx=await getProductUserManagementContext();if(!role||!ctx.canManageAccountUsers||ctx.accountUsage[role]>=ctx.accountLimits[role])throw new Error("No Account seat available");return <main className="employees-shell"><section className="employees-content"><h1>Add {accountRoleLabel(role)}</h1><form action={manageAccountUserForm} className="employee-form"><input type="hidden" name="returnTo" value="/workspace/employees?domain=account"/><input type="hidden" name="operation" value="create-account"/><input type="hidden" name="accountRole" value={role}/><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Branch scope<select name="branchAccessScope" defaultValue="ALL_BRANCHES"><option value="ALL_BRANCHES">All Branches</option><option value="SELECTED_BRANCHES">Selected Branches</option></select></label><fieldset><legend>Selected Branches</legend>{ctx.branches.map(branch=><label key={branch.id}><input type="checkbox" name="branchIds" value={branch.id}/>{branch.name} ({branch.code})</label>)}</fieldset><label>Password<input name="password" type="password" minLength={12} required/></label><label>Confirm password<input name="confirmPassword" type="password" minLength={12} required/></label><button>Save</button> <Link href="/workspace/employees?domain=account">Cancel</Link></form></section></main>}
