import Link from"next/link";import{manageAccountUserForm}from"@/app/actions/product-users";import{getProductUserManagementContext}from"@/lib/users/product-user-management";import{accountRoleLabel}from"../../../directory-policy";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,ctx=await getProductUserManagementContext(),user=ctx.users.find(x=>x.id===id&&x.accountRole);if(!ctx.canManageAccountUsers||!user?.accountRole)throw new Error("Not authorized");return <main className="employees-shell"><section className="employees-content"><h1>Edit {accountRoleLabel(user.accountRole)}</h1><p>{user.name} · {user.email}</p><form action={manageAccountUserForm} className="employee-form"><input type="hidden" name="returnTo" value="/workspace/employees"/><input type="hidden" name="operation" value="edit-account"/><input type="hidden" name="userId" value={user.id}/><input type="hidden" name="branchAccessScope" value={user.branchAccessScope}/><label>Account role<select name="accountRole" defaultValue={user.accountRole}>{(["ACCOUNT_ADMIN","ACCOUNTANT","PROJECT_MANAGER","DATA_ENTRY"] as const).map(x=><option key={x} value={x}>{accountRoleLabel(x)}</option>)}</select></label><label><input name="accountAccessActive" type="checkbox" defaultChecked={user.accountAccessActive}/>Active Account access</label>{user.branchAccesses.map(x=><input key={x.branchId} type="hidden" name="branchIds" value={x.branchId}/>) }<button>Save</button> <Link href="/workspace/employees?domain=account">Cancel</Link></form></section></main>}
