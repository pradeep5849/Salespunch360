"use client";

import { useActionState, useState } from "react";
import { manageEmployee, type EmployeeActionState } from "@/app/actions/employees";
import {setEmployeeBranches} from '@/app/actions/branches';
import {updateEmployeeTravelSettingsAction} from "@/app/actions/travel";
import type { AccountRole, ManagerType, SalesRole } from "@prisma/client";

type Employee = {
  id: string; name: string; email: string; phone: string | null; employeeCode: string | null; designation:string|null;dateOfJoining:string|null;branchAccessScope:"ALL_BRANCHES"|"SELECTED_BRANCHES";branchAccesses:{branch:{id:string;name:string;code:string;isActive:boolean}}[]; travelAllowanceEnabled:boolean; travelRatePerKm:string|null;
  salesRole: SalesRole; salesAccessActive:boolean; accountRole:AccountRole|null;accountAccessActive:boolean;managerType: ManagerType | null; isActive: boolean; managerId: string | null;
  manager: { id: string; name: string; isActive: boolean } | null;
};

const initialState: EmployeeActionState = {};

function ManagerSelect({ managers, defaultValue = "" }: { managers: Employee[]; defaultValue?: string }) {
  return <select name="managerId" defaultValue={defaultValue}><option value="">No manager</option>{managers.filter((manager) => (manager.isActive && manager.salesAccessActive) || manager.id === defaultValue).map((manager) => <option key={manager.id} value={manager.id}>{manager.name}{manager.isActive && manager.salesAccessActive ? "" : manager.id === defaultValue ? " (unavailable — current)" : " (unavailable)"}</option>)}</select>;
}

export function EmployeeManager({ employees, managers, managersEnabled, canAdd, canManageTravel, branches }: { employees: Employee[]; managers: Employee[]; managersEnabled: boolean; canAdd: boolean; canManageTravel: boolean;branches:{id:string;name:string;code:string}[] }) {
  const [state, action, pending] = useActionState(manageEmployee, initialState);
  const [createRole, setCreateRole] = useState<"MANAGER" | "SALES" | null>(null);
  return (
    <div>
      <div className="employee-actions">{managersEnabled && <button disabled={!canAdd} onClick={() => setCreateRole("MANAGER")}>Add Manager</button>}<button disabled={!canAdd} onClick={() => setCreateRole("SALES")}>Add Sales employee</button></div>
      {state.error && <p className="form-error" role="alert">{state.error}</p>}{state.success && <p className="form-success" role="status">{state.success}</p>}
      {createRole && <form action={action} className="employee-form create-form"><input type="hidden" name="operation" value={createRole === "MANAGER" ? "create-manager" : "create-sales"} /><div className="form-title"><h2>Add {createRole === "MANAGER" ? "Manager" : "Sales employee"}</h2><button type="button" className="close-button" onClick={() => setCreateRole(null)}>Close</button></div><div className="field-grid"><label className="field">Full name<input name="name" required /></label><label className="field">Email<input name="email" type="email" required /></label><label className="field">Phone (optional)<input name="phone" type="tel" /></label><label className="field">Employee code (optional)<input name="employeeCode" /></label><label className="field">Designation (optional)<input name="designation" /></label><label className="field">Date of Joining (optional)<input name="dateOfJoining" type="date" /></label>{createRole === "MANAGER" && <label className="field">Manager Type<select name="managerType" defaultValue="FIELD_MANAGER" required><option value="FIELD_MANAGER">Field Manager</option><option value="MANAGER_ONLY">Manager Only</option></select></label>}{createRole === "SALES" && managersEnabled && <label className="field full">Assigned Manager (optional)<ManagerSelect managers={managers} /></label>}<label className="field">Password<input name="password" type="password" minLength={12} required autoComplete="new-password" /></label><label className="field">Confirm password<input name="confirmPassword" type="password" minLength={12} required autoComplete="new-password" /></label></div><button className="primary-button" disabled={pending}>Create {createRole === "MANAGER" ? "Manager" : "Sales employee"}</button></form>}
      <div className="employee-list">
        {employees.length === 0 && <div className="empty-state"><h2>No employees found</h2><p>Try another filter or add your first team member.</p></div>}
        {employees.map((employee) => <article className={`employee-card ${employee.isActive && employee.salesAccessActive ? "" : "inactive"}`} key={employee.id}>
          <div className="employee-summary"><div className="avatar">{employee.name.charAt(0).toUpperCase()}</div><div className="employee-identity"><div><h2>{employee.name}</h2><span className={`role-chip ${employee.salesRole.toLowerCase()}`}>{employee.salesRole === "MANAGER" ? (employee.managerType === "MANAGER_ONLY" ? "MANAGER ONLY" : "FIELD MANAGER") : employee.salesRole}</span><span className={`state-chip ${employee.isActive && employee.salesAccessActive ? "active" : ""}`}>{!employee.isActive ? "Inactive identity" : employee.salesAccessActive ? "Active" : "Sales access suspended"}</span></div><p>{employee.email}{employee.employeeCode ? ` · ${employee.employeeCode}` : ""}</p><p>{employee.phone || "No phone"}{managersEnabled && (employee.manager ? ` · Manager: ${employee.manager.name}${employee.manager.isActive ? "" : " (inactive)"}` : employee.salesRole === "SALES" ? " · Unassigned" : "")}</p></div></div>
          <details><summary>Manage</summary><div className="manage-grid"><form action={action} className="employee-form"><input type="hidden" name="operation" value="edit" /><input type="hidden" name="employeeId" value={employee.id} /><h3>Edit details</h3><label>Name<input name="name" defaultValue={employee.name} required /></label><label>Email<input name="email" type="email" defaultValue={employee.email} required /></label><label>Phone<input name="phone" defaultValue={employee.phone ?? ""} /></label><label>Employee code<input name="employeeCode" defaultValue={employee.employeeCode ?? ""} /></label><label>Designation<input name="designation" defaultValue={employee.designation??""}/></label><label>Date of Joining<input name="dateOfJoining" type="date" defaultValue={employee.dateOfJoining??""}/></label>{employee.salesRole === "MANAGER" && <label>Manager Type<select name="managerType" defaultValue={employee.managerType ?? "FIELD_MANAGER"} required><option value="FIELD_MANAGER">Field Manager</option><option value="MANAGER_ONLY">Manager Only</option></select></label>}{employee.salesRole === "SALES" && managersEnabled && <label>Manager<ManagerSelect managers={managers} defaultValue={employee.managerId ?? ""} /></label>}<button disabled={pending}>Save changes</button></form><form action={action} className="employee-form"><input type="hidden" name="operation" value="reset-password" /><input type="hidden" name="employeeId" value={employee.id} /><h3>Set new password</h3><label>New password<input name="password" type="password" minLength={12} required autoComplete="new-password" /></label><label>Confirm password<input name="confirmPassword" type="password" minLength={12} required autoComplete="new-password" /></label><button disabled={pending}>Reset password</button></form></div><form action={setEmployeeBranches} className="employee-form"><input type="hidden" name="employeeId" value={employee.id}/><h3>Branch access</h3><label>Scope<select name="branchAccessScope" defaultValue={employee.branchAccessScope}><option value="ALL_BRANCHES">All branches</option><option value="SELECTED_BRANCHES">Selected branches</option></select></label>{branches.map(branch=><label key={branch.id}><input type="checkbox" name="branchIds" value={branch.id} defaultChecked={employee.branchAccesses.some(a=>a.branch.id===branch.id)}/>{branch.name} ({branch.code})</label>)}{employee.branchAccesses.filter(a=>!a.branch.isActive).map(a=><p className="muted" key={a.branch.id}>{a.branch.name} ({a.branch.code}) — inactive/current</p>)}<button>Save branch access</button></form>{canManageTravel && <form action={updateEmployeeTravelSettingsAction} className="employee-form"><input type="hidden" name="employeeId" value={employee.id}/><h3>Travel allowance</h3><label><span>Enabled</span><input name="travelAllowanceEnabled" type="checkbox" defaultChecked={employee.travelAllowanceEnabled}/></label><label>Custom ₹ / km <input name="travelRatePerKm" type="number" min="0" step="0.01" defaultValue={employee.travelRatePerKm??""} placeholder="Use company rate"/></label><button>Save travel settings</button></form>}<form action={action} onSubmit={(event) => { if (employee.isActive && employee.salesAccessActive && !window.confirm(`Deactivate ${employee.name}? Their sessions will be revoked.`)) event.preventDefault(); }}><input type="hidden" name="operation" value={employee.isActive && employee.salesAccessActive ? "deactivate" : "reactivate"} /><input type="hidden" name="employeeId" value={employee.id} /><button className={employee.isActive && employee.salesAccessActive ? "danger-button" : "primary-button"} disabled={pending}>{!employee.isActive ? "Reactivate identity" : employee.salesAccessActive ? (employee.accountRole ? "Suspend Sales access" : "Deactivate employee") : "Restore Sales access"}</button></form></details>
        </article>)}
      </div>
    </div>
  );
}
