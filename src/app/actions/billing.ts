"use server";import {revalidatePath} from 'next/cache';import {redirect} from 'next/navigation';import {changePrice,createAccountPackageOrder,createBillingOrder,manualOverride} from '@/lib/billing/service';import {mutationGuard} from '@/lib/security/request';
export async function createOrderAction(form:FormData){await mutationGuard('billing-order',10);if(form.get('paymentMethod')!=='MANUAL')throw new Error('INVALID_PAYMENT_METHOD');const order=await createBillingOrder({billingPeriod:form.get('billingPeriod'),adminSeats:form.get('adminSeats'),managerSeats:form.get('managerSeats'),salesSeats:form.get('salesSeats'),retainAdminUserIds:form.getAll('retainAdminUserIds').map(String),retainManagerUserIds:form.getAll('retainManagerUserIds').map(String),retainSalesUserIds:form.getAll('retainSalesUserIds').map(String),idempotencyKey:crypto.randomUUID().replaceAll('-','')});revalidatePath('/workspace/billing');redirect(`/workspace/billing/pending?order=${order.id}`)}
export async function createAccountPackageOrderAction(form:FormData){await mutationGuard('account-package-order',10);const quantity=Number(form.get('quantity')??1);await createAccountPackageOrder(quantity);revalidatePath('/workspace/billing')}
export async function changePriceAction(form:FormData){await mutationGuard('pricing-change',10);await changePrice({role:form.get('role'),period:form.get('period'),amount:form.get('amount'),currency:'INR'});revalidatePath('/admin/billing')}
export async function overrideSubscriptionAction(form:FormData){await mutationGuard('billing-override',10);await manualOverride({companyId:form.get('companyId'),status:form.get('status'),billingPeriod:form.get('billingPeriod'),adminSeats:form.get('adminSeats'),managerSeats:form.get('managerSeats'),salesSeats:form.get('salesSeats'),endsAt:new Date(String(form.get('endsAt'))).toISOString(),reason:form.get('reason')});revalidatePath('/admin/billing')}
