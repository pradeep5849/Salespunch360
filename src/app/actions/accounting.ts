"use server";
import { revalidatePath } from "next/cache";
import { createCostCentre, createLedgerAccount, postJournal, postOpeningBalances, reverseJournal, setPeriodLock } from "@/lib/accounting/service";
export async function accountingAction(fd:FormData){const operation=String(fd.get("operation")),raw=Object.fromEntries(fd.entries());if(operation==="account")await createLedgerAccount(raw);else if(operation==="cost-centre")await createCostCentre(raw);else if(operation==="period-lock")await setPeriodLock({...raw,lockedThrough:raw.lockedThrough||null});else if(operation==="reverse")await reverseJournal(raw);else if(operation==="journal"||operation==="opening"){const accountIds=fd.getAll("ledgerAccountId").map(String),debits=fd.getAll("debit").map(String),credits=fd.getAll("credit").map(String);const input={...raw,sourceType:operation==="opening"?"OPENING_BALANCE":"MANUAL_JOURNAL",sourceId:String(raw.sourceId),postingPurpose:"PRIMARY",lines:accountIds.map((ledgerAccountId,i)=>({ledgerAccountId,debit:debits[i]||"0",credit:credits[i]||"0"}))};if(operation==="opening")await postOpeningBalances(input);else await postJournal(input);}else throw new Error("INVALID_OPERATION");revalidatePath("/workspace/account/accounting");}
