'use server';import {revalidatePath} from 'next/cache';import{redirect}from'next/navigation';import {setEmployeeBranchAssignment} from '@/lib/branches/assignment';
export async function setEmployeeBranches(form:FormData){const employeeId=String(form.get('employeeId')),branchAccessScope=String(form.get('branchAccessScope'));await setEmployeeBranchAssignment(branchAccessScope==='ALL_BRANCHES'?{employeeId,branchAccessScope:'ALL_BRANCHES'}:{employeeId,branchAccessScope:'SELECTED_BRANCHES',branchIds:form.getAll('branchIds').map(String)});revalidatePath('/workspace/employees');if(form.get('returnTo')==='/workspace/employees')redirect('/workspace/employees')}
