export const metadata={title:'SalesPunch360 Android App',description:'Access SalesPunch360 for Android or explore products, trial and login options.',alternates:{canonical:'/android'}};
import {redirect} from "next/navigation";import Link from "next/link";import {PageHero} from "@/components/public/marketing-ui";
export default function Page(){const configured=process.env.GOOGLE_PLAY_URL;if(configured){try{const url=new URL(configured);if(url.protocol==='https:'&&url.hostname==='play.google.com')redirect(url.toString())}catch{/* Render the safe permanent page for invalid configuration. */}}return <><PageHero eyebrow="SalesPunch360 for Android" title="Take field workflows wherever work happens." copy="Use the SalesPunch360 Android experience for role-appropriate field operations. This permanent page will take you to our official Google Play listing as soon as it is configured."><Link className="button" href="/register">Start Free Trial</Link><Link className="button secondary" href="/sign-in">Login</Link></PageHero><section className="card-grid"><article className="card"><h2>Explore the platform</h2><p>Understand Sales, Account and Plus before choosing your workspace.</p><Link className="learn" href="/products">Explore Products →</Link></article><article className="card"><h2>Talk to us</h2><p>Tell us about your field team and the workflows you want to manage.</p><Link className="learn" href="/contact">Contact →</Link></article><article className="card"><h2>Already registered?</h2><p>Access your existing Company through the secure login.</p><Link className="learn" href="/sign-in">Login →</Link></article></section></>}
