import Link from "next/link";import {CTA,PageHero} from "@/components/public/marketing-ui";
export default function Page(){return <><PageHero eyebrow="Security" title="Business access designed around responsibility." copy="SalesPunch360 combines tenant isolation, secure sessions, role and branch-aware access, private document handling and financial controls."><Link className="button" href="/contact">Ask a Security Question</Link></PageHero><section className="card-grid">{[["Access control","Workspace, role and branch permissions determine what each user can access."],["Company separation","Tenant-aware application controls keep Company information scoped to the correct organization."],["Secure sessions","Authentication and secure session handling protect access to operational workspaces."],["Private documents","Business files and documents are handled through private application access paths."],["Financial control","Audit history, period locking and accounting controls support responsible financial operations."],["Operational accountability","Structured workflows and reports preserve useful context across field and Account activity."]].map(([t,p])=><article className="card" key={t}><h2>{t}</h2><p>{p}</p></article>)}</section><CTA/></>}
