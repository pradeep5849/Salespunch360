export const metadata={title:'Products',description:'Choose SalesPunch360 Field Sales, Account, or Plus for connected Sales and Accounts.',alternates:{canonical:'/products'}};
import Link from "next/link";import {CTA,PageHero} from "@/components/public/marketing-ui";import {editions} from "@/lib/public-site/content";
export default function Products(){return <><PageHero eyebrow="Products" title="One family. Three clear ways to work." copy="Choose field sales, accounts and business management, or both connected within one Company."><Link className="button" href="/compare">Compare Products</Link><Link className="button secondary" href="/pricing">See Pricing</Link></PageHero><section className="card-grid">{editions.map(e=><article className="card" key={e.key}><p className="eyebrow">{e.tagline}</p><h2>{e.name}</h2><p>{e.summary}</p><h3>Best for</h3><p>{e.key==="SALESPUNCH360"?"Field teams and sales managers who need disciplined daily execution.":e.key==="SALESPUNCH360_ACCOUNT"?"Accounts, project and operations teams managing business transactions.":"Companies that want field activity and financial outcomes connected."}</p><Link className="learn" href={e.href}>Product details →</Link><br/><Link className="learn" href={`/register?product=${e.key}`}>Start trial →</Link></article>)}</section><CTA/></>}
