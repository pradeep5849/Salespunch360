import Link from "next/link";import {CTA,PageHero} from "@/components/public/marketing-ui";import {features} from "@/lib/public-site/content";
export default function Page(){return <><PageHero eyebrow="Feature directory" title="Explore every connected workflow." copy="Start with the business problem you want to solve. Every capability below has a complete journey and a clear product context."><Link className="button" href="/compare">Compare Editions</Link></PageHero>{["Field sales","Business","Accounting"].map(g=><section key={g}><div className="section-heading"><p className="eyebrow">{g}</p><h2>{g} capabilities</h2></div><div className="card-grid">{features.filter(f=>f.group===g).map(f=><article className="card" key={f.slug}><h3>{f.title}</h3><p>{f.problem}</p><Link className="learn" href={`/features/${f.slug}`}>Learn more →</Link></article>)}</div></section>)}<CTA/></>}
