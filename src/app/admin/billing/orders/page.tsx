import Link from 'next/link';import {platformOrders} from '@/lib/billing/service';import {confirmManualPaymentAction} from '@/app/actions/billing';import {confirmTelecallerPaymentAction} from '@/app/actions/telecaller-billing';import {listTelecallerBillingOrdersForAdmin} from '@/lib/billing/telecaller';import {ACCOUNT_PACKAGE_ORDER_PROVIDER} from '@/lib/billing/account-package';import {PLUS_ORDER_PROVIDER} from '@/lib/billing/combined-order';
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
export default async function Page(){const [orders,allLegacy]=await Promise.all([platformOrders(),listTelecallerBillingOrdersForAdmin()]),legacy=allLegacy.filter(o=>o.paymentReference!=='UNIFIED_SALES_ORDER'),pending=orders.filter(o=>o.status==='PENDING'),history=orders.filter(o=>o.status!=='PENDING'),legacyPending=legacy.filter(o=>o.status==='PENDING'),legacyHistory=legacy.filter(o=>o.status!=='PENDING');return <main className="billing-content admin-content"><p className="eyebrow">Platform administration</p><h1>Billing</h1><p className="muted">All Sales-team orders and payment verification are shown together. Telecaller seats now share the normal Sales subscription term.</p><section className="billing-card"><h2>Pending Payments</h2>{pending.length===0&&legacyPending.length===0&&<p className="muted">No pending payments.</p>}{pending.map(o=><article className="company-billing" key={o.id}><div><strong>{o.company.name}</strong><span>{o.provider===ACCOUNT_PACKAGE_ORDER_PROVIDER?`${o.accountPackages||o.adminSeats} Account package${(o.accountPackages||o.adminSeats)===1?'':'s'} · ${o.billingPeriod}`:o.provider===PLUS_ORDER_PROVIDER?`${o.billingPeriod} · ${o.adminSeats} Admin / ${o.managerSeats} Manager / ${o.salesSeats} Sales / ${o.accountPackages} Account package${o.accountPackages===1?'':'s'}`:`${o.billingPeriod} · ${o.adminSeats} Admin / ${o.managerSeats} Manager / ${o.salesSeats} Sales`}</span><span>{money(Number(o.totalAmount))} · Ref {o.id.slice(0,8).toUpperCase()} · {o.createdAt.toLocaleString('en-IN')}</span></div><form action={confirmManualPaymentAction} className="billing-form"><input type="hidden" name="orderId" value={o.id}/><label>Payment reference<input name="reference" minLength={3} maxLength={100} placeholder="Bank / UPI reference" required/></label><button>Approve & Activate Subscription</button></form></article>)}{legacyPending.map(o=><article className="company-billing" key={`tele-${o.id}`}><div><strong>{o.companyName??o.companyId}</strong><span>Legacy Telecaller payment · {o.billingPeriod} · {o.targetSeats} seat{o.targetSeats===1?'':'s'}</span><span>{money(Number(o.totalAmount))} · Ref {o.id.slice(0,8).toUpperCase()} · {o.createdAt.toLocaleString('en-IN')}</span></div><form action={confirmTelecallerPaymentAction} className="billing-form"><input type="hidden" name="orderId" value={o.id}/><label>Payment reference<input name="reference" minLength={3} maxLength={200} placeholder="Bank / UPI reference" required/></label><button>Approve Legacy Telecaller Payment</button></form></article>)}</section><section className="billing-card"><h2>Payment History</h2>{history.length===0&&legacyHistory.length===0&&<p className="muted">No completed or closed orders.</p>}{history.map(o=><article className="history-row" key={o.id}><div><strong>{o.company.name}</strong><span>{o.billingPeriod} · Ref {o.id.slice(0,8).toUpperCase()}</span></div><b>{money(Number(o.totalAmount))} · {o.status}</b></article>)}{legacyHistory.map(o=><article className="history-row" key={`tele-${o.id}`}><div><strong>{o.companyName??o.companyId} · Telecaller</strong><span>{o.billingPeriod} · Ref {o.id.slice(0,8).toUpperCase()}</span></div><b>{money(Number(o.totalAmount))} · {o.status}</b></article>)}</section><Link href="/admin">← Dashboard</Link></main>}
