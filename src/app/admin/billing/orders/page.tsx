import Link from 'next/link';import {platformOrders} from '@/lib/billing/service';import {confirmManualPaymentAction} from '@/app/actions/billing';import {ACCOUNT_PACKAGE_ORDER_PROVIDER} from '@/lib/billing/account-package';
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR'}).format(n);
export default async function Page(){const orders=await platformOrders(),pending=orders.filter(o=>o.status==='PENDING'),history=orders.filter(o=>o.status!=='PENDING');return <main className="billing-content admin-content"><p className="eyebrow">Platform administration</p><h1>Billing</h1><p className="muted">Orders and payment verification. Pricing is managed separately.</p><section className="billing-card"><h2>Pending Payments</h2>{pending.length===0&&<p className="muted">No pending payments.</p>}{pending.map(o=><article className="company-billing" key={o.id}><div><strong>{o.company.name}</strong><span>{o.provider===ACCOUNT_PACKAGE_ORDER_PROVIDER?`${o.adminSeats} Account package${o.adminSeats===1?'':'s'} · YEARLY`:`${o.billingPeriod} · ${o.adminSeats} Admin / ${o.managerSeats} Manager / ${o.salesSeats} Sales`}</span><span>{money(Number(o.totalAmount))} · Ref {o.id.slice(0,8).toUpperCase()} · {o.createdAt.toLocaleString('en-IN')}</span></div><form action={confirmManualPaymentAction} className="billing-form"><input type="hidden" name="orderId" value={o.id}/><label>Payment reference<input name="reference" minLength={3} maxLength={100} placeholder="Bank / UPI reference" required/></label><button>Confirm Payment</button></form></article>)}</section><section className="billing-card"><h2>Payment History</h2>{history.length===0&&<p className="muted">No completed or closed orders.</p>}{history.map(o=><article className="history-row" key={o.id}><div><strong>{o.company.name}</strong><span>{o.billingPeriod} · Ref {o.id.slice(0,8).toUpperCase()}</span></div><b>{money(Number(o.totalAmount))} · {o.status}</b></article>)}</section><Link href="/admin">← Dashboard</Link></main>}