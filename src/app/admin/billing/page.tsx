import Link from 'next/link';import {platformBilling} from '@/lib/billing/service';import {changePriceAction,overrideSubscriptionAction} from '@/app/actions/billing';
export default async function Page(){const r=await platformBilling();return <main className="billing-content admin-content"><p className="eyebrow">Platform administration</p><h1>Billing administration</h1><p className="muted">Every price change creates a new effective version; historical orders retain snapshots.</p><section className="billing-card"><h2>Current independent INR prices</h2><div className="admin-price-grid">{r.prices.map(p=><form action={changePriceAction} key={p.id}><input type="hidden" name="role" value={p.role}/><input type="hidden" name="period" value={p.period}/><label>{p.role} · {p.period}<input name="amount" defaultValue={p.amount.toFixed(2)}/></label><button>Version price</button></form>)}</div></section><section className="billing-card"><h2>Company subscriptions</h2>{r.companies.map(c=><article className="company-billing" key={c.id}><div><strong>{c.name}</strong><span>{c.subscriptionStatus} · Trial end {c.trialEndsAt?.toLocaleDateString()||'—'} · {c._count.billingOrders} orders / {c._count.payments} payments</span><span>Paid: {c.subscriptions[0]?`${c.subscriptions[0].status} through ${c.subscriptions[0].endsAt.toLocaleDateString()} · ${c.subscriptions[0].adminSeats} Admin / ${c.subscriptions[0].managerSeats} Manager / ${c.subscriptions[0].salesSeats} Sales seats`:'None'}</span></div><Link className="danger-link admin-action" href={`/admin/companies/${c.id}`}>Manage Company</Link><details><summary>Manual override</summary><form action={overrideSubscriptionAction} className="billing-form"><input type="hidden" name="companyId" value={c.id}/><select name="status"><option>ACTIVE</option><option>SUSPENDED</option></select><select name="billingPeriod"><option>MONTHLY</option><option>SIX_MONTH</option><option>YEARLY</option></select><input name="adminSeats" type="number" min="0" placeholder="Additional Admin seats" required/><input name="managerSeats" type="number" min="0" placeholder="Manager seats" required/><input name="salesSeats" type="number" min="0" placeholder="Sales seats" required/><input name="endsAt" type="datetime-local" required/><input name="reason" minLength={10} maxLength={500} placeholder="Required operational reason" required/><button>Apply audited override</button></form></details></article>)}</section></main>}
