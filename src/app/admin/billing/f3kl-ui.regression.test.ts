import {readFileSync} from 'node:fs';import {describe,expect,it} from 'vitest';const company=readFileSync(new URL('../companies/[companyId]/page.tsx',import.meta.url),'utf8'),pricing=readFileSync(new URL('../pricing/page.tsx',import.meta.url),'utf8');
describe('F3KL platform billing UI',()=>{it('includes Admin seats in override and subscription summary',()=>{expect(company).toContain('name="adminSeats"');expect(company).toContain('Additional Admin seats');expect(company).toContain('.adminSeats} Admin')});it('keeps dynamic ADMIN/MANAGER/SALES price versioning',()=>{expect(page).toContain('r.prices.map');expect(page).toContain('name="role" value={p.role}');expect(page).toContain('changePriceAction')})});
