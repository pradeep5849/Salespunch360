import {readFileSync} from 'node:fs';import {describe,expect,it} from 'vitest';const company=readFileSync(new URL('../companies/[companyId]/page.tsx',import.meta.url),'utf8'),pricing=readFileSync(new URL('../pricing/page.tsx',import.meta.url),'utf8');
describe('F3KL platform billing UI',()=>{it('includes Admin seats in override and subscription summary',()=>{expect(page).toContain('name="adminSeats"');expect(page).toContain('Additional Admin seats');expect(page).toContain('.adminSeats} Admin')});it('keeps dynamic ADMIN/MANAGER/SALES price versioning',()=>{expect(page).toContain('r.prices.map');expect(page).toContain('name="role" value={p.role}');expect(page).toContain('changePriceAction')})});
