import {platformBilling} from '@/lib/billing/service';import {changePriceAction} from '@/app/actions/billing';
export default async function Page(){const r=await platformBilling();return <main className="billing-content admin-content"><p className="eyebrow">Platform administration</p><h1>Pricing</h1><p className="muted">Every price change creates a new effective version; historical orders retain their original price snapshots.</p><section className="billing-card"><h2>Sales subscription pricing</h2><div className="admin-price-grid">{r.prices.map(p=><form action={changePriceAction} key={p.id}><input type="hidden" name="role" value={p.role}/><input type="hidden" name="period" value={p.period}/><label>{p.role} · {p.period}<input name="amount" defaultValue={p.amount.toFixed(2)}/></label><button>Version price</button></form>)}</div></section></main>}