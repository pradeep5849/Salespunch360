import{db}from"@/lib/db";import{requirePermission}from"@/lib/auth/authorization";import{privateStorage}from"@/lib/storage";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const actor=await requirePermission("ACCOUNT_LEDGER_VIEW"),{id}=await params,version=await db.authorizedSignatureVersion.findFirst({where:{id,companyId:actor.companyId!}});if(!version)return new Response("Not found",{status:404});return new Response(new Uint8Array(await privateStorage().get(version.objectKey)),{headers:{"content-type":"image/webp","cache-control":"private, no-store"}})}
