import { exportAccountData } from "@/lib/account/exports"; export async function GET(request: Request, { params }: { params: Promise<{ type: string }> }) { const { type } = await params, format = new URL(request.url).searchParams.get("format") ?? "csv", out = await exportAccountData(type, format); return new Response(out.bytes, { headers: { "content-type": out.contentType, "content-disposition": `attachment; filename="${type}.${format}"`, "cache-control": "private, no-store" } }); }
