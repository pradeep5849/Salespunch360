import { createAccountBackup } from "@/lib/account/utilities"; export async function GET() { const output = await createAccountBackup(); return new Response(output.bytes, { headers: { "content-type": "application/json; charset=utf-8", "content-disposition": `attachment; filename="${output.fileName}"`, "x-content-sha256": output.checksum, "cache-control": "private, no-store" } }); }
