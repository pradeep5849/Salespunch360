import { importTemplate } from "@/lib/account/exports"; export async function GET(request: Request, { params }: { params: Promise<{ type: string }> }) { const { type } = await params, format = new URL(request.url).searchParams.get("format") ?? "xlsx", out = await importTemplate(type as never, format); return new Response(out.bytes, { headers: { "content-type": out.contentType, "content-disposition": `attachment; filename="${type.toLowerCase()}-import-template.${format}"`, "cache-control": "private, no-store" } }); }
