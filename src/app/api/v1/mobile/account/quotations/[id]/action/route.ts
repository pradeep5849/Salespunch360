import{QuotationStatus}from"@prisma/client";import{z}from"zod";import{authenticateMobileToken}from"@/lib/mobile/auth";import{mobileReviseQuotation,mobileShareQuotation,mobileTransitionQuotation}from"@/lib/mobile/account-quotations";import{mobileAuthorizationFailure,mobileJson,mobileUnauthorized,mobileUnexpected}from"@/lib/mobile/http";const input=z.discriminatedUnion("action",[z.object({action:z.literal("TRANSITION"),to:z.nativeEnum(QuotationStatus),note:z.string().max(5000).optional()}),z.object({action:z.literal("REVISE"),document:z.unknown()}),z.object({action:z.literal("SHARE"),expiresAt:z.string().datetime().optional()})]);export async function POST(r:Request,c:{params:Promise<{id:string}>}){try{const user=await authenticateMobileToken(r.headers.get("authorization")),id=(await c.params).id,d=input.parse(await r.json());return mobileJson(d.action==="TRANSITION"?await mobileTransitionQuotation(user,id,d.to,d.note):d.action==="REVISE"?await mobileReviseQuotation(user,id,d.document):await mobileShareQuotation(user,id,d.expiresAt))}catch(e){return mobileUnauthorized(e)??mobileAuthorizationFailure(e)??(e instanceof z.ZodError?mobileJson({error:"INVALID_INPUT"},400):null)??mobileUnexpected("MOBILE_QUOTATION_ACTION",e)}}
