import{authenticateMobileToken}from'@/lib/mobile/auth';import{mobileCheckIn,mobileCheckout,mobileFieldCheckIn,mobileFieldContext,MobileFieldError}from'@/lib/mobile/field';import{mobileJson}from'@/lib/mobile/http';
const user=(r:Request)=>authenticateMobileToken(r.headers.get('authorization'));function failure(e:unknown){if(e instanceof MobileFieldError)return mobileJson({error:e.code},e.status);const code=e instanceof Error?e.message:'INVALID_INPUT';const status=code==='MOBILE_UNAUTHORIZED'?401:['ATTENDANCE_REQUIRED','CHECKOUT_REQUIRED','REPEAT_VISIT_OUTSIDE_RADIUS','PHOTO_REQUIRED','PHOTO_INVALID','PHOTO_STORAGE_NOT_CONFIGURED','PHOTO_STORAGE_UNAVAILABLE','SUBJECT_OWNERSHIP_CONFLICT'].includes(code)?409:400;return mobileJson({error:code},status)}
export async function GET(r:Request){try{return mobileJson(await mobileFieldContext(await user(r),new URL(r.url).searchParams.get('q')??''))}catch(e){return failure(e)}}
export async function POST(r:Request){try{const principal=await user(r),contentType=r.headers.get('content-type')??'';if(contentType.startsWith('multipart/form-data')){const f=await r.formData();if(f.get('action')!=='CHECK_IN')return mobileJson({error:'INVALID_ACTION'},400);const visitType=String(f.get('visitType')),location={latitude:Number(f.get('latitude')),longitude:Number(f.get('longitude')),accuracyMeters:Number(f.get('accuracyMeters'))},common={visitType,location,visitNotes:String(f.get('visitNotes')||'')},input=visitType==='NEW'?{...common,name:String(f.get('name')),phone:String(f.get('phone')||'')}:visitType==='FOLLOW_UP'?{...common,leadId:String(f.get('leadId'))}:{...common,customerId:String(f.get('customerId'))},photo=f.get('photo');return mobileJson(await mobileFieldCheckIn(principal,input,photo instanceof File&&photo.size?photo:undefined),201)}const body=await r.json();if(body.action==='CHECK_IN'){const input={...body};delete input.action;return mobileJson(await mobileCheckIn(principal,input),201)}if(body.action==='CHECK_OUT'){const input={...body};delete input.action;return mobileJson(await mobileCheckout(principal,input))}return mobileJson({error:'INVALID_ACTION'},400)}catch(e){return failure(e)}}
