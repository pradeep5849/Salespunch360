import {authenticateMobileToken} from '@/lib/mobile/auth';import {mobileUploadPoint} from '@/lib/mobile/attendance';import {mobilePointSchema} from '@/lib/mobile/validation';import {mobileError,mobileJson,mobileUnauthorized} from '@/lib/mobile/http';
export async function POST(request:Request){try{const user=await authenticateMobileToken(request.headers.get('authorization')),parsed=mobilePointSchema.safeParse(await request.json());if(!parsed.success)return mobileError(400);return mobileJson(await mobileUploadPoint(user,parsed.data))}catch(error){return mobileUnauthorized(error)??mobileError(409)}}
