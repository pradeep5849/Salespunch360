import {authenticateMobileSalesToken} from "@/lib/mobile/auth";import {mobileBillingContext,mobileBillingQuote} from "@/lib/mobile/billing";import {MobileCompanyError} from "@/lib/mobile/company";import {mobileBranchFailure,mobileJson,mobileUnauthorized,mobileUnexpected} from "@/lib/mobile/http";
const principal=(r:Request)=>authenticateMobileSalesToken(r.headers.get("authorization"));function failure(e:unknown){if(e instanceof MobileCompanyError)return mobileJson({error:e.code},e.status);return mobileUnauthorized(e)??mobileBranchFailure(e)??mobileUnexpected("MOBILE_BILLING",e)}
export async function GET(r:Request){try{return mobileJson(await mobileBillingContext(await principal(r)))}catch(e){return failure(e)}}export async function POST(r:Request){try{return mobileJson(await mobileBillingQuote(await principal(r),await r.json()))}catch(e){return failure(e)}}