import Link from "next/link";import {CTA,PageHero} from "./marketing-ui";
export function ProductPage({edition,name,eyebrow,copy,sections,flow}:{edition:string,name:string,eyebrow:string,copy:string,sections:{title:string;items:string[]}[];flow:string[]}){return <><PageHero eyebrow={eyebrow} title={name} copy={copy}><Link className="button" href={`/register?product=${edition}`}>Start {name} Trial</Link><Link className="button secondary" href="/pricing">See Pricing</Link></PageHero><section><div className="section-heading"><p className="eyebrow">Capabilities</p><h2>Everything your team needs, in the right workspace.</h2></div><div className="card-grid">{sections.map(s=><article className="card" key={s.title}><h3>{s.title}</h3><ul className="check-list">{s.items.map(i=><li key={i}>{i}</li>)}</ul></article>)}</div></section><section className="full-bleed soft"><div className="inner"><div className="section-heading"><p className="eyebrow">How work flows</p><h2>A clear journey from action to insight.</h2></div><div className="workflow">{flow.map((x,i)=><span key={x}>{i+1}. {x}</span>)}</div></div></section><CTA product={edition}/></>}
