import Link from "next/link";import {CTA,PageHero} from "./marketing-ui";
export function ProductPage({edition,name,eyebrow,copy,sections,flow}:{edition:string;name:string;eyebrow:string;copy:string;sections:{title:string;items:string[]}[];flow:string[]}){const field=edition==="SALESPUNCH360",plus=edition==="SALESPUNCH360_PLUS";return <><PageHero eyebrow={eyebrow} title={name} copy={copy}><Link className="button" href={`/register?product=${edition}`}>Start {name} Trial</Link><Link className="button secondary" href="/pricing">See Pricing</Link><Link href="/compare">Compare products →</Link></PageHero><section><div className="section-heading"><p className="eyebrow">Who it is for</p><h2>{field?"Field employees, sales managers and administrators.":plus?"Businesses connecting field, accounts, project and leadership teams.":"Accounts, operations, inventory and project teams."}</h2><p>{field?"Give mobile field roles a focused daily workflow and authorized managers clear reports.":plus?"Let each person use the Sales role, Account role, or both workspaces that administrators configure.":"Give each Account role a focused workspace for controlled business operations and reporting."}</p></div><div className="card-grid">{sections.map(s=><article className="card" key={s.title}><h3>{s.title}</h3><ul className="check-list">{s.items.map(i=><li key={i}>{i}</li>)}</ul></article>)}</div></section><section className="full-bleed soft"><div className="inner"><div className="section-heading"><p className="eyebrow">How work flows</p><h2>A clear journey from action to insight.</h2></div><div className="workflow">{flow.map((x,i)=><span key={x}>{i+1}. {x}</span>)}</div></div></section><section><div className="section-heading"><p className="eyebrow">Access and visibility</p><h2>Web, Android, branches and reports—controlled by role.</h2></div><div className="card-grid"><article className="card"><h3>Web + Android</h3><p>Use the experience appropriate to the user’s responsibilities and authorized workspace.</p><Link className="learn" href="/apps">Explore app access →</Link></article><article className="card"><h3>Branch-aware work</h3><p>Keep transactions and activity in the right Branch while preserving permitted Company visibility.</p><Link className="learn" href="/features/branches-consolidation">Explore branches →</Link></article><article className="card"><h3>Decision-ready reports</h3><p>Review the field, business and financial reports included for this workspace and role.</p><Link className="learn" href={field?"/features/targets-performance":"/features/financial-reports"}>Explore reports →</Link></article></div></section><CTA product={edition}/></>}
