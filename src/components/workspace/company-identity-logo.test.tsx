import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{CompanyIdentityLogo}from"./company-identity-logo";
describe("CompanyIdentityLogo refresh",()=>{it("versions the private URL and changes it after replacement",()=>{const before=renderToStaticMarkup(<CompanyIdentityLogo name="Acme" hasLogo version={1}/>),after=renderToStaticMarkup(<CompanyIdentityLogo name="Acme" hasLogo version={2}/>);expect(before).toContain("company-logo?v=1");expect(after).toContain("company-logo?v=2");expect(after).not.toBe(before)});it("keeps the initial fallback",()=>expect(renderToStaticMarkup(<CompanyIdentityLogo name="Acme" hasLogo={false}/>)).toContain(">A</b>"))});
