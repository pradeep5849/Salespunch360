import Link from "next/link";
import type { AccountNavGroup } from "@/lib/account/navigation";

export function AccountShell({ children, companyName, branchContext, userName, role, notifications, navigation, showItems, showProjects }: { children: React.ReactNode; companyName: string; branchContext: string; userName: string; role: string; notifications: number; navigation: AccountNavGroup[]; showItems: boolean; showProjects: boolean }) { const bottom = [{ label: "Home", href: "/workspace/account", icon: "⌂" }, { label: "Dashboard", href: "/workspace/account/dashboard", icon: "▦" }, ...(showItems ? [{ label: "Items", href: "/workspace/account/inventory", icon: "□" }] : []), ...(showProjects ? [{ label: "Projects", href: "/workspace/account/projects", icon: "◇" }] : []), { label: "Menu", href: "/workspace/account/menu", icon: "☰" }]; return <div className="account-shell"><header className="account-topbar"><Link href="/workspace/account" className="account-identity"><strong>{companyName}</strong><small>{branchContext}</small></Link><div className="account-top-actions"><Link aria-label={`${notifications} account notifications`} className="account-icon-button" href="/workspace/account/notifications">♢{notifications > 0 && <b>{notifications > 99 ? "99+" : notifications}</b>}</Link><details className="account-profile"><summary aria-label="Open profile menu"><span>{userName.charAt(0).toUpperCase()}</span></summary><div><strong>{userName}</strong><small>{role.replaceAll("_", " ").toLowerCase()}</small><small>{branchContext}</small><Link href="/workspace/change-password">Profile & password</Link><form action="/api/auth/sign-out" method="post"><button>Sign out</button></form></div></details></div></header><div className="account-desktop-nav" aria-label="Account modules">{navigation.map((group) => <Link key={group.label} href={`/workspace/account/menu#${group.label.toLowerCase()}`}>{group.label}</Link>)}</div><main className="account-main">{children}</main><nav className="account-bottom-nav" aria-label="Account navigation">{bottom.map((item) => <Link key={item.label} href={item.href}><span aria-hidden>{item.icon}</span><small>{item.label}</small></Link>)}</nav></div>; }
export function AccountPageHeader({ title, subtitle, action }: { title: string; subtitle?: string; action?: React.ReactNode }) { return <header className="account-page-header"><div><h1>{title}</h1>{subtitle && <p>{subtitle}</p>}</div>{action}</header>; }
export function AccountCard({ children, className = "" }: { children: React.ReactNode; className?: string }) { return <section className={`account-card ${className}`}>{children}</section>; }
export function AccountEmptyState({ title, detail, action }: { title: string; detail: string; action?: React.ReactNode }) { return <div className="account-empty"><strong>{title}</strong><p>{detail}</p>{action}</div>; }
