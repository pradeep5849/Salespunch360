generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

enum Role {
  SUPER_ADMIN
  COMPANY_ADMIN
  FIELD_ADMIN
  MANAGER
  SALES
  ACCOUNT_USER
}

/// Explicit Sales workspace identity. Kept independent from legacy Role during migration.
enum SalesRole {
  PRIMARY_ADMIN
  ADMIN
  MANAGER
  SALES
}

/// Explicit Account workspace identity. One fixed role per user.
enum AccountRole {
  ACCOUNT_ADMIN
  ACCOUNTANT
  PROJECT_MANAGER
  DATA_ENTRY
}

enum ManagerType {
  FIELD_MANAGER
  MANAGER_ONLY
}

enum ProductEdition {
  SALESPUNCH360
  SALESPUNCH360_ACCOUNT
  SALESPUNCH360_PLUS
}

/// Company-selected product capabilities. Account/Plus modules remain reserved
/// until their roadmap gates are released.
enum CompanyModule {
  SALES_CRM
  ATTENDANCE
  GPS_TRACKING
  FOLLOW_UP_TASKS
  TARGETS
  REPORTS
  INVENTORY
  ACCOUNTING
  PROJECTS
}

enum BusinessType {
  INTERIOR_CONSTRUCTION
  RETAIL_TRADING
  SERVICE_BUSINESS
  MANUFACTURING
  OTHER_MIXED
}

enum AccountModule {
  QUOTATIONS_BOQ
  SALES
  SALES_ORDER
  PROFORMA_INVOICE
  DELIVERY_CHALLAN
  CREDIT_NOTE
  CUSTOMER_RECEIPTS
  CUSTOMER_ADVANCES
  PAYMENT_REMINDERS
  PURCHASES
  PURCHASE_ORDER
  PURCHASE_BILLS
  DEBIT_NOTE
  VENDOR_PAYMENTS
  VENDOR_ADVANCES
  SUBCONTRACTORS
  PROJECTS
  PROJECT_COSTING
  INVENTORY
  BARCODE
  EXPENSES
  ASSETS
  GST_ADVANCED
}

enum CommercialDocumentType {
  SALES_ORDER
  PROFORMA_INVOICE
  SALES_INVOICE
  DELIVERY_CHALLAN
  CREDIT_NOTE
  PURCHASE_ORDER
  PURCHASE_BILL
  DEBIT_NOTE
  SUBCONTRACT_PURCHASE
}

enum CommercialDocumentStatus {
  DRAFT
  POSTED
  CANCELLED
}

enum SettlementType {
  CUSTOMER_RECEIPT
  CUSTOMER_ADVANCE
  VENDOR_PAYMENT
  VENDOR_ADVANCE
}

enum CommercialLineType {
  PRODUCT
  SERVICE
  WORK_PACKAGE
  CUSTOM
  MATERIAL
  SUBCONTRACT
}

enum DiscountType {
  PERCENTAGE
  FIXED
}

enum PurchasePurpose {
  OFFICE
  PROJECT
}

enum PurchaseClassification {
  PURCHASE_COST
  GENERAL_EXPENSES
}

enum PaymentMode {
  CASH
  BANK
}

enum SettlementStatus {
  POSTED
}

enum PaymentReminderStatus {
  PENDING
  SENT
  DISMISSED
}

enum CommercialAuditEventType {
  DOCUMENT_CREATED
  INVOICE_POSTED
  CREDIT_NOTE_POSTED
  PURCHASE_BILL_POSTED
  DEBIT_NOTE_POSTED
  CUSTOMER_RECEIPT_POSTED
  CUSTOMER_ADVANCE_POSTED
  CUSTOMER_ADVANCE_APPLIED
  VENDOR_PAYMENT_POSTED
  VENDOR_ADVANCE_POSTED
  VENDOR_ADVANCE_APPLIED
  REMINDER_CREATED
  REMINDER_STATUS_CHANGED
}

enum BranchAccessScope {
  ALL_BRANCHES
  SELECTED_BRANCHES
}

enum PushPlatform {
  ANDROID
}

enum TeamStructure {
  MANAGERS_AND_SALES
  SALES_ONLY
}

enum SubscriptionStatus {
  TRIAL
  ACTIVE
  EXPIRED
  SUSPENDED
}

enum CheckoutSentiment {
  POSITIVE
  NEUTRAL
  NEGATIVE
}

enum VisitType {
  NEW
  FOLLOW_UP
  CUSTOMER
}

enum LeadStage {
  NEW
  QUALIFIED
  PROPOSAL
  NEGOTIATION
  WON
  LOST
}

enum LeadSource {
  MANUAL
  CUSTOMER_VISIT
  REFERRAL
  PHONE
  EMAIL
  WEBSITE
  OTHER
}

enum LeadActivityType {
  CREATED
  UPDATED
  ASSIGNED
  STAGE_CHANGED
  FOLLOW_UP_CHANGED
  WON
  LOST
  REOPENED
}

enum FollowUpTaskStatus {
  PENDING
  COMPLETED
  CANCELLED
}

enum GeofenceEventType {
  OUTSIDE_RADIUS
  INSUFFICIENT_ACCURACY
}

enum GeofenceAction {
  ATTENDANCE_START
  CUSTOMER_CHECK_IN
}

enum TargetMetric {
  LEADS_CREATED_COUNT
  WON_LEADS_COUNT
  WON_LEADS_VALUE
}

enum TargetPeriodType {
  MONTHLY
  QUARTERLY
  CUSTOM
}

enum BillingPeriod {
  MONTHLY
  SIX_MONTH
  YEARLY
}

enum BillingRole {
  ADMIN
  MANAGER
  SALES
}

enum PaidSubscriptionStatus {
  ACTIVE
  EXPIRED
  CANCELLED
  SUSPENDED
}

enum BillingOrderStatus {
  PENDING
  PAID
  FAILED
  CANCELLED
  EXPIRED
}

enum PaymentStatus {
  CREATED
  AUTHORIZED
  CAPTURED
  FAILED
  REFUNDED
}

enum TravelApprovalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum CompensationStatus {
  ACTIVE
  SUPERSEDED
  CANCELLED
}

enum EmployeeFinancialRequestStatus {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
}

enum AccountCategoryScope {
  PRODUCT
  SERVICE
  BOTH
}

enum CustomFieldEntity {
  CUSTOMER
  VENDOR
  PRODUCT
  SERVICE
  WORK_PACKAGE
}

enum CustomFieldDataType {
  TEXT
  TEXTAREA
  NUMBER
  DECIMAL
  DATE
  BOOLEAN
  SELECT
}

enum LedgerAccountClass {
  ASSET
  LIABILITY
  EQUITY
  INCOME
  EXPENSE
}

enum NormalBalanceSide {
  DEBIT
  CREDIT
}

enum JournalStatus {
  DRAFT
  POSTED
  REVERSED
  CANCELLED
}

enum AccountingAuditEventType {
  SYSTEM_ACCOUNTS_INITIALIZED
  JOURNAL_CREATED
  JOURNAL_POSTED
  JOURNAL_REVERSED
  OPENING_BALANCE_POSTED
  PERIOD_LOCKED
  PERIOD_UNLOCKED
}

enum QuotationDocumentType {
  QUOTATION
  ESTIMATE
  BOQ
}

enum QuotationStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  REJECTED
  SENT
  ACCEPTED
  DECLINED
  EXPIRED
  CANCELLED
}

enum QuotationLineType {
  PRODUCT
  SERVICE
  WORK_PACKAGE
  CUSTOM
}

enum QuotationAdjustmentType {
  DISCOUNT
  ADDITIONAL_CHARGE
}

enum QuotationValueType {
  PERCENTAGE
  FIXED
}

enum QuotationAuditEventType {
  CREATED
  REVISION_CREATED
  SUBMITTED_FOR_APPROVAL
  APPROVED
  REJECTED
  SHARED
  SENT
  SHARE_REVOKED
  ACCEPTED
  DECLINED
  EXPIRED
  CANCELLED
}

enum BillingAuditType {
  PRICING_CHANGED
  ORDER_CREATED
  PAYMENT_CAPTURED
  PAYMENT_FAILED
  SUBSCRIPTION_ACTIVATED
  SUBSCRIPTION_RENEWED
  SEATS_CHANGED
  SUPER_ADMIN_OVERRIDE
  SUBSCRIPTION_SUSPENDED
}

model Company {
  id                                  String                     @id @default(uuid()) @db.Uuid
  name                                String
  slug                                String                     @unique
  teamStructure                       TeamStructure              @default(MANAGERS_AND_SALES)
  productEdition                      ProductEdition             @default(SALESPUNCH360)
  enabledModules                      CompanyModule[]            @default([SALES_CRM, ATTENDANCE, GPS_TRACKING, FOLLOW_UP_TASKS, TARGETS, REPORTS])
  trialStartedAt                      DateTime?
  trialEndsAt                         DateTime?
  subscriptionStatus                  SubscriptionStatus         @default(SUSPENDED)
  attendanceEnabled                   Boolean                    @default(true)
  gpsTrackingEnabled                  Boolean                    @default(false)
  checkoutRequiredBeforeNextCheckIn   Boolean                    @default(true)
  attendanceGeofenceEnabled           Boolean                    @default(false)
  attendanceReferenceLatitude         Float?
  attendanceReferenceLongitude        Float?
  attendanceGeofenceRadiusMeters      Int?
  customerCheckInGeofenceEnabled      Boolean                    @default(false)
  customerCheckInGeofenceRadiusMeters Int?
  travelRatePerKm                     Decimal?                   @db.Decimal(10, 2)
  createdAt                           DateTime                   @default(now())
  updatedAt                           DateTime                   @updatedAt
  addressLine1                        String?
  addressLine2                        String?
  locality                            String?
  city                                String?
  state                               String?
  postalCode                          String?
  country                             String?
  primaryContactName                  String?
  primaryPhone                        String?
  contactEmail                        String?
  alternatePhone                      String?
  website                             String?
  gstin                               String?
  pan                                 String?
  registrationNumber                  String?
  description                         String?
  logoObjectKey                       String?                    @db.VarChar(512)
  accountSettings                     AccountSettings?
  financialYears                      FinancialYear[]
  numberingSeries                     NumberingSeries[]
  accountUnits                        AccountUnit[]
  accountCategories                   AccountCategory[]
  accountProducts                     AccountProduct[]
  accountServices                     AccountService[]
  vendors                             Vendor[]
  workCategories                      WorkCategory[]
  workPackages                        WorkPackage[]
  customFieldDefinitions              CustomFieldDefinition[]
  ledgerAccounts                      LedgerAccount[]
  journalEntries                      JournalEntry[]
  journalLines                        JournalLine[]
  costCentres                         CostCentre[]
  accountingPeriodLocks               AccountingPeriodLock[]
  accountingAuditEvents               AccountingAuditEvent[]
  users                               User[]
  branches                            Branch[]
  attendances                         Attendance[]
  locationPoints                      LocationPoint[]
  customers                           Customer[]
  customerVisits                      CustomerVisit[]
  leads                               Lead[]
  leadActivities                      LeadActivity[]
  geofenceEvents                      GeofenceEvent[]
  salesTargets                        SalesTarget[]
  subscriptions                       CompanySubscription[]
  billingOrders                       BillingOrder[]
  payments                            PaymentTransaction[]
  billingAudits                       BillingAuditEvent[]
  visitPhotos                         VisitPhoto[]
  pendingStorageDeletions             PendingStorageDeletion[]
  leadDeletionAudits                  LeadDeletionAudit[]
  followUpTasks                       FollowUpTask[]
  pushDevices                         PushDevice[]
  dailyTravelApprovals                DailyTravelApproval[]
  quotationDocuments                  QuotationDocument[]
  quotationRevisions                  QuotationRevision[]
  quotationLines                      QuotationLine[]
  quotationAdjustments                QuotationAdjustment[]
  quotationPaymentSchedules           QuotationPaymentSchedule[]
  quotationShares                     QuotationShare[]
  quotationAuditEvents                QuotationAuditEvent[]
  commercialDocuments                 CommercialDocument[]
  settlements                         AccountSettlement[]
  paymentReminders                    PaymentReminder[]
  commercialAudits CommercialAuditEvent[]
  settlementAllocations SettlementAllocation[]
  advanceApplications AdvanceApplication[]

  @@index([subscriptionStatus, trialEndsAt])
  @@map("companies")
}

model User {
  id                         String                   @id @default(uuid()) @db.Uuid
  companyId                  String?                  @db.Uuid
  company                    Company?                 @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  name                       String
  email                      String                   @unique
  passwordHash               String
  sessionVersion             Int                      @default(0)
  role                       Role
  salesRole                  SalesRole?
  accountRole                AccountRole?
  salesAccessActive          Boolean                  @default(false)
  accountAccessActive        Boolean                  @default(false)
  managerType                ManagerType?
  branchAccessScope          BranchAccessScope        @default(ALL_BRANCHES)
  phone                      String?
  employeeCode               String?
  designation                String?                  @db.VarChar(120)
  dateOfJoining              DateTime?                @db.Date
  travelAllowanceEnabled     Boolean                  @default(false)
  travelRatePerKm            Decimal?                 @db.Decimal(10, 2)
  isActive                   Boolean                  @default(true)
  managerId                  String?                  @db.Uuid
  manager                    User?                    @relation("ManagerReports", fields: [managerId], references: [id], onDelete: SetNull, onUpdate: Restrict)
  reports                    User[]                   @relation("ManagerReports")
  sessions                   Session[]
  branchAccesses             UserBranchAccess[]
  mobileSessions             MobileSession[]
  pushDevices                PushDevice[]
  attendances                Attendance[]
  locationPoints             LocationPoint[]
  customerVisits             CustomerVisit[]
  assignedCustomers          Customer[]               @relation("CustomerAssignee")
  uploadedVisitPhotos        VisitPhoto[]             @relation("VisitPhotoUploader")
  leadDeletionAudits         LeadDeletionAudit[]      @relation("LeadDeletionActor")
  assignedLeads              Lead[]                   @relation("LeadAssignee")
  createdLeads               Lead[]                   @relation("LeadCreator")
  leadActivities             LeadActivity[]           @relation("LeadActor")
  previousLeadAssignments    LeadActivity[]           @relation("PreviousLeadAssignee")
  newLeadAssignments         LeadActivity[]           @relation("NewLeadAssignee")
  geofenceEvents             GeofenceEvent[]          @relation("GeofenceEmployee")
  assignedTargets            SalesTarget[]            @relation("TargetAssignee")
  createdTargets             SalesTarget[]            @relation("TargetCreator")
  createdPrices              BillingPrice[]           @relation("PriceCreator")
  createdBillingOrders       BillingOrder[]           @relation("OrderCreator")
  billingAuditEvents         BillingAuditEvent[]      @relation("BillingAuditActor")
  createdAt                  DateTime                 @default(now())
  updatedAt                  DateTime                 @updatedAt
  emailVerifiedAt            DateTime?
  emailVerificationTokens    EmailVerificationToken[]
  assignedFollowUpTasks      FollowUpTask[]           @relation("FollowUpTaskAssignee")
  createdFollowUpTasks       FollowUpTask[]           @relation("FollowUpTaskCreator")
  dailyTravelApprovals       DailyTravelApproval[]    @relation("TravelApprovalEmployee")
  reviewedTravelApprovals    DailyTravelApproval[]    @relation("TravelApprovalReviewer")
  createdJournalEntries      JournalEntry[]           @relation("JournalCreator")
  postedJournalEntries       JournalEntry[]           @relation("JournalPoster")
  accountingAuditEvents      AccountingAuditEvent[]   @relation("AccountingAuditActor")
  updatedAccountingLocks     AccountingPeriodLock[]   @relation("AccountingLockUpdater")
  createdQuotationDocuments  QuotationDocument[]      @relation("QuotationCreator")
  acceptedQuotationDocuments QuotationDocument[]      @relation("QuotationAcceptor")
  quotationAuditEvents       QuotationAuditEvent[]    @relation("QuotationAuditActor")
  commercialDocuments        CommercialDocument[]     @relation("CommercialDocumentCreator")
  settlements                AccountSettlement[]      @relation("SettlementCreator")
  commercialAudits CommercialAuditEvent[] @relation("CommercialAuditActor")
  paymentReminders PaymentReminder[] @relation("PaymentReminderActor")
  advanceApplications AdvanceApplication[] @relation("AdvanceApplicationActor")

  @@unique([companyId, id])
  @@unique([companyId, employeeCode])
  @@index([companyId, role])
  @@index([companyId, salesRole])
  @@index([companyId, accountRole])
  @@index([companyId, salesAccessActive, salesRole])
  @@index([companyId, accountAccessActive, accountRole])
  @@index([companyId, isActive, role])
  @@index([managerId])
  @@map("users")
}

model Branch {
  id                  String               @id @default(uuid()) @db.Uuid
  companyId           String               @db.Uuid
  company             Company              @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  name                String
  code                String
  isPrimary           Boolean              @default(false)
  isActive            Boolean              @default(true)
  addressLine1        String?
  addressLine2        String?
  locality            String?
  city                String?
  state               String?
  postalCode          String?
  country             String?
  phone               String?
  email               String?
  gstin               String?
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  userAccesses        UserBranchAccess[]
  numberingSeries     NumberingSeries[]
  journalEntries      JournalEntry[]
  quotationDocuments  QuotationDocument[]
  commercialDocuments CommercialDocument[]
  settlements         AccountSettlement[]

  @@unique([companyId, code])
  @@unique([companyId, id])
  @@index([companyId, isActive])
  @@map("branches")
}

model UserBranchAccess {
  userId    String   @db.Uuid
  branchId  String   @db.Uuid
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  branch    Branch   @relation(fields: [branchId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  createdAt DateTime @default(now())

  @@id([userId, branchId])
  @@index([branchId])
  @@map("user_branch_accesses")
}

model EmailVerificationToken {
  id        String    @id @default(uuid()) @db.Uuid
  userId    String    @db.Uuid
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  tokenHash String    @unique @db.Char(64)
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())

  @@index([userId, createdAt])
  @@index([expiresAt])
  @@map("email_verification_tokens")
}

model Attendance {
  id                  String          @id @default(uuid()) @db.Uuid
  branchId            String          @db.Uuid
  companyId           String          @db.Uuid
  company             Company         @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  userId              String          @db.Uuid
  user                User            @relation(fields: [userId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  startedAt           DateTime        @default(now())
  endedAt             DateTime?
  startLatitude       Float?
  startLongitude      Float?
  startAccuracyMeters Float?
  endLatitude         Float?
  endLongitude        Float?
  endAccuracyMeters   Float?
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt
  locationPoints      LocationPoint[]
  customerVisits      CustomerVisit[]
  geofenceEvents      GeofenceEvent[]

  @@index([companyId, startedAt])
  @@index([companyId, userId, startedAt])
  @@index([companyId, branchId])
  @@map("attendances")
}

model Customer {
  id                        String              @id @default(uuid()) @db.Uuid
  branchId                  String              @db.Uuid
  companyId                 String              @db.Uuid
  company                   Company             @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  assignedUserId            String?             @db.Uuid
  assignedUser              User?               @relation("CustomerAssignee", fields: [assignedUserId], references: [id], onDelete: SetNull, onUpdate: Restrict)
  name                      String
  contactPerson             String?
  phone                     String?
  email                     String?
  address                   String?
  latitude                  Float?
  longitude                 Float?
  checkInReferenceLatitude  Float?
  checkInReferenceLongitude Float?
  checkInReferenceVisitId   String?             @db.Uuid
  checkInReferenceSetAt     DateTime?
  billingAddress            String?
  shippingAddress           String?
  gstin                     String?             @db.VarChar(15)
  pan                       String?             @db.VarChar(10)
  notes                     String?
  isActive                  Boolean             @default(true)
  isAccountCustomer         Boolean             @default(false)
  createdAt                 DateTime            @default(now())
  updatedAt                 DateTime            @updatedAt
  visits                    CustomerVisit[]
  leads                     Lead[]
  geofenceEvents            GeofenceEvent[]
  quotationDocuments        QuotationDocument[]

  @@unique([companyId, id])
  @@index([companyId, name])
  @@index([companyId, createdAt])
  @@index([companyId, assignedUserId])
  @@index([companyId, branchId])
  @@map("customers")
  commercialDocuments CommercialDocument[] @relation("CommercialCustomer")
  settlements AccountSettlement[] @relation("SettlementCustomer")
  paymentReminders PaymentReminder[] @relation("PaymentReminderCustomer")
}

model CustomerVisit {
  id                     String             @id @default(uuid()) @db.Uuid
  branchId               String             @db.Uuid
  companyId              String             @db.Uuid
  company                Company            @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  userId                 String             @db.Uuid
  user                   User               @relation(fields: [userId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  customerId             String?            @db.Uuid
  customer               Customer?          @relation(fields: [customerId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  leadId                 String?            @db.Uuid
  lead                   Lead?              @relation("VisitLead", fields: [leadId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  visitType              VisitType          @default(CUSTOMER)
  contactName            String?            @db.VarChar(160)
  contactPhone           String?            @db.VarChar(30)
  attendanceId           String?            @db.Uuid
  attendance             Attendance?        @relation(fields: [attendanceId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  checkedInAt            DateTime           @default(now())
  checkInLatitude        Float
  checkInLongitude       Float
  checkInAccuracyMeters  Float
  checkInAddress         String?            @db.VarChar(1000)
  checkedOutAt           DateTime?
  checkOutLatitude       Float?
  checkOutLongitude      Float?
  checkOutAccuracyMeters Float?
  checkoutSentiment      CheckoutSentiment?
  checkoutRemarks        String?
  visitNotes             String?
  createdAt              DateTime           @default(now())
  updatedAt              DateTime           @updatedAt
  sourceLeads            Lead[]             @relation("LeadSourceVisit")
  photo                  VisitPhoto?
  completedFollowUpTask  FollowUpTask?

  @@index([companyId, checkedInAt])
  @@index([companyId, userId, checkedInAt])
  @@index([companyId, customerId, checkedInAt])
  @@index([companyId, leadId, checkedInAt])
  @@index([companyId, userId, checkedOutAt])
  @@index([companyId, branchId])
  @@map("customer_visits")
}

model Lead {
  id                        String          @id @default(uuid()) @db.Uuid
  branchId                  String          @db.Uuid
  companyId                 String          @db.Uuid
  company                   Company         @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  customerId                String?         @db.Uuid
  customer                  Customer?       @relation(fields: [customerId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  sourceVisitId             String?         @db.Uuid
  sourceVisit               CustomerVisit?  @relation("LeadSourceVisit", fields: [sourceVisitId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  visits                    CustomerVisit[] @relation("VisitLead")
  checkInReferenceLatitude  Float?
  checkInReferenceLongitude Float?
  checkInReferenceVisitId   String?         @db.Uuid
  checkInReferenceSetAt     DateTime?
  assignedUserId            String          @db.Uuid
  assignedUser              User            @relation("LeadAssignee", fields: [assignedUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  createdByUserId           String          @db.Uuid
  createdByUser             User            @relation("LeadCreator", fields: [createdByUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  title                     String          @db.VarChar(200)
  contactName               String?         @db.VarChar(150)
  phone                     String?         @db.VarChar(30)
  email                     String?         @db.VarChar(320)
  companyName               String?         @db.VarChar(200)
  source                    LeadSource      @default(MANUAL)
  stage                     LeadStage       @default(NEW)
  estimatedValue            Decimal?        @db.Decimal(18, 2)
  currencyCode              String          @default("INR") @db.Char(3)
  followUpAt                DateTime?
  notes                     String?         @db.VarChar(5000)
  lostReason                String?         @db.VarChar(1000)
  wonAt                     DateTime?
  lostAt                    DateTime?
  version                   Int             @default(1)
  createdAt                 DateTime        @default(now())
  updatedAt                 DateTime        @updatedAt
  activities                LeadActivity[]
  followUpTasks             FollowUpTask[]

  quotationDocuments QuotationDocument[]

  @@unique([companyId, id])
  @@index([companyId, stage])
  @@index([companyId, assignedUserId])
  @@index([companyId, followUpAt])
  @@index([companyId, createdAt])
  @@index([customerId])
  @@index([sourceVisitId])
  @@index([companyId, branchId])
  @@map("leads")
}

model FollowUpTask {
  id               String             @id @default(uuid()) @db.Uuid
  branchId         String             @db.Uuid
  companyId        String             @db.Uuid
  company          Company            @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  leadId           String             @db.Uuid
  lead             Lead               @relation(fields: [leadId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  assignedUserId   String             @db.Uuid
  assignedUser     User               @relation("FollowUpTaskAssignee", fields: [assignedUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  createdByUserId  String             @db.Uuid
  createdByUser    User               @relation("FollowUpTaskCreator", fields: [createdByUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  dueDate          DateTime           @db.Date
  notes            String?            @db.VarChar(2000)
  status           FollowUpTaskStatus @default(PENDING)
  completedVisitId String?            @unique @db.Uuid
  completedVisit   CustomerVisit?     @relation(fields: [completedVisitId], references: [id], onDelete: SetNull, onUpdate: Restrict)
  completedAt      DateTime?
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt

  @@index([companyId, assignedUserId, status, dueDate])
  @@index([companyId, leadId, status, dueDate])
  @@index([companyId, status, dueDate])
  @@index([companyId, branchId])
  @@map("follow_up_tasks")
}

model VisitPhoto {
  id                 String        @id @default(uuid()) @db.Uuid
  companyId          String        @db.Uuid
  company            Company       @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  visitId            String        @unique @db.Uuid
  visit              CustomerVisit @relation(fields: [visitId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  uploadedByUserId   String        @db.Uuid
  uploadedByUser     User          @relation("VisitPhotoUploader", fields: [uploadedByUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  objectKey          String        @db.VarChar(500)
  thumbnailObjectKey String        @db.VarChar(500)
  mimeType           String        @db.VarChar(100)
  sizeBytes          Int
  thumbnailSizeBytes Int
  width              Int
  height             Int
  createdAt          DateTime      @default(now())

  @@index([companyId, createdAt])
  @@map("visit_photos")
}

model PendingStorageDeletion {
  id            String    @id @default(uuid()) @db.Uuid
  companyId     String    @db.Uuid
  company       Company   @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  objectKey     String    @unique @db.VarChar(500)
  attempts      Int       @default(0)
  lastAttemptAt DateTime?
  lastError     String?   @db.VarChar(100)
  createdAt     DateTime  @default(now())

  @@index([createdAt])
  @@map("pending_storage_deletions")
}

model LeadDeletionAudit {
  id            String   @id @default(uuid()) @db.Uuid
  companyId     String   @db.Uuid
  company       Company  @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  actorUserId   String   @db.Uuid
  actorUser     User     @relation("LeadDeletionActor", fields: [actorUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  deletedLeadId String   @db.Uuid
  visitsDeleted Int
  photosQueued  Int
  createdAt     DateTime @default(now())

  @@index([companyId, createdAt])
  @@map("lead_deletion_audits")
}

model LeadActivity {
  id                     String           @id @default(uuid()) @db.Uuid
  companyId              String           @db.Uuid
  company                Company          @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  leadId                 String           @db.Uuid
  lead                   Lead             @relation(fields: [leadId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  actorUserId            String           @db.Uuid
  actorUser              User             @relation("LeadActor", fields: [actorUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  type                   LeadActivityType
  fromStage              LeadStage?
  toStage                LeadStage?
  previousAssignedUserId String?          @db.Uuid
  previousAssignedUser   User?            @relation("PreviousLeadAssignee", fields: [previousAssignedUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  newAssignedUserId      String?          @db.Uuid
  newAssignedUser        User?            @relation("NewLeadAssignee", fields: [newAssignedUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  createdAt              DateTime         @default(now())

  @@index([leadId, createdAt])
  @@index([companyId, createdAt])
  @@map("lead_activities")
}

model LocationPoint {
  id             String     @id @default(uuid()) @db.Uuid
  branchId       String     @db.Uuid
  companyId      String     @db.Uuid
  company        Company    @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  userId         String     @db.Uuid
  user           User       @relation(fields: [userId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  attendanceId   String     @db.Uuid
  attendance     Attendance @relation(fields: [attendanceId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  latitude       Float
  longitude      Float
  accuracyMeters Float?
  capturedAt     DateTime
  receivedAt     DateTime   @default(now())
  sequenceNumber Int
  clientPointId  String?    @db.Uuid

  @@unique([attendanceId, sequenceNumber])
  @@unique([companyId, userId, clientPointId])
  @@index([companyId, userId, capturedAt])
  @@index([attendanceId, capturedAt])
  @@index([companyId, branchId])
  @@map("location_points")
}

model Session {
  id             String   @id @default(uuid()) @db.Uuid
  userId         String   @db.Uuid
  user           User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  tokenHash      String   @unique
  sessionVersion Int      @default(0)
  expiresAt      DateTime
  createdAt      DateTime @default(now())

  @@index([userId])
  @@index([expiresAt])
  @@map("sessions")
}

model MobileSession {
  id             String       @id @default(uuid()) @db.Uuid
  userId         String       @db.Uuid
  user           User         @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  tokenHash      String       @unique @db.Char(64)
  sessionVersion Int          @default(0)
  expiresAt      DateTime
  lastUsedAt     DateTime
  revokedAt      DateTime?
  createdAt      DateTime     @default(now())
  pushDevices    PushDevice[]

  @@index([userId, revokedAt])
  @@index([expiresAt])
  @@map("mobile_sessions")
}

model PushDevice {
  id              String        @id @default(uuid()) @db.Uuid
  companyId       String        @db.Uuid
  company         Company       @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  userId          String        @db.Uuid
  user            User          @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  mobileSessionId String        @db.Uuid
  mobileSession   MobileSession @relation(fields: [mobileSessionId], references: [id], onDelete: Cascade, onUpdate: Restrict)
  installationId  String        @unique @db.VarChar(128)
  fcmToken        String        @unique @db.VarChar(4096)
  platform        PushPlatform  @default(ANDROID)
  lastSeenAt      DateTime      @default(now())
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@index([companyId, userId])
  @@index([mobileSessionId])
  @@map("push_devices")
}

model GeofenceEvent {
  id                  String            @id @default(uuid()) @db.Uuid
  branchId            String            @db.Uuid
  companyId           String            @db.Uuid
  company             Company           @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  employeeId          String            @db.Uuid
  employee            User              @relation("GeofenceEmployee", fields: [employeeId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  type                GeofenceEventType
  action              GeofenceAction
  customerId          String?           @db.Uuid
  customer            Customer?         @relation(fields: [customerId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  attendanceId        String?           @db.Uuid
  attendance          Attendance?       @relation(fields: [attendanceId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  referenceLatitude   Float
  referenceLongitude  Float
  actualLatitude      Float
  actualLongitude     Float
  accuracyMeters      Float?
  allowedRadiusMeters Int
  distanceMeters      Float?
  occurredAt          DateTime          @default(now())

  @@index([companyId, occurredAt])
  @@index([companyId, employeeId, occurredAt])
  @@index([companyId, action, occurredAt])
  @@index([customerId, occurredAt])
  @@index([companyId, branchId])
  @@map("geofence_events")
}

model SalesTarget {
  id              String           @id @default(uuid()) @db.Uuid
  branchId        String           @db.Uuid
  companyId       String           @db.Uuid
  company         Company          @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  assignedUserId  String           @db.Uuid
  assignedUser    User             @relation("TargetAssignee", fields: [assignedUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  createdByUserId String           @db.Uuid
  createdByUser   User             @relation("TargetCreator", fields: [createdByUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  metric          TargetMetric
  periodType      TargetPeriodType
  startDate       DateTime         @db.Date
  endDate         DateTime         @db.Date
  targetValue     Decimal          @db.Decimal(18, 2)
  currencyCode    String           @default("INR") @db.Char(3)
  version         Int              @default(1)
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt

  @@unique([companyId, assignedUserId, metric, startDate, endDate])
  @@index([companyId, assignedUserId])
  @@index([companyId, startDate, endDate])
  @@index([companyId, metric])
  @@index([companyId, branchId])
  @@map("sales_targets")
}

model DailyTravelApproval {
  id               String               @id @default(uuid()) @db.Uuid
  branchId         String               @db.Uuid
  companyId        String               @db.Uuid
  company          Company              @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  employeeId       String               @db.Uuid
  employee         User                 @relation("TravelApprovalEmployee", fields: [employeeId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  businessDate     DateTime             @db.Date
  distanceMeters   Int
  ratePerKm        Decimal              @db.Decimal(10, 2)
  amount           Decimal              @db.Decimal(18, 2)
  status           TravelApprovalStatus @default(PENDING)
  reviewedByUserId String?              @db.Uuid
  reviewedBy       User?                @relation("TravelApprovalReviewer", fields: [reviewedByUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  reviewedAt       DateTime?
  createdAt        DateTime             @default(now())
  updatedAt        DateTime             @updatedAt

  @@unique([companyId, branchId, employeeId, businessDate])
  @@index([companyId, businessDate, status])
  @@index([employeeId, businessDate])
  @@index([companyId, branchId])
  @@map("daily_travel_approvals")
}

model EmployeeCompensationProfile {
  id             String             @id @default(uuid()) @db.Uuid
  companyId      String             @db.Uuid
  employeeId     String             @db.Uuid
  baseAmount     Decimal            @db.Decimal(18, 2)
  currencyCode   String             @default("INR") @db.Char(3)
  effectiveFrom  DateTime           @db.Date
  effectiveUntil DateTime?          @db.Date
  status         CompensationStatus @default(ACTIVE)
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt

  @@unique([employeeId, effectiveFrom])
  @@index([companyId, employeeId, status])
  @@map("employee_compensation_profiles")
}

model EmployeeAdvance {
  id           String                         @id @default(uuid()) @db.Uuid
  companyId    String                         @db.Uuid
  branchId     String                         @db.Uuid
  employeeId   String                         @db.Uuid
  amount       Decimal                        @db.Decimal(18, 2)
  currencyCode String                         @default("INR") @db.Char(3)
  advanceDate  DateTime                       @db.Date
  note         String?                        @db.VarChar(1000)
  reference    String?                        @db.VarChar(120)
  status       EmployeeFinancialRequestStatus @default(PENDING)
  createdAt    DateTime                       @default(now())
  updatedAt    DateTime                       @updatedAt

  @@index([companyId, branchId, employeeId])
  @@map("employee_advances")
}

model EmployeeReimbursement {
  id               String                         @id @default(uuid()) @db.Uuid
  companyId        String                         @db.Uuid
  branchId         String                         @db.Uuid
  employeeId       String                         @db.Uuid
  amount           Decimal                        @db.Decimal(18, 2)
  currencyCode     String                         @default("INR") @db.Char(3)
  requestedOn      DateTime                       @db.Date
  description      String                         @db.VarChar(2000)
  status           EmployeeFinancialRequestStatus @default(PENDING)
  reviewedByUserId String?                        @db.Uuid
  reviewedAt       DateTime?
  createdAt        DateTime                       @default(now())
  updatedAt        DateTime                       @updatedAt

  @@index([companyId, branchId, employeeId, status])
  @@map("employee_reimbursements")
}

model SalaryHistoryLink {
  id                    String   @id @default(uuid()) @db.Uuid
  companyId             String   @db.Uuid
  employeeId            String   @db.Uuid
  compensationProfileId String   @db.Uuid
  payrollReference      String?  @db.VarChar(120)
  periodStart           DateTime @db.Date
  periodEnd             DateTime @db.Date
  createdAt             DateTime @default(now())

  @@unique([companyId, employeeId, periodStart, periodEnd])
  @@index([compensationProfileId])
  @@map("salary_history_links")
}

model BillingPrice {
  id              String        @id @default(uuid()) @db.Uuid
  role            BillingRole
  period          BillingPeriod
  amount          Decimal       @db.Decimal(18, 2)
  currency        String        @default("INR") @db.Char(3)
  effectiveFrom   DateTime      @default(now())
  effectiveUntil  DateTime?
  createdByUserId String?       @db.Uuid
  createdByUser   User?         @relation("PriceCreator", fields: [createdByUserId], references: [id], onDelete: Restrict)
  createdAt       DateTime      @default(now())

  @@index([role, period, effectiveFrom])
  @@map("billing_prices")
}

model CompanySubscription {
  id            String                 @id @default(uuid()) @db.Uuid
  companyId     String                 @db.Uuid
  company       Company                @relation(fields: [companyId], references: [id], onDelete: Restrict)
  status        PaidSubscriptionStatus
  billingPeriod BillingPeriod
  adminSeats    Int                    @default(0)
  managerSeats  Int
  salesSeats    Int
  startsAt      DateTime
  endsAt        DateTime
  sourceOrderId String?                @unique @db.Uuid
  sourceOrder   BillingOrder?          @relation(fields: [sourceOrderId], references: [id], onDelete: Restrict)
  createdAt     DateTime               @default(now())
  updatedAt     DateTime               @updatedAt

  @@index([companyId, status, endsAt])
  @@map("company_subscriptions")
}

model BillingOrder {
  id                     String               @id @default(uuid()) @db.Uuid
  companyId              String               @db.Uuid
  company                Company              @relation(fields: [companyId], references: [id], onDelete: Restrict)
  createdByUserId        String               @db.Uuid
  createdByUser          User                 @relation("OrderCreator", fields: [createdByUserId], references: [id], onDelete: Restrict)
  billingPeriod          BillingPeriod
  adminSeats             Int                  @default(0)
  managerSeats           Int
  salesSeats             Int
  adminUnitPrice         Decimal              @default(0) @db.Decimal(18, 2)
  managerUnitPrice       Decimal              @db.Decimal(18, 2)
  salesUnitPrice         Decimal              @db.Decimal(18, 2)
  currency               String               @db.Char(3)
  subtotal               Decimal              @db.Decimal(18, 2)
  taxAmount              Decimal              @default(0) @db.Decimal(18, 2)
  totalAmount            Decimal              @db.Decimal(18, 2)
  status                 BillingOrderStatus   @default(PENDING)
  provider               String               @default("UNCONFIGURED") @db.VarChar(40)
  providerOrderId        String?              @unique @db.VarChar(200)
  idempotencyKey         String               @unique @db.VarChar(100)
  retainAdminUserIds     String[]             @default([]) @db.Uuid
  retainManagerUserIds   String[]             @default([]) @db.Uuid
  retainSalesUserIds     String[]             @default([]) @db.Uuid
  seatReductionAppliedAt DateTime?
  paidAt                 DateTime?
  expiresAt              DateTime?
  createdAt              DateTime             @default(now())
  updatedAt              DateTime             @updatedAt
  payments               PaymentTransaction[]
  subscription           CompanySubscription?

  @@index([companyId, createdAt])
  @@map("billing_orders")
}

model PaymentTransaction {
  id                String        @id @default(uuid()) @db.Uuid
  companyId         String        @db.Uuid
  company           Company       @relation(fields: [companyId], references: [id], onDelete: Restrict)
  orderId           String        @db.Uuid
  order             BillingOrder  @relation(fields: [orderId], references: [id], onDelete: Restrict)
  provider          String        @db.VarChar(40)
  providerPaymentId String        @unique @db.VarChar(200)
  amount            Decimal       @db.Decimal(18, 2)
  currency          String        @db.Char(3)
  status            PaymentStatus
  capturedAt        DateTime?
  failureCode       String?       @db.VarChar(100)
  failureMessage    String?       @db.VarChar(500)
  createdAt         DateTime      @default(now())
  updatedAt         DateTime      @updatedAt

  @@index([companyId, createdAt])
  @@index([orderId])
  @@map("payment_transactions")
}

model BillingAuditEvent {
  id          String           @id @default(uuid()) @db.Uuid
  companyId   String?          @db.Uuid
  company     Company?         @relation(fields: [companyId], references: [id], onDelete: Restrict)
  actorUserId String?          @db.Uuid
  actorUser   User?            @relation("BillingAuditActor", fields: [actorUserId], references: [id], onDelete: Restrict)
  type        BillingAuditType
  entityId    String?          @db.Uuid
  reason      String?          @db.VarChar(500)
  metadata    Json?
  occurredAt  DateTime         @default(now())

  @@index([companyId, occurredAt])
  @@index([type, occurredAt])
  @@map("billing_audit_events")
}

model RateLimitBucket {
  key       String   @id @db.VarChar(128)
  count     Int
  resetsAt  DateTime
  updatedAt DateTime @updatedAt

  @@index([resetsAt])
  @@map("rate_limit_buckets")
}

model AccountSettings {
  companyId      String          @id @db.Uuid
  company        Company         @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  baseCurrency   String          @default("INR") @db.Char(3)
  businessType   BusinessType    @default(OTHER_MIXED)
  enabledModules AccountModule[] @default([])
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt

  @@map("account_settings")
}

model FinancialYear {
  id             String                @id @default(uuid()) @db.Uuid
  companyId      String                @db.Uuid
  company        Company               @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  name           String                @db.VarChar(80)
  startDate      DateTime              @db.Date
  endDate        DateTime              @db.Date
  isCurrent      Boolean               @default(false)
  isActive       Boolean               @default(true)
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt
  journalEntries JournalEntry[]
  periodLock     AccountingPeriodLock?

  @@unique([companyId, id])
  @@unique([companyId, name])
  @@index([companyId, startDate, endDate])
  @@map("financial_years")
}

model NumberingSeries {
  id           String   @id @default(uuid()) @db.Uuid
  companyId    String   @db.Uuid
  branchId     String?  @db.Uuid
  company      Company  @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  branch       Branch?  @relation(fields: [companyId, branchId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  seriesKey    String   @db.VarChar(60)
  prefix       String   @default("") @db.VarChar(30)
  nextSequence BigInt   @default(1)
  padding      Int      @default(5)
  suffix       String   @default("") @db.VarChar(30)
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@unique([companyId, branchId, seriesKey])
  @@index([companyId, seriesKey])
  @@map("numbering_series")
}

model Vendor {
  id            String   @id @default(uuid()) @db.Uuid
  companyId     String   @db.Uuid
  company       Company  @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  name          String
  contactPerson String?
  phone         String?
  email         String?
  address       String?
  gstin         String?  @db.VarChar(15)
  pan           String?  @db.VarChar(10)
  notes         String?
  isActive      Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([companyId, name])
  @@unique([companyId, id])
  @@map("vendors")
  commercialDocuments CommercialDocument[] @relation("CommercialVendor")
  settlements AccountSettlement[] @relation("SettlementVendor")
}

model AccountUnit {
  id           String           @id @default(uuid()) @db.Uuid
  companyId    String           @db.Uuid
  company      Company          @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  name         String           @db.VarChar(80)
  symbol       String           @db.VarChar(20)
  isActive     Boolean          @default(true)
  products     AccountProduct[]
  services     AccountService[]
  workPackages WorkPackage[]
  createdAt    DateTime         @default(now())
  updatedAt    DateTime         @updatedAt

  quotationLines QuotationLine[]

  @@unique([companyId, id])
  @@unique([companyId, name])
  @@unique([companyId, symbol])
  @@map("account_units")
}

model AccountCategory {
  id          String               @id @default(uuid()) @db.Uuid
  companyId   String               @db.Uuid
  company     Company              @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  name        String               @db.VarChar(100)
  description String?
  scope       AccountCategoryScope @default(BOTH)
  isActive    Boolean              @default(true)
  products    AccountProduct[]
  services    AccountService[]
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @updatedAt

  @@unique([companyId, id])
  @@unique([companyId, name])
  @@index([companyId, scope, isActive])
  @@map("account_categories")
}

model AccountProduct {
  id          String           @id @default(uuid()) @db.Uuid
  companyId   String           @db.Uuid
  company     Company          @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  categoryId  String?          @db.Uuid
  category    AccountCategory? @relation(fields: [companyId, categoryId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  unitId      String?          @db.Uuid
  unit        AccountUnit?     @relation(fields: [companyId, unitId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  name        String
  code        String?          @db.VarChar(60)
  description String?
  salePrice   Decimal?         @db.Decimal(18, 2)
  costPrice   Decimal?         @db.Decimal(18, 2)
  taxRate     Decimal?         @db.Decimal(5, 2)
  isActive    Boolean          @default(true)
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt

  quotationLines QuotationLine[]
  commercialLines CommercialDocumentLine[]

  @@unique([companyId, id])
  @@unique([companyId, code])
  @@index([companyId, name])
  @@map("account_products")
}

model AccountService {
  id            String           @id @default(uuid()) @db.Uuid
  companyId     String           @db.Uuid
  company       Company          @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  categoryId    String?          @db.Uuid
  category      AccountCategory? @relation(fields: [companyId, categoryId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  unitId        String?          @db.Uuid
  unit          AccountUnit?     @relation(fields: [companyId, unitId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  name          String
  code          String?          @db.VarChar(60)
  description   String?
  sellingRate   Decimal?         @db.Decimal(18, 2)
  estimatedCost Decimal?         @db.Decimal(18, 2)
  taxRate       Decimal?         @db.Decimal(5, 2)
  isActive      Boolean          @default(true)
  createdAt     DateTime         @default(now())
  updatedAt     DateTime         @updatedAt

  quotationLines QuotationLine[]
  commercialLines CommercialDocumentLine[]

  @@unique([companyId, id])
  @@unique([companyId, code])
  @@index([companyId, name])
  @@map("account_services")
}

model WorkCategory {
  id           String        @id @default(uuid()) @db.Uuid
  companyId    String        @db.Uuid
  company      Company       @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  name         String        @db.VarChar(100)
  description  String?
  isActive     Boolean       @default(true)
  workPackages WorkPackage[]
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt

  @@unique([companyId, id])
  @@unique([companyId, name])
  @@map("work_categories")
}

model WorkPackage {
  id             String       @id @default(uuid()) @db.Uuid
  companyId      String       @db.Uuid
  company        Company      @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  workCategoryId String       @db.Uuid
  workCategory   WorkCategory @relation(fields: [companyId, workCategoryId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  unitId         String?      @db.Uuid
  unit           AccountUnit? @relation(fields: [companyId, unitId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  name           String
  code           String?      @db.VarChar(60)
  description    String?
  sellingRate    Decimal?     @db.Decimal(18, 2)
  estimatedCost  Decimal?     @db.Decimal(18, 2)
  isActive       Boolean      @default(true)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  quotationLines QuotationLine[]
  commercialLines CommercialDocumentLine[]

  @@unique([companyId, id])
  @@unique([companyId, code])
  @@index([companyId, name])
  @@map("work_packages")
}

model CustomFieldDefinition {
  id         String              @id @default(uuid()) @db.Uuid
  companyId  String              @db.Uuid
  company    Company             @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  entityType CustomFieldEntity
  fieldKey   String              @db.VarChar(60)
  label      String              @db.VarChar(100)
  dataType   CustomFieldDataType
  isRequired Boolean             @default(false)
  isActive   Boolean             @default(true)
  position   Int                 @default(0)
  options    Json?
  validation Json?
  createdAt  DateTime            @default(now())
  updatedAt  DateTime            @updatedAt

  @@unique([companyId, entityType, fieldKey])
  @@index([companyId, entityType, isActive, position])
  @@map("custom_field_definitions")
}

model LedgerAccount {
  id            String             @id @default(uuid()) @db.Uuid
  companyId     String             @db.Uuid
  company       Company            @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  code          String             @db.VarChar(30)
  name          String             @db.VarChar(160)
  accountClass  LedgerAccountClass
  normalBalance NormalBalanceSide
  parentId      String?            @db.Uuid
  parent        LedgerAccount?     @relation("LedgerTree", fields: [companyId, parentId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  children      LedgerAccount[]    @relation("LedgerTree")
  systemKey     String?            @db.VarChar(60)
  isSystem      Boolean            @default(false)
  isActive      Boolean            @default(true)
  allowPosting  Boolean            @default(true)
  journalLines  JournalLine[]
  createdAt     DateTime           @default(now())
  updatedAt     DateTime           @updatedAt

  @@unique([companyId, id])
  @@unique([companyId, code])
  @@unique([companyId, name])
  @@unique([companyId, systemKey])
  @@index([companyId, accountClass, isActive])
  @@map("ledger_accounts")
}

model CostCentre {
  id           String        @id @default(uuid()) @db.Uuid
  companyId    String        @db.Uuid
  company      Company       @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  code         String        @db.VarChar(30)
  name         String        @db.VarChar(120)
  description  String?
  isActive     Boolean       @default(true)
  journalLines JournalLine[]
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt

  @@unique([companyId, id])
  @@unique([companyId, code])
  @@unique([companyId, name])
  @@map("cost_centres")
}

model JournalEntry {
  id              String        @id @default(uuid()) @db.Uuid
  companyId       String        @db.Uuid
  company         Company       @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  financialYearId String        @db.Uuid
  financialYear   FinancialYear @relation(fields: [companyId, financialYearId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  branchId        String        @db.Uuid
  branch          Branch        @relation(fields: [companyId, branchId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  journalNumber   String        @db.VarChar(100)
  entryDate       DateTime      @db.Date
  status          JournalStatus @default(DRAFT)
  reference       String?       @db.VarChar(160)
  narration       String?
  sourceType      String        @db.VarChar(60)
  sourceId        String        @db.VarChar(100)
  postingPurpose  String        @default("PRIMARY") @db.VarChar(60)
  reversalOfId    String?       @db.Uuid
  reversalOf      JournalEntry? @relation("JournalReversal", fields: [companyId, reversalOfId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  reversedBy      JournalEntry? @relation("JournalReversal")
  reversalReason  String?
  createdById     String        @db.Uuid
  createdBy       User          @relation("JournalCreator", fields: [companyId, createdById], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  postedById      String?       @db.Uuid
  postedBy        User?         @relation("JournalPoster", fields: [companyId, postedById], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  postedAt        DateTime?
  lines           JournalLine[]
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@unique([companyId, id])
  @@unique([companyId, branchId, journalNumber])
  @@unique([companyId, sourceType, sourceId, postingPurpose])
  @@unique([companyId, reversalOfId])
  @@index([companyId, journalNumber])
  @@index([companyId, entryDate, status])
  @@index([companyId, branchId, entryDate])
  @@map("journal_entries")
  commercialDocuments CommercialDocument[] @relation("CommercialJournal")
  settlements AccountSettlement[] @relation("SettlementJournal")
  advanceApplications AdvanceApplication[] @relation("AdvanceApplicationJournal")
}

model JournalLine {
  id              String        @id @default(uuid()) @db.Uuid
  companyId       String        @db.Uuid
  company         Company       @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  journalEntryId  String        @db.Uuid
  journalEntry    JournalEntry  @relation(fields: [companyId, journalEntryId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  ledgerAccountId String        @db.Uuid
  ledgerAccount   LedgerAccount @relation(fields: [companyId, ledgerAccountId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  costCentreId    String?       @db.Uuid
  costCentre      CostCentre?   @relation(fields: [companyId, costCentreId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  lineNumber      Int
  debit           Decimal       @default(0) @db.Decimal(18, 2)
  credit          Decimal       @default(0) @db.Decimal(18, 2)
  description     String?
  createdAt       DateTime      @default(now())

  @@unique([journalEntryId, lineNumber])
  @@index([companyId, ledgerAccountId])
  @@index([companyId, costCentreId])
  @@map("journal_lines")
}

model AccountingPeriodLock {
  id              String        @id @default(uuid()) @db.Uuid
  companyId       String        @db.Uuid
  company         Company       @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  financialYearId String        @db.Uuid
  financialYear   FinancialYear @relation(fields: [companyId, financialYearId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  lockedThrough   DateTime?     @db.Date
  updatedById     String        @db.Uuid
  updatedBy       User          @relation("AccountingLockUpdater", fields: [companyId, updatedById], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  updatedAt       DateTime      @updatedAt
  createdAt       DateTime      @default(now())

  @@unique([companyId, id])
  @@unique([companyId, financialYearId])
  @@index([companyId, lockedThrough])
  @@map("accounting_period_locks")
}

model AccountingAuditEvent {
  id          String                   @id @default(uuid()) @db.Uuid
  companyId   String                   @db.Uuid
  company     Company                  @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  actorUserId String                   @db.Uuid
  actor       User                     @relation("AccountingAuditActor", fields: [companyId, actorUserId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  eventType   AccountingAuditEventType
  entityType  String                   @db.VarChar(60)
  entityId    String                   @db.VarChar(100)
  reason      String?
  metadata    Json?
  createdAt   DateTime                 @default(now())

  @@index([companyId, createdAt])
  @@index([companyId, entityType, entityId])
  @@map("accounting_audit_events")
}

model QuotationDocument {
  id                    String                @id @default(uuid()) @db.Uuid
  companyId             String                @db.Uuid
  branchId              String                @db.Uuid
  documentType          QuotationDocumentType
  documentNumber        String                @db.VarChar(80)
  customerId            String?               @db.Uuid
  sourceLeadId          String?               @db.Uuid
  status                QuotationStatus       @default(DRAFT)
  currentRevisionNumber Int                   @default(0)
  createdById           String                @db.Uuid
  acceptedById          String?               @db.Uuid
  acceptedAt            DateTime?
  acceptanceNote        String?
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt
  company               Company               @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  branch                Branch                @relation(fields: [companyId, branchId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  customer              Customer?             @relation(fields: [companyId, customerId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  sourceLead            Lead?                 @relation(fields: [companyId, sourceLeadId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  createdBy             User                  @relation("QuotationCreator", fields: [companyId, createdById], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  acceptedBy            User?                 @relation("QuotationAcceptor", fields: [companyId, acceptedById], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  revisions             QuotationRevision[]
  audits                QuotationAuditEvent[]

  @@unique([companyId, id])
  @@unique([companyId, branchId, documentType, documentNumber])
  @@index([companyId, branchId, status])
  @@map("quotation_documents")
}

model QuotationRevision {
  id                    String                     @id @default(uuid()) @db.Uuid
  companyId             String                     @db.Uuid
  documentId            String                     @db.Uuid
  revisionNumber        Int
  status                QuotationStatus            @default(DRAFT)
  issueDate             DateTime                   @db.Date
  validUntil            DateTime?                  @db.Date
  customerName          String
  contactPerson         String?
  phone                 String?
  email                 String?
  billingAddress        String?
  siteAddress           String?
  gstin                 String?                    @db.VarChar(15)
  pan                   String?                    @db.VarChar(10)
  terms                 String?
  inclusions            String?
  exclusions            String?
  notes                 String?
  subtotal              Decimal                    @default(0) @db.Decimal(18, 2)
  discountTotal         Decimal                    @default(0) @db.Decimal(18, 2)
  additionalChargeTotal Decimal                    @default(0) @db.Decimal(18, 2)
  taxableTotal          Decimal                    @default(0) @db.Decimal(18, 2)
  taxTotal              Decimal                    @default(0) @db.Decimal(18, 2)
  grandTotal            Decimal                    @default(0) @db.Decimal(18, 2)
  internalCostTotal     Decimal                    @default(0) @db.Decimal(18, 2)
  expectedProfit        Decimal                    @default(0) @db.Decimal(18, 2)
  expectedMarginPercent Decimal                    @default(0) @db.Decimal(9, 4)
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt
  company               Company                    @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  document              QuotationDocument          @relation(fields: [companyId, documentId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  lines                 QuotationLine[]
  adjustments           QuotationAdjustment[]
  paymentSchedule       QuotationPaymentSchedule[]
  shares                QuotationShare[]

  @@unique([companyId, id])
  @@unique([documentId, revisionNumber])
  @@index([companyId, documentId])
  @@map("quotation_revisions")
}

model QuotationLine {
  id                String              @id @default(uuid()) @db.Uuid
  companyId         String              @db.Uuid
  revisionId        String              @db.Uuid
  position          Int
  lineType          QuotationLineType
  productId         String?             @db.Uuid
  serviceId         String?             @db.Uuid
  workPackageId     String?             @db.Uuid
  unitId            String?             @db.Uuid
  itemName          String
  itemCode          String?
  description       String?
  specification     String?
  unitName          String?
  unitSymbol        String?
  quantity          Decimal             @db.Decimal(18, 4)
  rate              Decimal             @db.Decimal(18, 2)
  discountType      QuotationValueType?
  discountValue     Decimal?            @db.Decimal(18, 4)
  baseAmount        Decimal             @db.Decimal(18, 2)
  discountAmount    Decimal             @db.Decimal(18, 2)
  taxableAmount     Decimal             @db.Decimal(18, 2)
  taxRate           Decimal             @db.Decimal(5, 2)
  taxAmount         Decimal             @db.Decimal(18, 2)
  lineTotal         Decimal             @db.Decimal(18, 2)
  internalUnitCost  Decimal             @db.Decimal(18, 2)
  internalCostTotal Decimal             @db.Decimal(18, 2)
  company           Company             @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  revision          QuotationRevision   @relation(fields: [companyId, revisionId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  product           AccountProduct?     @relation(fields: [companyId, productId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  service           AccountService?     @relation(fields: [companyId, serviceId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  workPackage       WorkPackage?        @relation(fields: [companyId, workPackageId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  unit              AccountUnit?        @relation(fields: [companyId, unitId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)

  @@unique([companyId, id])
  @@unique([revisionId, position])
  @@map("quotation_lines")
}

model QuotationAdjustment {
  id         String                  @id @default(uuid()) @db.Uuid
  companyId  String                  @db.Uuid
  revisionId String                  @db.Uuid
  position   Int
  type       QuotationAdjustmentType
  label      String
  valueType  QuotationValueType
  value      Decimal                 @db.Decimal(18, 4)
  amount     Decimal                 @db.Decimal(18, 2)
  taxable    Boolean                 @default(false)
  taxRate    Decimal                 @default(0) @db.Decimal(5, 2)
  taxAmount  Decimal                 @default(0) @db.Decimal(18, 2)
  company    Company                 @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  revision   QuotationRevision       @relation(fields: [companyId, revisionId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)

  @@unique([revisionId, position])
  @@map("quotation_adjustments")
}

model QuotationPaymentSchedule {
  id             String             @id @default(uuid()) @db.Uuid
  companyId      String             @db.Uuid
  revisionId     String             @db.Uuid
  position       Int
  label          String
  valueType      QuotationValueType
  value          Decimal            @db.Decimal(18, 4)
  amount         Decimal            @db.Decimal(18, 2)
  dueDescription String?
  company        Company            @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  revision       QuotationRevision  @relation(fields: [companyId, revisionId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)

  @@unique([revisionId, position])
  @@map("quotation_payment_schedules")
}

model QuotationShare {
  id         String            @id @default(uuid()) @db.Uuid
  companyId  String            @db.Uuid
  revisionId String            @db.Uuid
  tokenHash  String            @unique @db.Char(64)
  expiresAt  DateTime?
  revokedAt  DateTime?
  createdAt  DateTime          @default(now())
  company    Company           @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  revision   QuotationRevision @relation(fields: [companyId, revisionId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)

  @@index([companyId, revisionId])
  @@map("quotation_shares")
}

model QuotationAuditEvent {
  id             String                  @id @default(uuid()) @db.Uuid
  companyId      String                  @db.Uuid
  documentId     String                  @db.Uuid
  actorId        String?                 @db.Uuid
  eventType      QuotationAuditEventType
  revisionNumber Int?
  note           String?
  createdAt      DateTime                @default(now())
  company        Company                 @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  document       QuotationDocument       @relation(fields: [companyId, documentId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)
  actor          User?                   @relation("QuotationAuditActor", fields: [companyId, actorId], references: [companyId, id], onDelete: Restrict, onUpdate: Restrict)

  @@index([companyId, documentId, createdAt])
  @@map("quotation_audit_events")
}

model CommercialDocument {
  id String @id @default(uuid()) @db.Uuid
  companyId String @db.Uuid
  branchId String @db.Uuid
  type CommercialDocumentType
  status CommercialDocumentStatus @default(DRAFT)
  documentNumber String @db.VarChar(100)
  customerId String? @db.Uuid
  vendorId String? @db.Uuid
  partyId String @db.Uuid
  partyName String @db.VarChar(240)
  sourceDocumentId String? @db.Uuid
  purchasePurpose PurchasePurpose?
  purchaseClassification PurchaseClassification?
  projectReference String? @db.VarChar(240)
  issueDate DateTime @db.Date
  dueDate DateTime? @db.Date
  subtotal Decimal @db.Decimal(18,2)
  discountTotal Decimal @default(0) @db.Decimal(18,2)
  taxableTotal Decimal @db.Decimal(18,2)
  taxTotal Decimal @db.Decimal(18,2)
  grandTotal Decimal @db.Decimal(18,2)
  balanceDue Decimal @db.Decimal(18,2)
  notes String?
  journalEntryId String? @db.Uuid
  createdById String @db.Uuid
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  company Company @relation(fields:[companyId],references:[id],onDelete:Restrict,onUpdate:Restrict)
  branch Branch @relation(fields:[companyId,branchId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  customer Customer? @relation("CommercialCustomer",fields:[companyId,customerId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  vendor Vendor? @relation("CommercialVendor",fields:[companyId,vendorId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  journalEntry JournalEntry? @relation("CommercialJournal",fields:[companyId,journalEntryId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  createdBy User @relation("CommercialDocumentCreator",fields:[companyId,createdById],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  sourceDocument CommercialDocument? @relation("CommercialSource",fields:[companyId,sourceDocumentId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  adjustments CommercialDocument[] @relation("CommercialSource")
  lines CommercialDocumentLine[]
  reminders PaymentReminder[]
  allocations SettlementAllocation[]
  advanceApplications AdvanceApplication[]
  @@unique([companyId,id])
  @@unique([companyId,branchId,type,documentNumber])
  @@index([companyId,type,issueDate])
  @@index([companyId,customerId])
  @@index([companyId,vendorId])
  @@map("commercial_documents")
}
model CommercialDocumentLine {
  id String @id @default(uuid()) @db.Uuid
  companyId String @db.Uuid
  documentId String @db.Uuid
  position Int
  lineType CommercialLineType
  productId String? @db.Uuid
  serviceId String? @db.Uuid
  workPackageId String? @db.Uuid
  itemName String @db.VarChar(240)
  itemCode String? @db.VarChar(80)
  description String?
  specification String?
  unitName String? @db.VarChar(80)
  unitSymbol String? @db.VarChar(20)
  quantity Decimal @db.Decimal(18,4)
  rate Decimal @db.Decimal(18,2)
  discountType DiscountType?
  discountValue Decimal? @db.Decimal(18,4)
  baseAmount Decimal @db.Decimal(18,2)
  discountAmount Decimal @db.Decimal(18,2)
  taxableAmount Decimal @db.Decimal(18,2)
  taxRate Decimal @default(0) @db.Decimal(7,4)
  taxAmount Decimal @db.Decimal(18,2)
  lineTotal Decimal @db.Decimal(18,2)
  document CommercialDocument @relation(fields:[companyId,documentId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  product AccountProduct? @relation(fields:[companyId,productId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  service AccountService? @relation(fields:[companyId,serviceId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  workPackage WorkPackage? @relation(fields:[companyId,workPackageId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  @@unique([companyId,id])
  @@unique([companyId,documentId,position])
  @@map("commercial_document_lines")
}
model AccountSettlement {
  id String @id @default(uuid()) @db.Uuid
  companyId String @db.Uuid
  branchId String @db.Uuid
  type SettlementType
  status SettlementStatus @default(POSTED)
  settlementNumber String @db.VarChar(100)
  partyId String @db.Uuid
  customerId String? @db.Uuid
  vendorId String? @db.Uuid
  idempotencyKey String @db.VarChar(120)
  paymentMode PaymentMode
  amount Decimal @db.Decimal(18,2)
  remainingAmount Decimal @default(0) @db.Decimal(18,2)
  transactionDate DateTime @db.Date
  reference String? @db.VarChar(160)
  notes String?
  journalEntryId String @db.Uuid
  createdById String @db.Uuid
  createdAt DateTime @default(now())
  company Company @relation(fields:[companyId],references:[id],onDelete:Restrict,onUpdate:Restrict)
  branch Branch @relation(fields:[companyId,branchId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  journalEntry JournalEntry @relation("SettlementJournal",fields:[companyId,journalEntryId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  createdBy User @relation("SettlementCreator",fields:[companyId,createdById],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  customer Customer? @relation("SettlementCustomer",fields:[companyId,customerId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  vendor Vendor? @relation("SettlementVendor",fields:[companyId,vendorId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  allocations SettlementAllocation[]
  applications AdvanceApplication[]
  @@unique([companyId,id])
  @@unique([companyId,branchId,type,settlementNumber])
  @@unique([companyId,idempotencyKey])
  @@index([companyId,partyId,transactionDate])
  @@map("account_settlements")
}
model SettlementAllocation {
  id String @id @default(uuid()) @db.Uuid
  companyId String @db.Uuid
  settlementId String @db.Uuid
  documentId String @db.Uuid
  amount Decimal @db.Decimal(18,2)
  createdAt DateTime @default(now())
  company Company @relation(fields:[companyId],references:[id],onDelete:Restrict,onUpdate:Restrict)
  settlement AccountSettlement @relation(fields:[companyId,settlementId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  document CommercialDocument @relation(fields:[companyId,documentId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  @@unique([companyId,settlementId,documentId])
  @@map("settlement_allocations")
}
model AdvanceApplication {
  id String @id @default(uuid()) @db.Uuid
  companyId String @db.Uuid
  advanceId String @db.Uuid
  documentId String @db.Uuid
  amount Decimal @db.Decimal(18,2)
  applicationDate DateTime @db.Date
  idempotencyKey String @db.VarChar(120)
  journalEntryId String @db.Uuid
  createdById String @db.Uuid
  createdAt DateTime @default(now())
  company Company @relation(fields:[companyId],references:[id],onDelete:Restrict,onUpdate:Restrict)
  advance AccountSettlement @relation(fields:[companyId,advanceId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  document CommercialDocument @relation(fields:[companyId,documentId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  journalEntry JournalEntry @relation("AdvanceApplicationJournal",fields:[companyId,journalEntryId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  createdBy User @relation("AdvanceApplicationActor",fields:[companyId,createdById],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  @@unique([companyId,idempotencyKey])
  @@unique([companyId,advanceId,documentId,idempotencyKey])
  @@map("advance_applications")
}
model PaymentReminder {
  id String @id @default(uuid()) @db.Uuid
  companyId String @db.Uuid
  documentId String @db.Uuid
  customerId String @db.Uuid
  remindAt DateTime
  message String @db.VarChar(1000)
  status PaymentReminderStatus @default(PENDING)
  sentAt DateTime?
  createdById String @db.Uuid
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  company Company @relation(fields:[companyId],references:[id],onDelete:Restrict,onUpdate:Restrict)
  document CommercialDocument @relation(fields:[companyId,documentId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  createdBy User @relation("PaymentReminderActor",fields:[companyId,createdById],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  customer Customer @relation("PaymentReminderCustomer",fields:[companyId,customerId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  @@index([companyId,remindAt,status])
  @@map("payment_reminders")
}
model CommercialAuditEvent {
  id String @id @default(uuid()) @db.Uuid
  companyId String @db.Uuid
  eventType CommercialAuditEventType
  entityType String @db.VarChar(60)
  entityId String @db.Uuid
  actorId String? @db.Uuid
  metadata Json?
  createdAt DateTime @default(now())
  company Company @relation(fields:[companyId],references:[id],onDelete:Restrict,onUpdate:Restrict)
  actor User? @relation("CommercialAuditActor",fields:[companyId,actorId],references:[companyId,id],onDelete:Restrict,onUpdate:Restrict)
  @@index([companyId,entityType,entityId,createdAt])
  @@map("commercial_audit_events")
}
