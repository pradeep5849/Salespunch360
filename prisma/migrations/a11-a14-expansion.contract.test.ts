import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const migration=(n:string)=>readFileSync(`prisma/migrations/${n}/migration.sql`,"utf8");describe("A11-A14 forward-only migrations",()=>{it.each(["20260911210000_a11_inventory","20260911220000_a12_gst_taxes","20260911230000_a13_assets"])("keeps %s safe",n=>{const sql=migration(n);expect(sql).not.toMatch(/session_replication_role|DROP TABLE|TRUNCATE|DELETE FROM|UPDATE\s+\w+/i);expect(sql).toContain("RESTRICT")});it("makes stock immutable and idempotent",()=>{const sql=migration("20260911210000_a11_inventory");expect(sql).toContain("stock_movements_source_idempotency_key");expect(sql).not.toContain('"currentStock"')});});
