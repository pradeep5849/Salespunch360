import {readFileSync} from "node:fs";import {describe,expect,it} from "vitest";const sql=readFileSync(new URL("./20260910030000_a4_a5_sales_purchases_modules/migration.sql",import.meta.url),"utf8");
describe("A4/A5 migration",()=>{it("backfills only Account and Plus with every safe implemented module",()=>{expect(sql).toContain("SALESPUNCH360_ACCOUNT");expect(sql).toContain("SALESPUNCH360_PLUS");expect(sql).toContain("QUOTATIONS_BOQ");expect(sql).not.toMatch(/WHERE[^;]+SALESPUNCH360'\)/)});it("stores documents without destructive cascades",()=>{expect(sql).toContain('CREATE TABLE "commercial_documents"');expect(sql).not.toContain("ON DELETE CASCADE")});it("creates controlled business and module enum types",()=>{expect(sql).toContain('CREATE TYPE "BusinessType"');expect(sql).toContain('CREATE TYPE "AccountModule"')})});
