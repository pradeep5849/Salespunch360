import { readFileSync } from "node:fs"; import { describe, expect, it } from "vitest";
const schema = readFileSync("prisma/schema.prisma", "utf8"), migration = readFileSync("prisma/migrations/20260912120000_a15_a16_account_finalization/migration.sql", "utf8"), pkg = JSON.parse(readFileSync("package.json", "utf8"));
describe("A15-A17 migration and deployment contracts", () => { it("adds forward-only tenant-scoped foundations", () => { for (const model of ["AccountImportJob", "AccountRecycleRecord", "CustomFieldValue", "PrintTemplate", "AccountBackupRecord"]) expect(schema).toContain(`model ${model}`); expect(migration).not.toMatch(/DROP TABLE|session_replication_role|UPDATE\s+"journal/i); }); it("preserves the deployed repair prebuild and Prisma versions", () => { expect(pkg.scripts.prebuild).toBe("prisma generate && node scripts/repair-a11-a14-runtime.mjs && node scripts/migrate-deploy-safe.mjs"); expect(pkg.dependencies.prisma).toBe("6.12.0"); expect(pkg.dependencies["@prisma/client"]).toBe("6.12.0"); }); it("enforces closed Financial Years in posting code", () => expect(readFileSync("src/lib/accounting/service.ts", "utf8")).toContain('fy.status==="CLOSED"')); });
